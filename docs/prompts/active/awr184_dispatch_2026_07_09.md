# AWR-184 dispatch — real-void blackout rung (labels batch-1, operator-directed)

doc_status: current
truth_level: dispatch-brief
date: 2026-07-09 evening (dispatched ~18:15 by superman4, executive seat)

## Part A — verified ground truth (executive-verified at HEAD d455e67)

Operator directive (via labels lane, AWR-182, his words): "changes need to be moving
now, not postponed. every 10 tracks i give you needs to be added to whatever system
we have and used for tuning." This round is batch-1, candidate-1.

The gap (operator labels + labels-lane measurements, recorded in
`docs/research/operator_track_labels_2026_07_09.md` items UT-3 / UT-6 / UT-8):

- Utopia (Dombresky): operator labels **1:27 = "2 bar blackout"** and
  **3:00 = "1 bar blackout"**. Both measured as REAL musical voids: sub collapses
  to −25..−28 dB for exactly 8 beats into drop b192; deepest track trough beats
  379–382 into b384.
- The F2 plan answers BOTH with **balloon-shrink**, not blackout.

Why (seams verified by the executive at HEAD, `lighting_moments_v2.py`):
- `:99` `BALLOON_PERC_BOUNDARY = 0.35` — build-window `perc_full <` this ⇒
  balloon-shrink (melodic swell). **AWR-147-adjacent pinned constant — DO NOT CHANGE.**
- `:399` `darkness_ladder()` — the decision ladder.
- `:438` rung 0 = Part H TRUE SILENCE (AWR-180): requires the **full band**
  collapsed (Killa 513-521 pin). Utopia's voids are **sub-band** collapses; they
  don't clear the full-band rung.
- `:463` rung 1 = BALLOON: `perc_build < 0.35 and not stop` ⇒ balloon. Utopia's
  NEUTRAL-melodic drops land here. That is the gap: a real measured void answered
  with a shrink, never darkness.

## Part B — scope

ONE additive rung in `darkness_ladder()` in `lighting_moments_v2.py`, placed AFTER
the Part H true-silence rung and BEFORE the balloon rung: a deep sub-band void
sustained over a bar-scale span into the drop ⇒ `blackout` for the void's length
(bar-quantized), instead of falling through to balloon.

FIRST TASK — evidence gate, before any code: print the actual per-beat cache
curves (sub band + full band) for Utopia at b192-window and b384-window from the
real spectral cache, and confirm the discriminating signal exists and separates
these two voids from (a) Caramelle's −14 dB melodic swell (`:363`, the canonical
balloon case — MUST stay balloon) and (b) at least one more perc<0.35 melodic
track without a deep sub void. If the cache cannot discriminate, STOP — write the
`.blocked` signal with the curves you printed. Do not invent a proxy feature.

New thresholds: additive, desk-calibrated, named-evidence comments (same style as
the Part H block `:130`). No existing constant moves.

## Part C — non-scope

- NO change to `BALLOON_PERC_BOUNDARY`, the Part H thresholds, or any
  AWR-147-frozen decision constant (that class needs the library-scale re-check,
  which is post-20:00).
- NO consumer/dispatch-side changes; the rung only changes the planned darkness kind.
- NO bridge restart, NO config edits, NO process signals. Staged-only: the change
  activates at the executive-coordinated bounce.
- F2-off and scripted-deck paths byte-identical (the ladder is only consulted by F2 planning).

## Part D — acceptance (all must hold)

1. Utopia b192: darkness kind = `blackout`, 8 beats (his "2 bar blackout").
2. Utopia b384: darkness kind = `blackout`, 4 beats (his "1 bar blackout") —
   if the measured trough span (379–382) argues 4 beats ending AT the drop,
   pin what the ladder computes and note the delta for the operator.
3. Killa 513-521 true-silence pins UNCHANGED (Part H tests stay green untouched).
4. Caramelle −14 dB swell: STILL balloon (add the explicit control pin if absent).
5. Sexy 1:27 snare-wall rung behavior UNCHANGED.
6. One perc<0.35 melodic control WITHOUT deep sub void: STILL balloon.
7. Scoped tests green: lighting_moments suite + any file you touch; the three
   hard doc checks green; contract-first (find the matching key in
   `docs/agents/change_contracts.yml` BEFORE code; update its `docs_update` list after).
8. Commits by explicit paths only. Registry row AWR-184 (re-check the max id at
   write time — parallel lanes race).

## Part E — chain + signals

Build lane: ledfix3 (this dispatch). Reviewer: ledtune (LED manager) —
adversarial review at its desk. Gate: superman4 (executive), scoped-evidence gate
tonight (the consolidated suite window has passed by the time you land; your
scoped runs + ledtune's line-read + my read are the gate).

On completion: write `/tmp/rbss_lane_signals/ledfix3.A184.done` AND message tmux
session superman4 (one line: commits + test counts + which acceptance pins hold).
If blocked: `/tmp/rbss_lane_signals/ledfix3.A184.blocked` + the same message with
the evidence you printed. Run straight through; do not idle at checkpoints.
SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED language throughout.
