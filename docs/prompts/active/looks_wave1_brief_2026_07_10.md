# LOOKS wave-1 authoring brief — AWR-194 (operator campaign, gap-fill scope)

doc_status: current
truth_level: dispatch-brief
seat: `looks` lane, Fable/MEDIUM (operator's explicit tier), authoring-only.

## Operator order + executive scoping

Operator: "authorize up more looks... author a bunch of new looks for each phrase
and each energy level." Executive split (ratified): WAVE 1 NOW = fill the measured
pool gaps against CURRENT axes; WAVE 2 = the full phrase×family×energy matrix,
BANKED until the spectral decision-layer refactor lands (tier/family semantics are
about to be rebuilt — do not tune hundreds of looks to moving axes).

## Wave-1 scope (~25 drafts, all AUTHORED AS LAB DRAFTS, status "iterating" — the
operator's eyes accept later; NOTHING self-accepts, NOTHING promotes this round)

Per-family DROP identities where pools are starving (Round A live table):
- COMET t2/t3: +5 burst/comet-class looks (center bursts variants, dual-comet
  collisions, palette_comet variations — the new primitive is production-registered)
- NEUTRAL t2/t3: +5 flow/sparkle-class (nebula evolutions, soft bursts, NO strobes
  below the ceiling per the taste anchor)
- WALL t3: +3 wall/strobe-class escalations (duo-color + white punch patterns)
- HOUSE t2/t3: +4 chase+punch class (groove-driving chases, tasteful stab accents)
Section refresh (variety, energy-graded within current sections):
- post_drop: +4 (remnant/echo/decay family — pair-compatible tails)
- buildup: +2 ramp variants; breakdown: +2 breathing/starfield variants

## Authoring rules (all standing law)

- Palette-fed by default (`color_source: engine`, slots discipline, slot-5 white
  reserved) — baked colors ONLY for deliberate fixed identities, and then say why
  in the draft notes. Two-color discipline rides the palette engine.
- Time-based sparkles (AWR-153), Hz-gate discipline for anything strobe-class,
  peak-normalized heads (the 0.53x dip lesson), single-slot-per-pixel rule.
- Every draft: brief + notes fields filled (what it is, which family/tier/section
  it targets, the taste anchor it serves); param_specs complete with sane bounds
  (min<max, labels human-readable — the operator tunes these by hand).
- Names must NOT collide with production renderer effects (the Lab guard) and
  must be future-promotion-clean.
- WRITE THROUGH THE SERVER API (http://127.0.0.1:8766/api/lab/save) or the
  registry file with atomic-write discipline — FENCE: the `pad` lane is
  overhauling the pad/Lab server+UI concurrently; drafts data must stay
  format-compatible; coordinate any registry-format questions via the executive
  mailbox (tmux send-keys -t superman4), never assume.
- NO Fable Agent-tool subagents. No production files, no config banks/routing,
  no bridge contact. Drafts + the lab effects module only.

## Deliverable + signals

Report: draft names + one-line intent each + how wave-2 should extend them
post-refactor. /tmp/rbss_lane_signals/looks.WAVE1.report.md + .done (.blocked +
evidence if the Lab API fights you). Registry row AWR-194 (authoring campaign,
wave-1). Run straight through. SOFTWARE-VALIDATED ONLY; operator acceptance
happens at his desk later.
