---
doc_status: current
truth_level: executive seat-handoff state brief (AWR-195/AWR-239), written at handoff
last_verified_commit: HEAD at 2026-07-15 ~19:30 local — verify fresh at boot
last_verified_date: 2026-07-15
validation_scope: >
  Seat-state transfer only. Nothing here authorizes implementation or live
  operation. Successor must verify every load-bearing claim against code,
  ledger, and git before acting. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED.
---

# Spectral exec seat handoff — 2026-07-15 (Fable → Fable)

You are the **standing executive orchestrator** of Brandon's spectral AI program
(AWR-195/AWR-239). Predecessor retired at 72% context after the full pilot day.
Your memory store auto-loaded the program state
(`project_spectral_ai_program.md`, `user_true_drop_definition.md`,
`user_lighting_vision_orchestrated.md` — read them first). This brief carries
only the seat-operational delta. **Never notes over reality: re-verify fresh.**

## Where the program stands (all 2026-07-15)
- Pilot COMPLETE end-to-end in one day: spec → build → prep → freeze → sessions
  A/P1/P2/P3 (85/113 decisions, ledger chain verified) → scored.
- **Integrated verdict: FAIL, over-determined** (gates 2/3/4/6/7; 5 INCONCLUSIVE;
  1 PASS). Phase-2 embedding door CLOSED by rule. Full verdict UNSEALED to
  Brandon already — he has the complete picture in chat.
- **Headline:** runway_v1 diagnostic (his true-drop rule) 88.6% on genuine axis
  vs candidate 60.0% / baseline 65.7%. Post-hoc label — never promote via this
  pilot; needs a clean confirmation.
- Workspace: `local/spectral_ai_pilot/spectral-ai-pilot-v1-790c625-2026-07-14/`
  (gitignored; verdict.json/metrics.json/report.md + companion + all ledgers).
  Session evidence reports in its `scratch/` — incl. Brandon's rich LED look
  descriptions (creative gold; not yet moved anywhere durable).

## IMMEDIATE DUTY (operator-approved plan, in order)
1. **Author the TRUE-DROP spec** (predecessor promised it "tonight"): wire
   Brandon's rule into live F2 — drops fed to `build_track_plan` filtered to
   section-entry (`select_smart_drops`, smart_phrasing.py:657, 64-beat gap) AND
   buildup-runway-present (`runway_beats`, drop_presentation.py:173); the true
   drop's family/tier governs its whole section. Today F2 consumes RAW
   `read_anlz_drops` (state_manager.py:245→281). LIVE-BEHAVIOR CHANGE: full
   codex-spec-format spec, live-mixing reasoning (Part C), change contract
   (`spectral_analysis` + core), hostile review, then WAIT for Brandon's
   **"build it"** before any implementation dispatch.
2. After it lands: sustain+growl span protocols (his laser path) → family
   vocabulary redesign from his none/unsure data → cue expansion (SOL4 catalog)
   → optional AI-agent-labeler challenger through the same exam machinery.

## Seat operations (verbatim-critical)
- Topology: YOU orchestrate; execution rounds in Claude CLI tmux seats
  (dispatch: `tools/agents/dispatch_lane.sh SESSION MODEL EFFORT MSGFILE TAG`,
  watch signal files `/tmp/rbss_lane_signals/<session>.<TAG>.done|.blocked` —
  file-first; pane-idle watchers false-fire); non-Fable Agent-tool subagents OK
  for reads/adversarial reviews; NEVER Fable-tier subagents; NEVER Codex for
  this program. Worker lane = tmux `claude` (/clear per dispatch); operator
  listening seat = tmux `sessionA` (has /remote-control active; do NOT /clear
  it without checking with Brandon — it holds his creative-tangent context).
- Auto-sync Stop hook commits+pushes dirty tree each turn end;
  `/tmp/rbss_orchestration.lock` pauses it mid-round (remove after gating).
- Gate discipline that caught real defects today (keep it): fence-audit every
  commit, rerun tests at your desk, read the diffs yourself (Brandon explicitly
  requires the exec to personally review), reconcile suite reds BY NAME
  (baseline: 4 pack flappers + 2 pre-existing LED color-engine reds ~07-12;
  soundswitch_pack byte-identity tests flap on mid-run commits — isolate first).
- Dispatch briefs: quote spec constants VERBATIM (a "±2-beat" compression cost a
  re-freeze today); seats must use the real runner API; strict segment
  discipline in any future session driver.
- Communication: chat is Brandon's ONLY surface — everything fully in chat,
  never "see the doc". ELI5 with mechanism. No triumphalism; evidence class +
  untested remainder together. Safe defaults + veto-only asks. He is ANGRY
  (rightly) when agents re-ask things he's ruled — check memory FIRST.

## Open threads not to lose
- Family-repeat contradiction context (seat coaxed "idfk"→HOUSE) is documented
  in P3 evidence + companion file — part of any future repeatability reading.
- Workload accounting ambiguity (session-A zero-span segs; P2 stall bridged in
  seg 16) — resolved by presenting ALL computations; never a silent formula.
- Brandon wants 16+32 clip windows next time (deck was 16+16) — successor-pilot
  parameter, not retroactive.
- His verbatim vision is the north star (memory file) — every roadmap answer
  routes to it.
- Docs bookkeeping debt: this state file needs its doc_index/registry rows
  (do with your first docs commit).

Boot ritual: read memory files → `git log --oneline -15` + `git status` → verify
the three code cites above at HEAD → confirm to Brandon in chat that you have
the watch, then start duty #1. Retire this file (archive header) when the
program moves phases.
