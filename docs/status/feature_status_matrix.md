---
doc_status: current
truth_level: code-and-config-grounded
last_verified_commit: 0abbbff
last_verified_date: 2026-07-10
validation_scope: software-validated only plus Rekordbox 7.2.11 passive mixer RE evidence routing; Stream Deck palette control Package 2 software-tested; AWR-157 blank-role hold + reader freshness software-tested; AWR-160 phantom track-load stability gate software-tested; AWR-161 LED round 3 (Hz-gate migration + rainbow/firework promotions + center-burst fix) software-tested; AWR-186 USB gate-fix (Enttec positive-identity port detection + make_stick pack-config fail-closed) software-tested 2026-07-10, no hardware action; hardware-unvalidated in repo evidence
---


# Feature Status Matrix

Status vocabulary:

- `implemented`: runtime path exists in code.
- `software-tested`: tests exist or deterministic validation exists.
- `local-setup-operational`: I have it working locally, but repo hardware evidence is incomplete.
- `hardware-unvalidated`: no repeatable hardware-validation record in this repo.
- `experimental`: implemented or prototyped, but behavior may change.
- `partial`: some pieces exist, but important gaps remain.
- `planned`: not implemented yet.
- `unknown`: not enough evidence.
- `unsupported`: out of current scope.
- `stale/superseded`: old docs or plans no longer match code.

| Feature area | Implementation status | Validation status | Compatibility scope | Notes |
| --- | --- | --- | --- | --- |
| Core startup | implemented | software-tested indirectly | local setup | `__main__.py` wires the runtime components. When smart-rearm and spectral analysis are both enabled, stale spectral-cache eviction runs once on a daemon startup thread. |
| StateManager event/push loop | implemented | software-tested partially | local setup | Central owner for deck/runtime/lighting state. Ordinary loop exceptions are covered for tick-throttled skip/continue, bounded logging, process-control exception escape, and no duplicate pack ZERO after `_push_tick()` inner failures. Govee realtime-to-cloud teardown no longer performs transport calls on the StateManager caller path. |
| Runtime `[BEAT]` heartbeat | implemented | software-tested partially | local setup | `StatusWriter` emits a throttled status-only heartbeat and writes the same summary into status JSON. It exposes show deck separately from Rekordbox master when available. Hardware-visible output is unchanged and unvalidated. |
| Logging visibility (AWR-125 overhaul) | implemented | software-tested | local setup | One JSONL event stream (`bridge_log.py`) plus a read-only curses viewer (`bridge_view.py`) render four fixed lenses (PERFORMANCE/OPERATOR/SYSTEM/DEBUG) with latched WARNING+/health alerts; the retired `logging_manager.py` control-file/env-var-maze pipeline is gone. `scripts/ss_bridge_watcher.sh` opens one monitor window in both auto and manual launch modes; closing it never stops the bridge. It changes logging visibility and the watcher's monitor-window launch only, and remains hardware-unvalidated (viewer/watcher behavior, not lighting output). |
| SoundSwitch OS2L output | implemented | software-tested partially | local setup | Uses OS2L TCP and VirtualDJ-shaped messages. File-driven OS2L injection tooling is default-off unless `RBSS_OS2L_INJECT=1` or `RBSS_OS2L_INJECT_PATH` is set. Elapsed sends use raw elapsed values; the old zero-valued timing-compensation constant was removed. |
| Runtime audit P1 cleanup | implemented | software-tested | local setup | Smart-drop/breakdown callback failures now surface in runtime-command status; stale docs/comments and confirmed unused internal symbols were removed. No hardware-visible output path was validated. |
| SoundSwitch offline decoder/exporter/pack/verifier | implemented | software-tested | pinned SoundSwitch 2.10.3 canonical UUID/RAVE profile only | Strict read-only full rescan, deterministic repo-local canonical export, independent verification, dynamic saved-project inventory reconciliation, required binding-sidecar stage-before-swap publication, and one-click canonical replacement/reload. The old exact-count snapshot is retained only as an explicit proof gate; live export accepts internally consistent saved edits. |
| SoundSwitch scripted pack player/runtime/direct-DMX lane | partial | substantial software/wire tests; live runtime/hardware unvalidated | pinned pack/local setup only | Loader/player, MIDI adapter, backend abstraction, Enttec sender, default-off config, startup, StateManager driver, commands, copied RW-5 operational status, and the temporary Art-Net U1 truth-check gate exist. The MIDI adapter honors the SoundSwitch-saved Static Override Press/Toggle interaction mode (decoded byte; unknown fails closed to momentary) and auto-binds static-controller input unless an alias overrides it; missing/ambiguous controller input degrades manual Static Looks without disabling pack DMX. Truth-check requires `RBSS_ARTNET_TRUTH_CHECK=1` plus a valid universe, opens no Enttec/serial, keeps production software-zero while SoundSwitch is connected, and is measured by `tools/artnet_compare.py`; it is not hardware validation. Capture-derived parity registries now directly prove 16 active lanes and generalize the rest through the supported scripted/Autoloop layout family plus the C6 Static Look assertion; fresh export reports active lanes `algorithm_generalized: 67`, `oracle_proven: 16`, `unverified_parity: 0`, so the trusted publication software gate passes. The canonical pack lives at repo-local ignored `local/soundswitch/rbss_canonical_pack`. Status preserves degraded-input plus scripted-active truth, but software zero/attempted frames do not prove sender or fixture state; local pack hardware remains unvalidated. |
| SoundSwitch native-DMX Autoloops | implemented, default-off | resolver/player/loader/StateManager software-tested; live runtime/hardware unvalidated | pinned canonical pack/local setup only | StateManager selects canonical-pack Autoloops when SoundSwitch is absent, using the already-selected laser scene edge, 32-beat phase at 600 ticks/beat, and `phase_offset_beats`. Missing bindings/files/layout fail closed; all-zero authored looks are valid dark looks. Old T7d hidden-SoundSwitch-phase proof remains historical evidence/tooling, not a native gate. |
| Rekordbox memory position reader | implemented | software-tested partially | macOS local setup | macOS Mach APIs. Other OSes unsupported/unknown. Deck-2 playhead scan filters are numpy-vectorized with a GIL-yielding pure fallback (AWR-148) to kill the multi-second freezes (~54 ms/512 KB zone, ~427 ms/4 MB heap chunk up to 128 MB in the old pure loop); candidate values/order/logs unchanged, byte-identical vs an old-loop oracle in `tests/test_rb_memory_scans.py`, numpy optional. Deck-2 live_pos chain health is freshness-gated (AWR-157): a chain frozen for 5 consecutive reads while the external playing hint says playing now counts as a miss so the ObjC fallback re-engages, instead of trusting a dead value forever; a real pause is exempt; deck 1 is untouched. `tests/test_rb_memory_chain.py` (`ChainFreshnessTests`) covers it, pure seams only. |
| Rekordbox direct state reader | implemented | software-tested partially | current local Rekordbox only | Offset/version assumptions require validation per version. Transient ANLZ pointer-chain read failures no longer overwrite the previous successful ANLZ cache entry. Load emission (AWR-160) now requires a candidate track title to read identically for 3 consecutive deck ticks before `ANLZ_PATH`+`TRACK_LOADED` fire (ANLZ_PATH still first) — a stability window, not a playing/position requirement, so a load that never plays still emits. This suppresses library-browsing phantom loads (up to 168 fake loads measured in one mix) while a late-resolving ANLZ path can still catch up on its own once a title is confirmed. Every discarded pre-stability candidate logs a throttled DEBUG line and an edge-triggered 60s INFO storm summary. `tests/test_rb_state_reader.py` covers the gate, the FEIN never-played case, and the browse-storm/instrumentation behavior. |
| USB-export local-twin resolution (AWR-207) | implemented; staged | software-tested; hardware-unvalidated | operator laptop with local collection | A device UUID miss accepts exactly one BPM/duration + beatgrid-confirmed local twin. The payload carries local identity and `local_anlz_path`, restoring local PSSI/phrase, spectral/F2, SoundSwitch, and laser-tag inputs. Zero/ambiguous matches stay unresolved; device misses skip lsof. Local UUID/title behavior is unchanged. Foreign-laptop portability is AWR-208, not implemented here. |
| Rekordbox mixer active-deck authority | implemented | software-tested plus static/passive RE evidence for local Rekordbox 7.2.11 mixer chains; hardware unvalidated | current local Rekordbox 7.2.11 target only | `active_deck` is now the resolved show deck selected from Deck 1/2 playing state, upfader, LOW/BASS, `rb_master_deck` tie/fallback, validity/freshness, and stability state. `rb_master_deck` is separate. Named optional mixer offsets fail closed, `RBStateReader` publishes finite f32 mixer snapshots with concrete invalid reasons, refreshes valid raw Deck A/B master truth before stale expiry, invalidates raw Deck C/D/sentinel/unreadable master truth without aliasing, and fails closed on lost Deck 1/2 transport support. StateManager routes resolver inputs by default when named mixer offsets exist, legacy OSC/mirror/resume bypasses are gated while mixer authority is enabled, invalid/stale fallback goes through current valid/fresh `rb_master_deck`, idle `active_deck=0` clears fixed safe-deck OS2L/SoundSwitch outputs without `deck_route(0)`, and status/heartbeat expose show deck plus non-stale Rekordbox master separately. CFX FILTER and non-authority inputs do not affect active-deck authority. Thresholds/stability windows are implementation policy, not RE facts. No bridge restart, live process-memory sampling, SoundSwitch, laser, LED/Govee, DMX, MIDI, Enttec, or hardware-visible output validation has been performed. Other Rekordbox versions and real loaded-track play/stop survival remain unvalidated. |
| Live BPM | implemented | software-tested partially | current local Rekordbox only | Direct offset-table path plus discovery fallback. |
| MTC fallback | implemented | software-tested unknown | local setup | Fallback path, not primary compatibility proof. |
| Smart phrasing/drop/breakdown | implemented | software-tested partially | local setup | Must remain StateManager-owned at runtime. Selected smart-drop markers collapse clustered raw ANLZ candidates to the first marker of each drop section; raw ANLZ drops still feed phrase labels. |
| Laser Director policy | implemented | software-tested partially | local setup | The default-on gated drop/post-drop lifecycle is software-tested and mirrors the LED flat-window role gate: predecessor labels and real smart-drop crossings fire impacts, one capped chorus-to-chorus label boundary can re-fire the second drop hit, and later chorus boundaries demote to post_drop. `max_drops_in_a_row` caps those per-section hits. Execution remains separate. |
| Laser MIDI executor | implemented | software-tested partially | hardware-unvalidated in repo evidence | Drop/post-drop cycles use usable-only shuffle bags on autoloop ticks; disallowed high-impact or missing bank entries are skipped when a usable replacement exists, and blocked/missing selections restore cursor/active-scene state. Transient send-error degradation can reopen the MIDI port after a cooldown. Local rig may work, but broad safety/fixture support is not claimed. |
| Laser Pad web UI | implemented | software/frontend tested partially | local setup | Operator tool, not broad support claim. The master enabled toggle saves the draft and appends the live `set_laser_director` command; append failure is reported instead of silently succeeding. |
| Laser color (menu/follow-LED + brightness floor + CH9=90) | implemented | software-tested / hardware-unvalidated | local setup | Per-mood `menus` in `config/laser_color_map.json`; `_target()` follows the LEDs' last-emitted color (`color_state()["live_rgb"]`), applies a 3-tier brightness floor, fires the mood's chase on drops, drives CH9=90, and fails open to authored CH8/CH9. CH3/CH4 never touched — chase CH8 values (172/68/100/164/72) await operator live eyeball. Moods with no menu keep the legacy single-solid nearest-fixed pick. |
| LED Look Director | implemented | software-tested partially | local setup | Active bank behavior must be checked against code before changing docs. Committed drop selection uses the same DIY eligibility predicate as normal tick automation. LED drop-impact gating mirrors the laser pure resolver while staying StateManager-owned; real smart-drop crossings fire, one capped chorus-to-chorus label boundary can re-fire the second drop hit, and later chorus boundaries demote to post_drop. Long buildup, pre-drop, breakdown, and monotonic ambient sections now rotate looks every 32 beats by changing only the role-key cycle while keeping the section id stable; drop/groove/post_drop keys and blackout/scripted/manual gates are unchanged. The active-content hold still exists, but its phrase-less worst case is bounded by 16 beats / 8 seconds. Mixed-transport look rotation is deterministic (AWR-149): a pure `plan_backend_sequence()` evenly interleaves each role's realtime and cloud looks (realtime leads size ties) and replaces the removed WI-7 session-long transport-sticky latch; both transports stay reachable in every role that has both (operator contract, pinning rejected), cursors start at plan index 0 for a reproducible session phase, `reset_for_track()` is a no-op, and look-within-transport variety comes from per-`(role, backend)` shuffle bags. The `RBSS_LED_TRANSPORT_STICKY` env flag is deleted. Software-tested, hardware-unvalidated. |
| LED dispatch policy extraction | implemented | software-tested | hardware-unvalidated | LED trigger/accept/reject bookkeeping is centralized in `led_dispatch_policy.py` and mixed into `StateManager`; `_led_*` fields and runtime call sites remain on the StateManager instance. This is a pure refactor with no intended live behavior, status payload, or log-format change. |
| LED phrase-aware active-content hold | implemented | software-tested partially | hardware-unvalidated | `StateManager` LED automation holds the previous look after a nonzero active-deck switch or active-deck track load when the incoming track is more than `1.0` beat into its current phrase, then releases at the next phrase crossing when phrase data exists. Missing phrase data releases by a 16-beat backstop, and unreadable beat data releases after 8 seconds. `tests/test_led_state_manager.py` covers the boundary, backstops, arm/cleanup sites, reset-reason logging, and automation `perf.led.look` beat/phrase fields; room-visible behavior still needs operator hardware sign-off. |
| LED idle/pause ambient | implemented | software-tested | hardware-unvalidated | No-audible idle entry dispatches one ambient decision from the last audible deck; accepted realtime ambient decisions keep a synthetic 120 BPM idle beat until blackout/manual/playing automation clears it. Idle-grace teardown now sends blackout before deactivate. `tests/test_led_state_manager.py` and `tests/test_govee_realtime_runner.py` cover the software behavior; the firmware-revert explanation and room-visible pause result still need operator hardware sign-off. |
| LED scripted-track automation policy | implemented | software-tested partially | hardware-unvalidated | When enabled during `lighting_mode == "scripted"`, groove/drop/post-drop select the existing `utility` blackout bank while buildup/pre-drop and breakdown remain active. The shipped example config now enables the master switch (`true`) with the conservative blackout policy, so out-of-box scripted LED automation is active; room-visible behavior still needs hardware validation. |
| LED color engine M2 work | implemented/partial | software-tested partially | local setup | Current code includes color engine paths, fixed six-slot slot-color output, configurable slot-fill strategies including Patch S `random_with_mono_chance`, software-tested generic groove/post_drop/drop chase, drop center-burst, Patch E1 nebula slot cues, Patch E2 center-comet slot cue, Patch E3 ambient twinkle slot cue, and Patch F default-bank cleanup into generic slot looks plus `legacy_color_suffix` storage. The M2.5 slot-cue (Patch E1/E2/E3), color-engine core (decoupled color/drift/drop-snap), realtime-comet, and beat-sync paths (AWR-101–104) have operator hardware sign-off — 2026-06-29, Home Govee, visual; see `docs/validation/hardware_validation_log.md`. The solid-color strategy (Patch S), role-mapping v2 (AWR-105/106), and Patch D sparkle remain SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| Stream Deck palette control Package 2 | implemented | software-tested | local setup; hardware-unvalidated | Palette queue/override-fade/lock, manual-only `white_sand`, LED mute owner, laser mute binding through existing blackout ownership, Rainbow mode, feedback-file writer, pinned 15-key deck layout, runtime commands, and tracked example config are implemented. Laser Solo display/note is reserved for a later package. No bridge restart, deck-in-hand pass, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI-device, DMX, Enttec, or hardware-visible output validation was performed. |
| CFX filter-sweep LED overlay (AWR-173) | implemented | software-tested; hardware-unvalidated; bloom threshold + ramps pending desk calibration | current local Rekordbox 7.2.11 target only | Reads the CFX FILTER knob (param0 + selected-effect-id + unit-channel) for decks 1/2 as tracking-only (`_tick_cfx` → `Ev.CFX_STATE` → `CfxFilterSnapshot`), fully isolated from mixer/active-deck authority (never in `_authoritative_kinds`, never in `_tick_mixer`'s reads; the isolation pin is `tests/test_rb_state_reader.py::CfxTickTests`). LED overlay: clockwise-only (low→high) knob floods the strips toward the track's darkest v2 hue and dims continuously past a desk-calibrated bloom threshold; counterclockwise does nothing. Pure `cfx_sweep_envelope`, `CfxSweepConfig` (ships `enabled:false`), frame-space child overlay `scale(lerp(px,rgb,mix),dim)` on the ~20 ms `BeatAnchor` pump with `.get`-default wire fields (frozen-child safe). Blackout/emergency/F2-darkness structurally win. `tests/test_led_cfx_sweep.py` + CFX groups in `tests/test_rb_offsets.py`/`tests/test_rb_state_reader.py`. No bridge restart, process-memory sampling, or hardware output performed. |
| LIGHTING ENGINE v2 F1 identity + correction surface | implemented | software-tested | local setup; hardware-unvalidated | Default-off v2 identity branch, content-keyed store, zone dressing, async identity event wiring, load-generation stale guard, Stream Deck v2 zone/manual/max-energy surface, temporary menubar latch, runtime commands, and tracked example config are implemented. v1 remains the compatibility path when the latch is off or v2 is unconfigured. Max-energy is arm/consume/log only until F2. No bridge restart, deck-in-hand pass, live Rekordbox, SoundSwitch, laser, LED/Govee, MIDI-device, DMX, Enttec, or hardware-visible output validation was performed. |
| LED Pad + Template Lab | implemented/partial | software-tested | local setup; hardware-unvalidated | LED Pad Phases 1-3 and Template Lab Phase 2 are implemented. Locked Palette writes `color_engine.locked_palette_by_look`, locked playback ignores Test Palette, and renderer param unlocks keep frames identical when params are absent. Tests cover service/editor plumbing, locked-palette config/engine behavior, and renderer default parity. No bridge restart, live Govee output, or visual hardware validation was performed. |
| Govee cloud scene adapter | implemented | software-tested | local setup; hardware-unvalidated | Single API-key path, device compatibility not generalized. Mirror target send failures/recoveries now log one edge-triggered `[RGB] mirror-send-*` line and expose `mirror_send_ok`; successful sends clear a stale `circuit_open` degraded reason. Light output, send ordering, queueing, and rate limits are unchanged. |
| Govee realtime runner/transport | implemented/experimental | software-tested partially | local setup | H612D evidence exists in config examples, broad Govee support unknown. Realtime-to-cloud handoff teardown is runner-thread work, not caller-thread socket work; idle-grace teardown sends blackout before deactivate. |
| LED razer keepalive + blackout backstop + dispatch retry + pad mutual exclusion (AWR-145) | implemented | software-tested | hardware-unvalidated | `GoveeRealtimeRunner` re-asserts razer `activate()` on demand and unconditionally every 2 s while streaming (replaces WI-6 reconcile; `status()` reports `razer_assert_count`), so a cloud DIY scene that knocks the strip out of razer mode heals in ~2 s. Any-mode LAN brightness backstop: pure-emergency teardown hard-dims via `set_brightness(0)` before blackout (cloud handoff untouched); operator-blackout clear restores brightness 100. Coordinator-rejected automation/idle dispatches retry the SAME decision every 0.35 s (max 8) without re-ticking the director, cleared on every transition. `tools/led_pad_playback.py` auto-stops a playing pad when a fresh bridge owns the strip. `tests/test_govee_realtime_runner.py`, `tests/test_led_dispatch_coordinator.py`, `tests/test_led_state_manager.py`, `tests/test_led_pad_playback.py` cover the software behavior; no bridge restart or room-visible validation performed. |
| Govee frame engine child process (AWR-146) | implemented | software-tested | hardware-unvalidated | The realtime frame trio (`GoveeRealtimeRunner` + renderer + transport) now runs in a bridge-owned child process (`python3 -m rb_ss_bridge_v2.govee_frame_engine`) so bridge GIL contention (628 ms RBMEM scans, two-deck loads) can no longer starve LED frame timing. `GoveeFrameEngineClient` is a drop-in for the runner from the coordinator's point of view, talking to the child over an `AF_UNIX` `SOCK_STREAM` socketpair (newline-JSON IPC, 50 Hz beat anchors, `hb` heartbeats). Supervision respawns a dead/hung child with intent replay (emergency → `emergency_stop` + unconditional brightness-0; look → `set_desired` + `activate_assert`); every coordinator-facing client method is lock-and-flag with no push-loop socket I/O. `achieved_fps`/`engine_alive`/`respawn_count`/`fps_degraded` are surfaced in runtime status. Task 6: operator blackout now dims via `blackout_brightness()` regardless of runner active state (tactical pre-drop blackout still never dims). `tests/test_govee_frame_engine.py` and `tests/test_govee_frame_engine_integration.py` (60.9 fps this machine) cover the software behavior; live fps under real load and hardware are unproven. |
| Govee frame-engine scheduling-band self-report (AWR-151 Tasks 1-2) | implemented | software-tested | hardware-unvalidated | Instrumentation round only — no timing behavior changes; the runner is byte-identical. `raise_scheduling_band()` reads the live darwin band back via `getpriority(PRIO_DARWIN_PROCESS)` (0 = not backgrounded on this machine); every `hb` re-asserts `setpriority(PRIO_DARWIN_PROCESS,0,0)` and re-reads the band, carrying `band_setpriority`/`band_nsactivity`/`band_darwin_prio` (edge-triggered child INFO on value change). The client logs the band report once per (re)spawn into the jsonl with an edge-triggered health warning on a failed raise, and surfaces the three scalars through `status()` (whitelisted in `_sanitize_led_adapter_status`). The Task 3 adaptive precision-sleep backstop was DROPPED per the operator's root-cause doctrine (bandage). `tests/test_govee_frame_engine.py` (band-scalar heartbeat via faked libc seam, client passthrough/defaults, once-per-spawn jsonl log, sanitize whitelist) + `tests/test_govee_frame_engine_integration.py` (real child first `hb` carries `band_setpriority`/`band_darwin_prio`) cover software behavior. WHY the raise is only partially effective from the real launch chain is the separate Phase B root-cause investigation; the true fix is Phase C (executive-gated). Live fps under real load answered only by the operator's next mix. |
| LED drop-impact transport guarantee (AWR-150) | implemented | software-tested | hardware-unvalidated | Every drop impact renders realtime on the beat so it never waits on internet latency. Pre-drop blackout always rides realtime tactical frames when the adapter has them (Task 3.1, `e707199`; cloud `room_blackout` only for cloud-only adapters / no-preview ticks). When the committed drop pick is `cloud_diy` AND the adapter can stage AND the drop bank has a realtime look, the impact dispatches a realtime SUBSTITUTE (`LEDLookDirector.substitute_realtime_drop`, from the drop bank's `realtime_razer` subset, advancing only the `(drop, realtime_razer)` backend cursor — never the plan cursor — and queuing no pair) through the normal send path, and on accept stages the committed cloud scene via `LEDDispatchCoordinator.stage_cloud_takeover` (cloud send, NO realtime teardown / owner change / dwell double-record) plus an event-cancelled 30 s keepalive yield (`GoveeRealtimeRunner.request_keepalive_yield`, forwarded across the frame-engine IPC, cancelled by any new intent, not replayed on respawn). Drop recorded under the committed cloud identity; the AWR-145 retry stages exactly once; a stage failure logs and continues. Cloud drops stay in rotation; cloud-only adapters and cloud-only drop banks keep today's cloud dispatch. `tests/test_govee_realtime_runner.py`, `tests/test_led_dispatch_coordinator.py`, `tests/test_led_look_director.py`, `tests/test_led_state_manager.py` cover the software behavior; the live feel is the operator's next mix. |
| LED white-knob round 1 (AWR-152 #2/#3/#6/#7/#8/#10) | implemented | software-tested | hardware-unvalidated | `Palette.white`/`ZoneRampConfig.white` (both dead) replaced by `ZoneRampConfig.slot5_white` (per-zone RGB tint, resolved verbatim, no sat-floor/hue-shift); palate reset now dims all 6 slots including slot 5 (was 5 + hardcoded pure white); `_blend_white` and its 7 call sites deleted; `_led_diy_eligible_predicate` returns `None` under the v2 latch so cloud looks rotate evenly instead of filtering against a frozen v1 palette; `breakdown_star_twinkle` no longer picks slot 5 (matches `_slot_twinkle`'s 0-4 range); example config moves `groove_diy_bright_white_chase` to the buildup bank and carries per-zone `slot5_white` defaults. Live config untouched (read-only per spec); the operator's mirror + menubar restart is the remaining gate. `tests/test_led_identity_v2.py`, `tests/test_led_config.py`, `tests/test_led_color_engine.py`, `tests/test_led_state_manager.py`, `tests/test_govee_frame_renderer.py` cover the software behavior. |
| LED pad blackout unlatch fix (AWR-154) | implemented | software-tested | hardware-unvalidated | Fixed a real live defect: pad takeover set the `led_pad` blackout owner via `led_blackout reason=led_pad`, but pad release sent a bare `led_clear_blackout` with no reason, so the reason-blind clear chain (`runtime_status.py` zero-arg callback → `__main__.py` empty-payload event → `led_dispatch_policy.py` discarding only `legacy`) could never remove the pad's own claim. `led_pad_playback.py`'s `release()` now sends `reason=led_pad`; `parse_command()` accepts an optional non-empty `reason` on `led_clear_blackout`; `CommandReader` parses and passes it to the callback; `_led_clear_blackout(reason=None)` puts it in the event payload only when present. Absent reason resolves to an empty payload everywhere, so a bare clear stays byte-identical to before. `led_dispatch_policy.py`'s discard line was already correct and is untouched. `tests/test_led_pad_playback.py`, `tests/test_runtime_status.py`, `tests/test_led_state_manager.py` cover the software behavior. The only way to clear an already-latched blackout on the currently-running process is a bridge restart; this fix takes effect on the next restart. Superseded by AWR-155 below: bare-clear semantics changed from byte-identical/legacy-only to clear-all. |
| LED bare-clear fail-open (AWR-155) | implemented | software-tested | hardware-unvalidated | Executive-approved: a bare `led_clear_blackout` (no reason) is operator authority and now clears every owner in `led_dispatch_policy.py`'s `_led_blackout_owners` at once instead of only `legacy`; a reasoned clear is unchanged (still scoped to the named owner, per AWR-154). The no-reason branch snapshots the owners, clears the set, and emits one INFO outcome log (`[RGB] blackout-clear-all owners=...`) naming what was cleared — not per-tick. `_led_emergency_blackout` recomputes from the now-empty set so `restore_brightness()` fires like any other now-clear transition. Accepted, not guarded against: a bare clear during a lasers-only solo window also clears `drop_spotlight` and lights the LEDs mid-solo (intended operator override); the window's later release discard is a safe no-op on the already-empty set. `tests/test_led_state_manager.py` covers the software behavior. Effect begins at the next bridge restart; none was performed while implementing this. |
| LED round 2: strobe-gate rebuild + accepted-look promotion (AWR-156) | implemented | software-tested | hardware-unvalidated | Rebuilt `drop_white_aggressive`'s strobe gate on a new `_hz_strobe_on` (time-domain hz/duty, frame-timing-aware via a new runner-injected `frame_period_s` EMA), replacing the old stateless beat-domain 29 ms window that missed up to ~31% of cycles under jitter; 7 colorway strobes (`rt_drop_strobe_blue/_cyan/_green/_red/_red_white/_blue_cyan/_cyan_white`) ride the same gate on `drop_strobe_colorway`. Promoted 3 accepted looks: `buildup_balloon_comet` (baked, shrinking dual-head chase), `rt_groove_heartbeat` (engine-palette, 4 color_mode slot-routing variants), `rt_post_drop_firework_remnants` (engine-palette, time-based ember field, slot-5 dimming background) — all three share a new peak-normalized `_head_weights` helper that fixes a measured 0.53× between-pixel brightness dip. Knob #4: the intensity-derived comet slot mashup (`slot_coord = intensity * N`) is gone at all 8 renderer sites — a spawn now picks ONE fixed palette slot; intensity is brightness only. Knob #9: `width` is a real config knob on 7 cues (3 of them — `rt_groove_chase`/`rt_groove_nebula`/`rt_post_drop_center_comet` — previously hardcoded it and ignored `params` entirely; now read with the same fallback so an un-mirrored config is unchanged). Bank recast + rename: the two sparkle chases move drop→post_drop and are renamed `rt_post_drop_remnant_chase`/`rt_post_drop_remnant_nebula` (scene_ref unchanged); their `drop_pairs` entries are deleted. Task 9 (same-day operator refinement): a new `BAKED_WHITE_SLOT5_EFFECTS` set forces `post_drop_firework_chase`'s slot 5 to literal pure white; nebula white comets and the remnants background keep reading the zone tint. `tests/test_govee_frame_renderer.py`, `tests/test_govee_realtime_runner.py`, `tests/test_led_config.py`, `tests/test_led_pad_controls.py` (plus regression-lock updates across `tests/test_led_color_engine_m2_patch_*.py`) cover the software behavior. Live config untouched; the operator's mirror + menubar restart is the remaining gate. `strobe_red_white` side B (restored to white) and the groove-chase/nebula width 2.5 default are one-line operator veto points. |
| LED darkness-fix round: blank-role hold + reader freshness (AWR-157) | implemented | software-tested | hardware-unvalidated | New `blank_role_hold` config knob (default `true`) intercepts the automation dispatch path: when a decision's look equals the configured blackout look while the deck is audibly playing and a look was already accepted this session, the blackout dispatch is suppressed and the room holds its current look instead of falling dark — the actual mid-song darkness bug this round fixes. Emergency/manual/tactical blackout paths are structurally unreachable from the guard (test-pinned). Knob `false` restores byte-identical pre-round behavior. Paired with the deck-2 reader freshness gate (see the Rekordbox memory position reader row above). `tests/test_led_state_manager.py` (`BlankRoleHoldTests`), `tests/test_led_config.py` (`BlankRoleHoldConfigTests`), `tests/test_rb_memory_chain.py` (`ChainFreshnessTests`) cover the software behavior; the operator's next menubar start + mix is the remaining validation gate. |
| LED round 3: Hz-gate migration + rainbow/firework promotions + center-burst fix (AWR-161) | implemented | software-tested | hardware-unvalidated | Migrated the last ten BPM-tied `int(beat*16)%2`/`int(cue_beat*16)%2` strobe gates (18 effect names) onto the AWR-156 `_hz_strobe_on` gate; `hz`/`duty` now dialable on all of them. Promoted `rainbow_ordered` (ordered hue-by-position, beat-locked `travel_per_beat` movement fix, legacy `loop_beats` pace when absent) as `rt_rainbow_drop`/`rt_rainbow_post_drop`. Promoted `drop_firework_explosion` behind a measured contrast gate (post-surge ember deviation >= 60/255 at defaults — measured 101/255, PASSED) as `rt_drop_firework_explosion`, paired to the existing `rt_post_drop_firework_remnants` (the AWR-149 explosion->remnants arc, now real). Fixed `_slot_drop_center_burst`'s even-pixels-only gate (was lighting only half the strip). `tests/test_govee_frame_renderer.py` and `tests/test_led_color_engine_m2_patch_d.py` cover the software behavior; the operator's mirror + menubar restart plus a live mix are the remaining hardware gate. |
| Firework remnants sparse-tail rebuild (AWR-215) | implemented/staged | software-tested | hardware-unvalidated | Replaces `rt_post_drop_firework_remnants`'s full-strip slot-5 wash with only the sparse first-eight-beat `rt_drop_chase` sparkle mechanics; no comet half and dark from beat 8 onward. A 60-segment/480-frame dry-render test caps peak lit coverage at 20% and pins no background, flicker, reference parity, and duration. Bank/pair routing and the ignored live config are unchanged; no restart or hardware output was performed. |
| SoundSwitch export/operator UI | implemented | software-tested | pinned canonical project only | `Export from SS` publishes one canonical pack, shows exporting/reloading/result state, and uses only the conservative existing reload command. Verification now fails closed on loader-only runtime metadata gaps before publication, and the import report surfaces reserved-event/deactivated-script diagnostics. Pack output auto-switches by SoundSwitch connection using `set_soundswitch_pack action=enable`; a fresh disconnected `pack_start_failed` gets one bounded retry. There is no manual pack button, and auto-switching does not imply hot-enable without a real pack backend + Enttec port. The combined pack row is copied-state only; stale status renders `Lighting: no status yet`. SoundSwitch save remains operator-owned. |
| Multi-Rekordbox-version support | unknown | unvalidated | unknown | Needs explicit compatibility expansion tasks. |
| Windows/Linux support | unsupported/unknown | unvalidated | unsupported currently | Current reader architecture is macOS-bound. |
