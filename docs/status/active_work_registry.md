---
doc_status: current
truth_level: code-verified
last_verified_commit: 8147064
last_verified_date: 2026-07-09
validation_scope: software-only plus Rekordbox 7.2.11 passive mixer RE evidence routing for Deck 1/2 upfader, LOW/BASS EQ, CFX FILTER, Deck 1 mid fader, relaunch reacquire, and mixer-chain readability after operator-labeled master-button actions; Stream Deck palette control Package 2 plus AWR-121 gesture v2 and drop presentation policy Package 3/AWR-138/AWR-139 software-tested; Template Lab Rounds 1-3 (AWR-126) software-tested; laser color held-snapshot CH8 forwarding, LED pad queued-color restore, AWR-140 capped two-hit drop restore, AWR-141 realtime wrap guard, AWR-157 darkness-fix round (blank-role hold + reader freshness), AWR-159 Laser Solo fix round (cancel wire + honest manual arms + feedback truth), AWR-160 phantom track-load stability gate, and AWR-161 LED round 3 (Hz-gate migration + rainbow/firework promotions + center-burst fix) software-tested; hardware-output unvalidated
---

# Active Work Registry

This is the single repo-facing place for unfinished work. Old prompts and plans are not active unless listed here.

## Active engineering work

Each item points at an on-disk spec. **A spec is a plan, not proof of implementation — verify against current code before acting.**

| ID | Area | Spec | Verify |
| --- | --- | --- | --- |
| AWR-105 | LED role mapping v2 | `docs/plans/completed/led_role_mapping_v2_spec.md` | scripted groove/drop/post-drop blackout remap is implemented and software-tested in current worktree; broader role-mapping-v2 scope still needs verification against `led_look_director.py`/`led_models.py` |
| AWR-106 | LED color engine M2.5 solid-color strategy + Patch F cleanup | `docs/plans/completed/led_color_engine_solid_color_and_patch_f_spec.md` | Patch S `random_with_mono_chance` and Patch F tracked-example bank cleanup are implemented and software-tested only; live-config mirror remains operator-gated; all hardware behavior remains unvalidated |
| AWR-107 | SoundSwitch static exporter / bridge-native player | `docs/plans/active/soundswitch_exporter_remaining_work.md`, `docs/plans/active/soundswitch_README.md`, `docs/research/soundswitch/README.md`, `docs/architecture/native_autoloop_pack_authority.md`, `docs/plans/active/native_autoloop_dmx_runtime_spec.md`, `docs/validation/soundswitch_hardware_validation_procedure.md`, `docs/validation/soundswitch_hardware_runs/TEMPLATE.md` | **SOFTWARE-COMPLETE (2026-07-02 finalization pass):** values byte-proven (261/261 + A5 16/16), selection-beat autoloop anchor restored (grid anchor disproven by live capture), idle manual-overlay fix, playing-scrub dark latch, playing-sibling load guard, and edited-witness auto-retire all landed with tests; suite green, proof gate 29/0/0, active lanes `unverified_parity: 0`. Capture/exam-era plans and prompts are retired to `docs/plans/completed/soundswitch/` and `docs/prompts/completed/`; the Art-Net truth-check lane is dormant validation-only tooling, opt-in per launch via `RBSS_BRIDGE_TRUTH=1` on the watcher (normal launches are truth-off). The single remaining gate is the operator's live hardware run with the Enttec present. SOFTWARE/WIRE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-108 | Laser drop/post-drop lifecycle mirror | `docs/plans/completed/chorus_drop_cycling_spec.md` | Tasks 1-5 and Part D are implemented and software-tested at `9918dd4` and `ed78263`. Live LED behavior is unchanged. Laser hardware/SoundSwitch observation and kill-switch rehearsal remain pending; SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-109 | Stream Deck MIDI controller / layered static-look compositor | `docs/plans/active/streamdeck_midi_bridge_integration_spec.md` (Phase 1 implemented; Part F Phase 2 spec), `docs/prompts/active/streamdeck_phase2_codex_implementation_prompt.md` (implementation handoff), `docs/plans/active/streamdeck_phase2_plan_review.md` and `docs/plans/active/streamdeck_phase2_codex_review_prompt.md` (review evidence) | Phase 1 controller lifecycle hardening is implemented in current `main`. Phase 2 is a revised implementation plan only: generic layered DMX compositor, lock-free hot-path snapshots, worker-thread input-port-gone recovery, pure render path, sibling binding sidecar, and local controller LED state. No bridge restart, controller smoke, Enttec/DMX, fixture, or live hardware action is authorized. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-110 | Rekordbox mixer active-deck authority | `docs/architecture/active_deck_authority.md` (operator-authoritative target behavior), `docs/plans/active/rekordbox_mixer_active_deck_re_spec.md` (Ghidra/Ghidra-MCP RE + implementation handoff), `docs/research/rekordbox_mixer_active_deck_re_evidence.md` (current RE proof), `docs/prompts/reviews/rekordbox_mixer_active_deck_re_review_prompt.md` (adversarial RE review handoff) | Runtime implementation is present and software-tested at `0d3aa5c`. `active_deck` is now the resolved show deck selected from Deck 1/2 playing state, decoded upfader, decoded LOW/BASS, `rb_master_deck` tie/fallback, validity/freshness, and stability state; `rb_master_deck` is retained separately. Named mixer offset labels fail closed, mixer reads use finite range-checked f32 instead of the live-BPM float helper, partial/invalid snapshots invalidate authority, and status/heartbeat expose show deck plus Rekordbox master separately. Legacy OSC, playing-only mirror, and `_do_resume()` active-deck bypasses are gated while mixer authority is enabled; invalid/stale mixer fallback is resolver-mediated current valid/fresh `rb_master_deck` fallback only. CFX FILTER is confirmed RE evidence but is non-authority and is not decoded for active-deck authority in this implementation. Static Ghidra/GhidraMCP evidence plus operator-approved passive process-memory proof still applies only to local Rekordbox 7.2.11 mixer chains. Resolver thresholds/stability windows are implementation policy, not RE facts. Versions beyond 7.2.11, actual loaded-track play/stop survival, live runtime observation, SoundSwitch/laser/LED/Govee/DMX/MIDI/Enttec behavior, and hardware-visible behavior remain unvalidated. No bridge restart, additional live capture, process-memory sampling, or hardware action is authorized by the spec alone. |
| AWR-111 | Laser color (CH8/CH9 color control) + blackout re-wire | `docs/plans/active/laser_color_engine_design_spec.md` (expanded design), `docs/plans/active/laser_color_hold_spec.md` (held CH8 implementation spec), `docs/plans/active/laser_color_menu_spec.md` (menu/follow-LED/brightness-floor/CH9=90 implementation spec), `docs/architecture/laser_color_authority.md`, `docs/architecture/laser_blackout_authority.md` (operator-authoritative behavior contracts) | Blackout re-wire Package 1 is implemented/software-tested in current worktree: smart-side blackout latches survive pack-backend note rejection and feed the single StateManager pack mask writer alongside manual MIDI-input blackout. Laser color Package 4 plumbing is implemented/software-tested: the engine holds its last accepted LED-derived color snapshot, and StateManager forwards that held snapshot on every pack drive so CH8 stays palette-driven on every healthy native Autoloop frame instead of only the LED-trigger tick. Missing/disabled/all-null snapshots still pass authored CH8/CH9 through. CHART FIXED-COLOR HALF LANDED 2026-07-04: operator-supplied camera calibration (virtuallasernode project, 2026-06-05 sweep) — CH8 4-31 in 4-value bands, order W,R,Y,G,C,B,M; pack-authored values cross-validate cleanly; `config/laser_color_map.json` carries the seven fixed CH8 values and is ENABLED (operator decision 2026-07-04; supervised first visual pass = the remaining hardware gate). The bundled anchor change landed: quantizer `purple` anchor = (255,0,255), the fixture's magenta band. **MENU/FOLLOW-LED LAYER LANDED 2026-07-07 (software-tested / hardware-unvalidated):** `config/laser_color_map.json` gains `fixed_ch9: 90` and a per-mood `menus` block; `LaserColorEngine._target()` now follows the LEDs' actual last-emitted color (`color_state()["live_rgb"]`), applies a 3-tier brightness floor (laser never dimmer than LEDs), fires the mood's chase on drops, drives CH9=90, and stays fail-open. Moods with no menu keep the legacy single-solid pick. CH3/CH4 are never touched; the chase CH8 values (172/68/100/164/72) await a supervised live eyeball. Tests: `tests/test_laser_color_engine.py::LaserColorMenuTests` (10 cases). Still operator-pending: CH8 effect-family ranges (≥32) and the CH9 rainbow curve — they gate only the Rainbow laser tier and settle texture. **Operator verdict 2026-07-08 late night for the laser config round: VOLT → CH8 168 red+cyan APPROVED.** **LASER CONFIG ROUND LANDED 2026-07-09 overnight (manager-committed per executive release; software-tested):** `config/laser_color_map.json` — `rainbow_family.ch8 = 188` (no longer pending; CH9 rainbow curve remains null/pending), v2:ION menu adds `{chase 144, green+white}`, v2:VOLT adds `{chase 168, red+cyan}`, yellow-purge verified a NO-OP (no menu ever referenced yellow; the fixed quantizer chart keeps its calibration entry). Loader test pin updated to the approved truth (`tests/test_laser_color_engine.py` — 28/28 green from repo root; the module's parent-dir relative-path sensitivity is pre-existing and unchanged). All four new chase values (144/168/188 + existing 172/68/100/164/72) still await the supervised live eyeball. Package 3/drop presentation landed under AWR-119. All hardware-visible behavior remains unvalidated. |
| AWR-119 | Stream Deck palette control + drop presentation policy | `docs/plans/active/streamdeck_palette_control_design_spec.md` (expanded design), `docs/plans/active/streamdeck_palette_control_impl_spec.md` (Package 2 implementation spec), `docs/plans/active/drop_presentation_impl_spec.md` (Package 3 implementation spec), `docs/architecture/palette_control_authority.md`, `docs/architecture/drop_presentation_authority.md` (operator-authoritative behavior contracts) | Package 1 blackout re-wire is implemented/software-tested under AWR-111. Package 2 Stream Deck palette control is implemented/software-tested under the `streamdeck_palette` change contract: palette queue/override-fade/lock, `white_sand`, LED/Laser mute pads, Rainbow mode, feedback file, pinned pad iconography, runtime commands, and config/example wiring. AWR-121 updates that same surface to gesture v2: physical palette pads now tap-toggle queue/unqueue, long-press take-and-hold, tap the locked active pad to unlock, and key 6 is dark when `lock_note` is absent. Package 3 drop presentation policy is implemented/software-tested under the `drop_presentation` change contract: the pure planner/ladder/session/learned-store/window-machine (`drop_presentation.py`), hot-cue tag reading (`filepath_resolver.py`), laser base suppression (`soundswitch_laser_player.py`), and full `state_manager.py` wiring including the Laser Solo pad (arm/disarm/veto/learn), zero-RNG auto-solo tiers (hotcue/learned/gear-shift/record-breaker), finale guarantee, opening damper, darkness guard, and track personality. `enabled: false` is the master regression gate (every drop renders `leds_plus_lasers` exactly as today, byte-identical) and is verified green. Two reported limitations: impact detection reuses the Laser Director's own decision (inert if Laser Director is ever unconfigured, matching the operator's actual setup); the "manual interaction" fail-open trigger is tested at the window-machine level only, not yet wired to a state_manager detector. No bridge restart, live process, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action is authorized by the spec alone. 2026-07-04 evening: deck-surface hardening pass landed under this contract (authority rules 25-27, design spec Part D.2) — deck-script exception containment, read-thread liveness detect, stall watchdog, feedback/layout transition logging, latch clear on bridge restart, pass-through projections, feedback-writer fail/recover logs, real-caller-path tests; bridge-side siblings are findings only in `docs/plans/active/streamdeck_surface_hardening_findings_2026_07_04.md`; their implementation handoff (F-B1/F-B3/F-B4) is `docs/plans/active/streamdeck_bridge_side_hardening_impl_spec.md` — IMPLEMENTED 2026-07-04 night (Codex Tasks 1-8 + operator-sanctioned Sonnet fallback Tasks 9-12 after a Codex quota stop; suite 2954 OK): input group survives output failures and pack reloads, truthful `input_degraded` while output is disabled, `static_held` bridge-truth latches on the deck, watcher stop attribution + bridge-gap respawn with first watcher tests. F-B2 (retry log spam) still open. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-121 | Palette gesture v2 (tap-toggle + long-press take-and-hold) | `docs/plans/active/palette_gesture_v2_spec.md` (implementation spec), `docs/architecture/palette_control_authority.md` §Palette Selection Rules v2 banner (behavior contract) | implemented/software-tested in current `main` (`daa8804` verification anchor, full suite 2941 OK) under the `streamdeck_palette` change contract. Replaces the Package 2 v1 gesture surface (two-tap override + dedicated lock pad): tap = queue/unqueue toggle, long-press = take-and-hold (override-fade + lock), tap locked pad = unlock, key 6 dark when `lock_note` is absent, HSV value-only dim so idle pads keep their palette color, and top-level feedback carries `gesture: 2` plus `long_press_s`. Runtime commands keep explicit queue/override/lock/unlock semantics. Live-config mirror DONE 2026-07-04 (operator-authorized: `long_press_s: 1.0` — operator chose 1.0 over the 0.5 default — and `lock_note` removed; loader verified clean, no `palette_lock_pad` binding built). Remaining gates, operator-side: a menubar off→on cycle when convenient (not during a live set — it restarts bridge AND deck script together via the watcher exit trap + manual loop; the running processes serve v1 until then), then the deck-in-hand validation pass. No bridge restart, live process, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was authorized or performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-122 | USB bridge launcher design (macOS-only) | `docs/plans/active/usb_bridge_launcher_design.md` | **PAPER PHASE EXECUTED 2026-07-09 (operator re-raise; USB-workstream Fable manager): design re-verified in full at `9ead100`** — every file:line cite updated; drift folded: AWR-149 env delta (`RBSS_LED_TRANSPORT_STICKY` removed; the 2026-07-04 manual/auto env drift is fixed structurally — watcher modes now share one `start_bridge()`), AWR-125 log surface (`~/Library/Logs/rb_ss_bridge/`; `RBSS_RUNTIME_DIR` exists but covers logs only), AWR-151 `ProcessType=Interactive` requirement for any generated plist (+ `tools/check_launch_agents.py`), AWR-146 frame-engine child (`sys.executable -m` self-spawn breaks frozen — new §6 M1 must-prove), §5 port-map correction (SS pack MIDI-in is config-driven per-device, not a hardcoded IAC literal), dependency-manifest gaps (StreamDeck/Pillow/rtmidi/pyserial undeclared in `pyproject.toml`), cwd-relative stores flagged (`local/state/*` resolves to the repo PARENT today). The M1 spec lives at `docs/plans/active/usb_bridge_launcher_m1_codex_spec.md` (authored 2026-07-09). Track-file move-invariance designed separately (AWR-165). **BUILD PHASE OPENED 2026-07-09 ~03:30 (executive green light on operator approval, verbatim: "yes test the lights button is good" / "yes ill do the cert right now"): an Opus 4.8 orchestrator in tmux claude8 executes the M1 spec under the Fable build manager, amended by `docs/prompts/active/usb_m1_build_orchestrator_brief_2026_07_09.md`.** Decisions locked: guest-first SETTLED (operator: his MacBook is already the permanent install), Test-the-Lights replay button IN SCOPE (idea 9 answered), signing = Apple Development cert (idea 8 answered; identity checked at the signing task's turn — present ⇒ sign, absent ⇒ unsigned build + re-runnable sign step, never blocks). **FILE FENCE while F2/F4 build on claude4: zero edits to `state_manager.py`, `led_dispatch_policy.py`, `govee_frame_renderer.py`, `lighting_moments_v2.py`, or any laser/reader file; the frozen frame-engine re-exec task is sequenced LAST and dispatch-gated on executive F4 confirmation — all other M1 tasks build now.** AWR-107 dependency RE-SCOPED per the operator correction: pack path is proven-by-use; M1's must-prove is frozen-bundle equivalence. Live safety: no bridge starts, no pad restarts, live configs untouched; the closing hardware parity run is OPERATOR-gated. Operator decision recorded at parking (2026-07-04) stands: USB mode drives lighting via the bridge-native pack player (AWR-107), not SoundSwitch-on-host. **M1 BUILD COMPLETE 2026-07-09 ~05:15 (all tasks incl. the F4-gated Task 4; software-tested).** Landed: `launch_profile.py` single source (watcher consumes it fail-closed — one env list for dev + bundle by construction), `usb_launcher.py` entrypoint (`--run-bridge` in-process + govee.env sourcing, `--run-streamdeck`, `--run-frame-engine` dispatch pre-AppKit, `--replay-session`), Test-the-Lights (menubar action + `replay_event_source.py` with DOUBLE live-safety preflight — launcher/menubar AND bridge-side; a stray `RBSS_REPLAY_SESSION` on a live bridge refuses loudly), `launch_agent_plist.py` generator (`ProcessType=Interactive` hardcoded), `packaging/rbss_launcher.spec` + `sign.sh` (ad-hoc fallback taken — no cert on disk tonight; re-runnable), DMG built (`RBSS Bridge.dmg`, ~111MB), `pyproject.toml` bundle extras declared, frozen frame-engine re-exec (`122be01`: pure `_child_argv` seam, source-run byte-identical, frozen re-exec form). **Task 0 answer: PyInstaller 6.21.0 × Python 3.14.6 — the design's biggest unknown is dead, no fallback interpreter.** Build manager adversarial review PASS WITH REQUIRED FIXES; fix round F1-F4 landed (`78fec3e`) and independently re-verified. Suite at the documented known-red baseline every round (final attribution vs known-six; zero M1 reds); three hard checks green throughout; F2/F4 file fence held (zero M1 fingerprints in fenced files, verified by grep). Known misattribution, not a failure: `122be01` swept two dirty AWR-170 lane test files (`test_laser_tier_prechorus.py`, `test_state_manager_pack_driver.py`) — parallel-lane commit race, work preserved, wrong label. **REMAINING GATE (operator-only): the §2 parity run in `docs/setup/usb_launcher_runbook.md`** — bundled run vs watcher run on a test session, memory-read STOP rule in force. No bridge starts, no pad restarts, live configs untouched by the build. **M2 OPERATOR DIRECTIVE RECEIVED 2026-07-09 ~19:25 (attended usb lane), captured verbatim in `docs/plans/active/usb_launcher_m2_operator_directive_2026_07_09.md`:** one-action DMG install, automated cache/payload copy, home-parity (secrets-on-stick default YES with open veto), survive-unplug, menubar PURGE with explicit confirmation; R5 (read the stick inside the XDJ-RX3) re-answered IMPOSSIBLE per the settled AWR-167 verdict — performance mode with the stick in the laptop port is the working equivalent. Routed to superman4 for the fresh M2 executive gate (M1 close-out rule); Saturday stick-side install/purge .command helpers proposed as the interim. OPERATIONAL: operator renaming the stick — the after-20:00 AWR-183 sweep must target the NEW /Volumes/<name>. **Interim helpers BUILT + PROVEN same evening (superman4 constraints honored):** `packaging/stick/install.command` (mount DMG → app to `~/Applications` + `RBSS_payload/spectral_cache` install, file-level manifest of every created path; app+pre-warm ONLY, no secrets) + `purge.command` (typed `PURGE` confirmation, removes exactly manifest paths under allowed roots, `..` rejected, prunes empties — never a blanket delete); `tests/test_stick_commands.py` 4/4 (deletion scoping vs the real script, throwaway `$HOME`), desk smoke real-DMG→temp-HOME→install→purge clean; copied to the stick root; payload-export one-liner in runbook §Stick helpers (runs post-sweep). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-120 | Cross-platform portability plan | `docs/plans/active/cross_platform_portability_plan.md` | **PARKED with AWR-122 (operator 2026-07-04 evening; final-project bundle — resumes only when the bridge itself is done being added-to/refined/optimized/refactored).** State at parking: plan is REVIEWED, NOT YET REVISED — AWR-124 delivered `PASS WITH REQUIRED FIXES` and its findings R1-R11 + the five composition stitches are not folded into this doc. Pickup order: fold R1-R11 and the stitches FIRST (strobe-floor §4 respec, seam completeness incl. `live_bpm.py`, DMG-on-exFAT carrier, dependency manifest, Phase-2 verify respec, §6/§7 ad-hoc rewrite, Windows MIDI Services, path inventory), re-verify every file:line cite against then-current code (same-day drift already proven twice; AWR-125's logging overhaul will move the `/tmp` surface §3.5/R9 name), THEN author Codex specs. Standing note for the parked interval: AWR-124's R1/R2 show the strobe-floor gap (unclamped live-BPM ingress → SS tempo) exists on today's single-Mac rig, not just foreign hosts — extractable as a small standalone safety item independent of this project if the operator ever wants it sooner. **Operator decision recorded at parking (2026-07-04): USB mode uses the bridge-native pack player (AWR-107), not SoundSwitch-on-host** — at pickup also re-scope §1.3 (virtual-MIDI blocker shrinks to Stream Deck + internal wiring), §4 boundary honesty (pack mode renders DMX in-bridge, so authored strobes become clampable at the frame sender), Phase 3 verify (pack render + Enttec, not SS rotation), and AWR-124's R8/E2/stitch-3 (decision blockquote added to plan §0). Not implementation-authorizing. |
| AWR-123 | USB bridge launcher — Fable 5 design review | `docs/plans/active/usb_bridge_launcher_fable_review.md` | **PARKED with AWR-122 (operator 2026-07-04 evening; final-project bundle).** Review delivered 2026-07-04 (verdict `PASS WITH REQUIRED FIXES`); P1 findings F1-F4 + F5/F7/F8/F9 mechanics folded into the design at `02250de`; F6/F10-F12 are Codex-plan content for the (unwritten) M1 spec; Part 2 carries 15 ranked expansion ideas awaiting operator adopt labels at pickup. All claims verified at 2026-07-04 HEAD — treat file:line cites as stale on pickup. Analysis only; not implementation-authorizing. |
| AWR-124 | Cross-platform portability plan — Fable 5 review | `docs/plans/active/cross_platform_portability_fable_review.md` | **PARKED with AWR-120/AWR-122 (operator 2026-07-04 evening; final-project bundle).** Review delivered 2026-07-04 (verdict `PASS WITH REQUIRED FIXES`; composition ruling vs AWR-122 `COMPOSE` with five stitches, reconciled against AWR-123 with no disagreements). Findings R1-R11 + the stitches are NOT folded into AWR-120 — they are the first pickup work. Strobe-floor §4 ruled insufficient-as-specified (respec included in the report). All claims verified at 2026-07-04 HEAD `7aef68f` — treat file:line cites as stale on pickup. Analysis only; changes no runtime behavior; not implementation-authorizing. |
| AWR-125 | Logging overhaul (one JSONL event stream, 4 lenses, `bridge-view` TUI) | `docs/architecture/logging_authority.md` (intent contract, re-verified against as-built 2026-07-05), `docs/plans/active/logging_overhaul_design.md` (Phase 1 design), `docs/plans/active/logging_overhaul_spec.md` (Phase 2 spec, Tasks W1-W7) | **W1-W7 implemented/software-tested at `4f4c1ad`; suite 3180+ OK.** History: design authored 2026-07-04 (key decisions folded in: `RBSS_RUNTIME_DIR`-aware log dir for the parked USB launcher, ONE monitor window in both auto/manual launch modes, ADHD-first latched-never-transient alert contract, per-feature 3-question extension rule); Fable-orchestrated Sonnet 5 implementers built W1 (`bridge_log.py`) through W7 (watcher swap + docs closeout) same day under an operator-granted per-workstream exception (Codex not used for this workstream). W7 also fixed the manual-launch env-list drift AWR-123 found (manual mode now shares `start_bridge()` with auto) and re-verified `logging_authority.md` against as-built code (two amendments: SYSTEM lens legacy-infra clause is INFO+-gated; the TRACK_LOADED→FILEPATH_RESOLVED→SCRIPTED_ARM trace-id chain propagates via explicit thread-hop parameter-passing plus contextvar fallback, not automatically). **CLOSEOUT 2026-07-05 at `18689e9`: end-to-end integration proof landed (`tests/test_bridge_log_integration.py`, 9 tests — real subprocess stream through the viewer lens layer) and the final whole-diff adversarial review (Opus, fresh-context) returned PASS WITH REQUIRED FIXES + GO for operator first-run; all findings fixed same-session (os2l connect-fail guard re-arms, `format_age` tolerates corrupt ts, watcher reopens a missing viewer, handler exceptions count as drops). Suite 3189 OK.** Remaining: operator-only — bridge restart via menubar, `bridge-verify`, first-run viewer pass during a real mix. Known follow-ups (non-blocking): `tools/t7d_capture_conductor.py` greps the old `/tmp/bridge.log` INFO text (point it at `current.jsonl` cats before the next AWR-107 capture run); staleness tool has no per-contract `last_verified_commit` (global-only). 2026-07-05 operator-experience review (Fable): launch regression fixed (orphaned headless viewer suppressed reopen — viewer now exits on tty death, `monitor_open` requires a real terminal) and UX items 1-10 + 12 implemented same-session (heartbeat header-only, newest-first alert strip, plain-words display + friendly surfaces, sentence header, key legend, 140×40 window, severity-colored health list, watcher-log truncation fix, persistent acks via `viewer_acks.json`, 17 emit-site rewordings); item 11 spec authored, AWAITING CODEX: `docs/plans/active/logging_edge_trigger_warning_spam_spec.md` (edge-triggered health for repeat-warning spam + rb re-attach recovery). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-126 | Template Lab — creative + engineering direction | `docs/plans/active/template_lab_direction_2026_07_04.md` (direction, operator-approved rounds 1-3), `docs/plans/active/template_lab_round1_codex_spec.md`, `docs/plans/active/template_lab_round2_codex_spec.md`, `docs/plans/active/template_lab_round3_codex_spec.md` (Round 1-3 Codex implementation specs) | Round 1 (`template_lab_round1_codex_spec.md`) implemented/software-tested 2026-07-04: `/api/lab/update`, `/api/lab/switch`, `/api/lab/preview` on `LedPadService`, auto-apply + preview strip in the Lab UI, and the rewritten `template-lab` skill. Round 2 (`template_lab_round2_codex_spec.md`, `2040c1f`) implemented/software-tested 2026-07-04: `param_specs` slider/toggle controls and slot swatches in the Lab UI, JSON demoted under an Advanced disclosure. Round 3 (`template_lab_round3_codex_spec.md`) implemented/software-tested 2026-07-04 (housekeeping, `61304ad`): `LabRegistry.delete` + `POST /api/lab/delete` (refuses while that draft is playing, never touches `effects_lab.py`), and a default-hidden rejected-drafts filter chip in the Lab UI (`GET /api/lab/list` still returns every entry). Suite + hard docs checks pass for all three rounds; hardware/on-strip verification pending for all. |
| AWR-127 | Self-learning workflow (`/retro` skill + raw-launch guard) | `docs/plans/active/self_learning_workflow_design.md` (design + evidence base; companion prompt `docs/prompts/active/fable_self_learning_workflow_prompt.md`), `docs/plans/active/retro_launch_guard_spec.md` (Phase 2 Codex implementation spec) | open — verdict BUILD SMALLER; operator approved the build 2026-07-04. Phase 1 DONE 2026-07-04: global `/retro` skill live at `~/.claude/skills/retro/SKILL.md`; first retro run applied its full 5-item digest (stale watcher-path fix in two bridge memory files, `RBSS_SPECTRAL_ENABLE`/env-flag correction against the current watcher script, AWR-121 status correction, `MEMORY.md` closed-workstream split, lesson-placement rule added to the operator's global CLAUDE.md); high-water marks recorded per store in `memory/retro_state.md`. Memory-frontmatter status-field pass deliberately deferred (no mechanical consumer). Phase 2 spec authored, AWAITING CODEX: global PreToolUse guard (`~/.claude/settings.json`) denying raw `python -m rb_ss_bridge_v2` launches, fail-open, pure-seam tests, no bridge runtime behavior change; live deny-message probe is the post-implementation verify. Phase 3 (scheduled retros) not built — conditional entry criteria in design §3. Agent-workflow tooling only; changes no bridge runtime behavior. |
| AWR-128 | LIGHTING ENGINE v2 — F1 per-track color identity + Stream Deck correction surface | `docs/plans/active/lighting_engine_v2_f1_spec.md` (Codex implementation spec), `docs/architecture/LIGHTING_ENGINE_V2_DESIGN.md` + `docs/architecture/lighting_engine_v2_authority.md` (locked design/operator contract) | IMPLEMENTED/SOFTWARE-TESTED in current worktree. Scope remains F1 only: default-off v2 identity branch, deterministic zone/hue/depth dressing, content-keyed first-write-wins store (`local/state/led_identity_v2.json`), async measured/provisional identity arrival with `load_gen` stale-event guard, zone correction surface, manual `white_sand`/red/green/blue/Rainbow, first-play bloom, soft flip, palate reset, motion-style/travel look-selection bias, v1↔v2 live master latch, temporary menubar checkbox, runtime commands (`led_engine`, `led_manual_override`, `led_manual_clear`, `led_max_energy_toggle` plus zone names through `led_palette_queue`/`led_palette_override`), tracked example config, and max-energy arm/consume/log only (render unchanged until F2). v1 remains the compatibility path when v2 is off or unconfigured. **ACCEPTED — PASS WITH REQUIRED FIXES APPLIED (Fable acceptance review, 2026-07-06).** History: round 1 (f9172e8..a481d01) was structurally safe but dual review (fresh-Opus + Fable) found 3 MAJOR functional breaks + cleanups, recorded as spec Part F FR-1..FR-10; Codex implemented all ten in the fix round (landed via auto-sync `e0817c0` after Codex's context window died mid-closeout; Fable verified every FR item at the code, ran the full closeout, and finished the docs). Fix-round substance: FR-1 abs-beat pending-stamp windows (bloom fires after an 8-beat hold and lasts 8 beats; palate reset holds 4 beats — both now tested), FR-2 `moments_blocked` wired from blackout/manual/predark state, FR-3 consumer always freezes measured records (provisional→measured upgrade preserves corrections), FR-4 integration tests (stale-load_gen drop, correct-early→measured→clear, max-energy mutate-only, moments_blocked-through-predark, real-worker v4 cache-hit, flip-back journey-state bit-equality), FR-5 dip renders dimmed NEUTRAL, FR-6 TypeError shim deleted, FR-7 spec zone ramps restored, FR-8 TEMPORARY menubar comment, FR-9 observability catches narrowed, FR-10 explicit scripted stand-down guards on all v2 branches. Verification at acceptance: full suite 3307 OK; both contracts' test lists + `tests.test_bridge_menubar` OK; 3 hard doc checks green; staleness advisory residue is the known global-marker limitation (AWR-125 follow-up). Accepted tolerance: the scripted-mode probe reads host attrs via `getattr` defaults for test doubles (`led_dispatch_policy.py:688-697`) — reading-only, defaults to not-scripted. **Remaining gates are operator-only: live config mirror of the v2 block (ships `enabled:false`), menubar-watcher restart, and the F1 live pass (zone ramp taste, bloom/soft-flip/dip feel, correction store across restart).** No bridge restart, live process, or hardware action performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-129 | Laser Solo observability | `docs/plans/active/laser_solo_observability_spec.md` | Implemented/software-tested in the current worktree: bridge-received pad events now leave a DEBUG breadcrumb, Laser Solo feedback transitions and veto outcomes now emit `perf.override` records, and harmless structural `*key*` bookkeeping fields stay visible while real secret-shaped fields remain masked. Lighting behavior, arming/disarming, veto, learned-store, and feedback logic are intended unchanged. No live process, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-130 | LED intra-section look rotation | `docs/plans/active/led_intra_section_rotation_spec.md` | implemented/software-tested in the current worktree: long buildup, pre-drop, breakdown, and monotonic ambient LED role keys now carry 32-beat cycle terms so the look director can rotate inside long sections while the color engine keeps a stable section id. Drop, groove, post_drop role keys and blackout/hold/scripted/manual-override gates are unchanged by this patch. Focused LED suites cover role-key cycle math, stable section/cycle publication, unchanged drop/groove/post_drop key strings, and a dispatch-path second look across a buildup cycle boundary. No live process, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. hardware-unvalidated. |
| AWR-132 | LED hold starvation fix + LED freeze observability | `docs/plans/active/led_hold_starvation_fix_spec.md` | Implemented/software-tested in the current worktree. Active-deck switches and active-deck track loads still hold the prior LED look until the next phrase entry when phrase data exists, but missing/stale phrase data now releases by a 16-beat backstop or an 8-second no-beat fallback. The bridge logs `[RGB] hold-engaged`, `[RGB] hold-released`, and `[SP] reset-reason-change`, and accepted automation `perf.led.look` rows carry beat/phrase context. Root-cause label stays likely, not confirmed, until live logs show whether the observed freezes align with hold/reset episodes. No live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-133 | LED idle/pause ambient fix (no revert-to-random-scene) | `docs/plans/active/led_idle_pause_ambient_fix_spec.md` | Implemented/software-tested in the current worktree: no-audible idle entry now dispatches one ambient decision from the last audible deck, accepted realtime ambient decisions get a 120 BPM idle freewheel beat, blackout/manual/playing automation clears that freewheel, and idle-grace teardown sends blackout before deactivate. Watch `[RGB] idle-freewheel-start` and `deactivate reason=idle_grace blackout_sent=1`. No live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. The firmware-revert explanation remains hardware-assumed until an operator live pause lands on ambient. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-134 | LED pad color immediacy (realtime looks recolor on press) | `docs/plans/completed/led_pad_color_immediacy_spec.md` | Superseded-by-operator-decision on 2026-07-07 afternoon. The instant-apply behavior was removed by AWR-137; the old spec is marked superseded and must not be treated as current behavior. |
| AWR-137 | Restore queued pad-color semantics (revert AWR-134 instant-apply) | `docs/plans/active/led_pad_color_queue_restore_spec.md` | Implemented/software-tested in the current worktree. Manual color pad events mutate the color engine only; they no longer refresh the live realtime runner on the pad press. The next accepted LED automation dispatch with a new role key injects the updated engine color through `_led_inject_engine_colors(...)`, so color pads queue to the next look boundary again. The AWR-134 instant-apply cache, refresh method, coordinator color refresh, StateManager call, and instant-apply tests were removed; the pure color-injection refactor remains. No live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-140 | Restore capped two-hit drop rule after marker collapse | `docs/plans/active/drop_two_hit_rule_restore_spec.md` | Implemented/software-tested in the current worktree. The AWR-131 smart-drop marker collapse stays: selected markers still collapse clustered raw ANLZ candidates to one smart-drop marker per drop section, so pre-drop blackout and true-drop presentation impacts do not chain every 32 beats. The laser and LED drop-impact gates now restore the capped label re-arm: a section can render exactly two drop hits, the true smart-drop marker and one later chorus-to-chorus label boundary, capped by `max_drops_in_a_row` / `LED_MAX_DROP_IMPACTS = 2`; later chorus labels demote to `post_drop`. Focused tests cover the sequence, cap reset, single-marker unchanged path, LED/laser parity, and presentation no-reentry guard. Presentation-leak closed by AWR-143 (2026-07-07): the restored label re-arm was also emitting `reason="drop_crossing"` and mis-re-entering the presentation window; AWR-143 gates the presentation impact on a real smart-drop marker (the drop LOOK restored here is unchanged). No live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-141 | Realtime LED wrap-flicker guard | `docs/plans/active/led_realtime_wrap_flicker_fix_spec.md` | Implemented/software-tested in the current worktree. `TriggerClock.advance()` now holds backward beat moves smaller than `WRAP_HOLD_BEATS = 0.5` as live jitter or short rolls instead of treating them as track-loop wraps, so continuous realtime looks do not restart on tiny beat wobble. Real loops, cues, and seeks at or above the threshold still wrap and re-sync; forward crossing behavior stays capped by `MAX_CATCHUP`. No runner, renderer, WI-1 clamp, config, live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-142 | Smart-drop blackout transport + runway observability | `docs/plans/active/led_blackout_transport_observability_spec.md` | Implemented/software-tested in the current worktree. Observability only: `_dispatch_led_smart_drop_blackout` now records which transport carried each accepted pre-drop room blackout and how much runway it had. The realtime accept line keeps every existing field and appends `transport=realtime runway_beats=<beats_to_next_drop>`; the previously silent cloud accept now emits `[RGB] smart-drop-blackout-accepted transport=cloud phase=... next_drop=... runway_beats=... role_key=... active_deck=...`, so a cloud blackout that lands late is distinguishable from one that never armed. Transport selection, send order, return points, and the `_led_smart_drop_blackout_key` lifecycle are unchanged; the only additions are two log lines and the values they read. No new test asserts log text; the existing suite stays at its known baseline. No live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-143 | Drop-presentation label-rearm leak fix (AWR-140 follow-up) | `docs/plans/active/drop_presentation_label_rearm_leak_fix_spec.md` | Implemented/software-tested in the current worktree (2026-07-07). Fixes the AWR-140 presentation-impact leak found by the AWR-140 adversarial review and confirmed by code trace: the restored 2nd-chorus label re-arm makes the Laser Director emit `reason="drop_crossing"` without a real smart-drop marker, so `_drop_presentation_tick` mis-counted it as a true-drop impact and re-entered/extended the presentation window (~192 beats) at the 2nd chorus, breaking AWR-139's per-true-drop invariant. Fix is a one-line gate in `_drop_presentation_tick` (`impact_now = bool(impact_now and sp_state.smart_drop_crossing)`) so only a genuine smart-drop marker counts as a presentation impact; the AWR-140 drop LOOK is unchanged. A new focused test (`test_label_rearm_with_active_drop_still_set_does_not_reenter_window`) proves the leak is closed while `test_true_drop_inside_open_window_reenters_with_own_presentation` still re-enters on a real second marker. No live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-150 | Drop-impact transport guarantee (impact never gambles on internet latency) | `docs/plans/active/led_drop_impact_transport_guarantee_spec.md` | **implemented / software-tested; hardware-unvalidated (2026-07-08).** Every drop impact now renders realtime on the beat. Task 3.1 (pre-drop blackout always rides realtime tactical frames when the adapter has them; the cloud `room_blackout` scene remains only for cloud-only adapters and no-preview ticks) landed earlier as the interim guard `e707199` and is unchanged. New this round: when the committed drop pick is cloud AND the adapter can stage (`stage_cloud_takeover` present) AND the drop bank has a realtime look, the impact dispatches a realtime SUBSTITUTE (`LEDLookDirector.substitute_realtime_drop`, from the drop bank's `realtime_razer` subset — advances only the `(drop, realtime_razer)` backend cursor, never the plan cursor, and queues no paired post_drop) through the normal send path, and on accept stages the committed cloud scene via `LEDDispatchCoordinator.stage_cloud_takeover` (cloud send with NO realtime teardown, NO owner-state change, NO dwell double-record) plus an event-cancelled 30 s keepalive yield (`GoveeRealtimeRunner.request_keepalive_yield`, forwarded across the frame-engine IPC, cancelled by any new intent so a blackout always finds the keepalive awake, not replayed on child respawn). The drop is recorded under the committed cloud identity; the AWR-145 retry carries the committed decision so a rejected-then-accepted substitute stages exactly once; a stage failure logs and continues (the room is already lit on-beat). Cloud drops stay in rotation; cloud-only adapters and cloud-only drop banks keep today's cloud dispatch. Software-tested (`tests/test_govee_realtime_runner.py`, `tests/test_led_dispatch_coordinator.py`, `tests/test_led_look_director.py`, `tests/test_led_state_manager.py`); hardware-unvalidated — needs the operator's next live mix. |
| AWR-151 | Frame-engine scheduling-band follow-up (verify the raise, self-heal the timing) | `docs/plans/active/frame_engine_band_raise_followup_spec.md` | **PHASE B ROOT CAUSE CONFIRMED BY EXECUTIVE + PHASE C DEPLOYED TO MACHINE CONFIG 2026-07-08.** Phase B named the mechanism with a one-key flip proof: launchd throttles the coalition of any `ProcessType`-less LaunchAgent (probe agent unset = 28.1 fps — the Pad's exact 28 — vs `ProcessType=Interactive` = 60.0 fps; `getpriority` reads 0 in both, explaining every failed in-process lever; interpreter and ambient load eliminated). Phase C executed under the release conditions: `ProcessType=Interactive` in all four plists (incl. the disabled watcher), pads bounced + verified spawn type `interactive (4)` + serving (latent break root-fixed in passing: pad plists pinned Python 3.9 below the repo floor and crashed on `dataclass(slots=True)` at first restart — both pads now on homebrew 3.14), menubar bounced with the bridge down (executive-verified) so the operator's next normal start inherits the healthy coalition; `tools/check_launch_agents.py` = machine-local advisory guard (ProcessType + interpreter floor). Expected verification: `achieved_fps` ≈ 60 steady on the next mix. Reserve levers named-not-implemented: thread time-constraint policy; launchd timer-coalescing opt-out. History: reshaped by operator root-cause doctrine — Tasks 1-2 (band instrumentation + lever readback/re-assert) IMPLEMENTED / software-tested 2026-07-08 as root-cause enablement; Task 3 precision-sleep DROPPED as a declared bandage (never implemented); Phase B (offline spawn-band experiments: coalition inspection via launchctl procinfo on test children, spawn-attribute variations, detached-spawn comparison — never the live bridge) is the core deliverable with a HARD executive review gate on its findings before Phase C implements the true fix (correct scheduling placement at spawn and/or thread time-constraint policy on the frame thread).** Live evidence 2026-07-08: the child self-reported achieved_fps ≈33 with fps_degraded flapping in production — the AWR-146 raise is only partially effective when the band is inherited from the real launch chain (menubar→watcher→bridge→child), and the `[ENGINE]` band report went only to stderr. Landed (Tasks 1-2): band state (setpriority/NSActivity results + live getpriority read) on every heartbeat, per-heartbeat setpriority re-assert + getpriority re-read (edge-triggered child INFO on change), client once-per-spawn jsonl band report + failed-raise health edge + `status()` passthrough, and the `_sanitize_led_adapter_status` whitelist — instrumentation only, the runner is byte-identical (no `sleep_mode`, no precision sleep). Tests: `tests/test_govee_frame_engine.py` (band-scalar heartbeat via faked libc seam, client passthrough/defaults, once-per-spawn log, whitelist) + CI-skipped integration first-heartbeat band assertion; pure module green. Demotion mechanism stays [unknown] by design — visible now, named in Phase B, fixed in Phase C. Tasks 1-2 implemented / software-tested; live fps + hardware unvalidated. **First operator hardware signal 2026-07-08 evening, pad path only: mid-pad-session report that the pad effects are now "silky smooth wow" — the ProcessType fix visually confirmed on the pad LaunchAgents.** **LIVE-CONFIRMED ON THE REAL BRIDGE, post-restart drill 2026-07-08 night (executive-run): achieved_fps 59.94, fps_degraded false, "scheduling band healthy" in the log, single bridge process — the Phase C fix holds on the full menubar→watcher→bridge→child chain. Remaining: nothing; the AWR-151 verification gate is closed.** |
| AWR-149 | LED deterministic mixed-transport rotation (Phase 2 — planned alternation, NOT pinning) | `docs/plans/active/led_deterministic_transport_rotation_spec.md` | **Implemented / software-tested 2026-07-08.** Executive constraint (operator taste 2026-07-08, do not re-litigate): both cloud DIY and realtime looks stay in EVERY role; the old pin-roles-to-transports framing is REJECTED. Replaces the session-long transport coin flip (random initial cursor `led_look_director.py:63` + palette DIY-eligibility + WI-7 sticky latch `:314-328`, env `RBSS_LED_TRANSPORT_STICKY` set by the watcher `:166`) with a deterministic Bresenham backend interleave over each role's filtered bank: transport per pick = pure plan function of bank composition (realtime leads ties, F2-forward), look-within-transport keeps shuffle variety via per-(role,backend) bags, cursors start at 0 (deterministic session phase), `reset_for_track` becomes a documented no-op. Frequent transport transitions are safe via the AWR-145 keepalive/assert/retry + AWR-146 frame-engine chain. F2-forward: RT drop picks recur at planned cadence for future within-drop choreography to ride; cloud drops keep their scenes. Implemented as `plan_backend_sequence()` + per-`(role,backend)` cursors/bags in `led_look_director.py` (cursors init 0, `reset_for_track` no-op), the `RBSS_LED_TRANSPORT_STICKY` line removed from the watcher, and `LEDLookDirectorPlanRotationTests` in `tests/test_led_look_director.py`. Files: `led_look_director.py`, watcher env line, director tests, docs. Implemented / software-tested; hardware-unvalidated — live gate is the operator's next mix. |
| AWR-146 | Govee frame engine process (lift runner+renderer+transport into a bridge-owned child process) | `docs/plans/active/govee_frame_engine_process_spec.md` | **Implemented/software-tested 2026-07-08.** Spec authored 2026-07-08 (Fable orchestration session); Tasks 1-6 landed one commit each (Task 7 dead-config cleanup skipped — precondition (a) fails: `tests/test_led_dispatch_coordinator.py` constructs `LEDRateLimits(rt_reconcile_window_s=..., rt_reconcile_interval_s=...)`, a reader outside the dataclass/config-parsing). Phase-2 platform work after AWR-145: the realtime frame trio moved into a small child process the bridge spawns and supervises, so bridge GIL contention (628 ms RBMEM scans, two-deck load) can no longer starve LED animation (integration test measured 60.9 fps in the child this machine vs 28-29 decaying to 16.5 in-bridge, 2026-07-08). As built against post-AWR-145 code: socketpair(AF_UNIX, SOCK_STREAM) JSON-lines IPC (kernel EOF on any death = crash/orphan detection), 50 Hz beat-anchor stream (cross-process `time.monotonic()` comparability confirmed on this machine), intent-replay respawn supervision (emergency replays as emergency_stop + unconditional brightness-0), explicit macOS scheduling-band control (setpriority clear-bg + NSActivity latency-critical + thread QoS, all three calls confirmed working 2026-07-08; which lever defeats the [unknown] faceless-process demotion is answered by measurement, not assumption), achieved-fps self-report in status, fail-dark EOF teardown mirroring today's shutdown semantics (blackout+deactivate, never brightness-0 outside the emergency path so cloud looks survive). AWR-145 guarantees move wholesale inside the unchanged runner. Bridge keeps the decision layer, cloud dispatch, owner state machine, and pad mutual exclusion untouched; coordinator surface unchanged (drop-in client). Also folded in a Task 6 operator-review gap (independent of the child move): a pure operator blackout while the runner is INACTIVE never sent the LAN brightness-0 backstop (`_emergency_teardown` is guarded by `if self._active or handoff:`); the policy `LED_BLACKOUT` handler now calls the coordinator's new `blackout_brightness()` after the blackout is accepted, so darkness no longer depends on the runner's teardown branch (tactical pre-drop blackout still never dims). New modules `govee_frame_engine.py` (+`FrameEngineHost`) and `govee_frame_engine_client.py` (+`GoveeFrameEngineClient`) added to the `led_govee` contract; `on_thread_start` hook is the only runner change. Part D tests green: `tests/test_govee_frame_engine.py` (host+client 1-14), `tests/test_govee_frame_engine_integration.py` (real subprocess 15-16, 60.9 fps), plus tests 17-19 in `test_led_dispatch_coordinator.py`/`test_led_state_manager.py`/`test_govee_realtime_runner.py`; the three hard doc checks pass; full suite green except the five pre-existing environmental reds in untouched subsystems (live-config LED test, export-pack parity fixtures, SS golden `test_ddj_slots_8_16_17_24`, laser-color-map `enabled:false`, SS parity-oracle fixture drift). Known gaps: live fps under real load and on real hardware is unproven until the operator's next mix; child-death leaves the strip on its last frame for ~1 s until respawn (strictly better than today's silent in-process thread death). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-145 | Govee LED Phase 1: blackout hammer + razer keepalive + dispatch retry + pad mutual exclusion | `docs/plans/active/govee_led_phase1_blackout_keepalive_spec.md` | **Implemented/software-tested 2026-07-08** via the operator-sanctioned delegate lane (Codex down until Jul 11); spec authored 2026-07-08 (Fable audit session), Tasks 0-5 one commit each. Root causes verified at `d2ed39c` and hardware-confirmed by the operator the same day: razer-mode knockout by cloud DIY scenes with edge-only re-activate (stuck cloud look / invisible blackout), LAN brightness-0 proven to darken the strip in any mode (blackout backstop), role_key latch-before-outcome swallowing rejected looks ("drops not cycling"), idle freewheel dead on dwell-reject, and the LED Pad streaming through a bridge mix as a second writer (ghost look/flicker incident). Tasks: unconditional 2 s razer keepalive + assert-on-takeover/blackout replacing WI-6 reconcile; emergency-blackout brightness-0 with double-send restore on clear; bounded same-decision dispatch retry (no director re-tick); pad auto-stop when bridge-owned; `led_govee` contract gains `tools/led_pad_playback.py`. As built: `GoveeRealtimeRunner` re-asserts razer `activate()` on demand + unconditionally every 2 s while streaming (`status()` now `razer_assert_count`, WI-6 `note_cloud_dispatch`/`RBSS_LED_RT_RECONCILE` removed; the now-unused `rt_reconcile_window_s/interval_s` config fields stay inert in `led_models.py`); pure-emergency teardown hard-dims via `set_brightness(0)` before blackout (cloud handoff untouched, never dims), operator-blackout clear restores brightness 100; coordinator-rejected automation/idle dispatches retry the SAME decision every 0.35 s (max 8) without re-ticking the director, both retry slots cleared at every deck switch/track load/blackout/manual override/gate transition; pad `_poll_once` auto-stops a playing pad when a fresh bridge status is `bridge_owned` (`pad_owned`/`free`/stale keep playing). Part D tests green (`tests/test_govee_realtime_runner.py`, `tests/test_led_dispatch_coordinator.py`, `tests/test_led_state_manager.py`, `tests/test_led_pad_playback.py`); the three hard doc checks pass; full suite green except pre-existing environmental reds in untouched subsystems (live-config LED test, export-pack parity fixtures, SS golden `test_ddj_slots_8_16_17_24`, laser-color-map `enabled:false`, SS parity-oracle fixture drift). Adversarial review vs Part C invariants + named risks (director cursors, brightness-0 in cloud handoff, push-loop socket I/O) found no defects. Operator restart + live taste pass is the remaining gate. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-148 | RBMEM scan vectorization (kill the multi-second freezes from deck-2 playhead scans) | `docs/plans/active/rbmem_scan_vectorization_spec.md` | **Implemented/software-tested 2026-07-08** via the operator-sanctioned delegate lane (Codex down until Jul 11); spec authored 2026-07-08 (executive session), Tasks 1-3 one commit each (`18cd930`, `2f43a4b`, `4a827f7`). Root cause verified at `d2ed39c` and measured on-machine: the deck-2 playhead scan loops in `rb_memory.py` (`_scan_objc_zone`, `_scan_objc_heap_moving`, `_scan_static_elapsed_candidates`) compared int32s one at a time in pure Python holding the GIL — ~54 ms per 512 KB zone pass, ~427 ms per 4 MB heap chunk up to a 128 MB cap = multi-second whole-bridge freezes recurring every 5 s while deck-2 is unresolved during play (live fingerprint: 1-7 fps LED dips + event-late bursts at cue/load moments, session 2026-07-08 10:00). As built: the shared numeric pre-filters `_i32_moving_candidates` / `_i32_static_candidates` have a numpy fast path (int64-cast subtract so int32 wraparound matches the big-int loop; mask chain + `np.nonzero`; measured 28-40× — 1.9 ms / 10.6 ms) and a pure fallback that yields the GIL every `_SLICE_INTS=16384` ints; numpy is a lazy OPTIONAL import (`_numpy()`, never a hard dependency) and any numpy error falls back to the pure loop for that call. The three scans call the helpers for the int32 pre-filter and keep every per-candidate pointer/`_is_objc`/secondary check, dedupe/sort/cap, retry cadence, and log line unchanged; the heap scan also yields the GIL between chunks. Candidate VALUES and ORDER are byte-identical to the old loop — proven by an old-loop oracle equivalence test incl. the int32-overflow edge and a wrap-trap case (numpy == fallback == oracle). Part D: `tests/test_rb_memory_scans.py` (10 tests) green; full `discover` at the known baseline (the 6 pre-existing environmental reds are all in soundswitch/laser-color/LED-Patch-D/export-pack, none in a reader). Part E: `rekordbox_readers` contract `docs_update` satisfied; the three hard doc checks pass. Adversarial diff review vs Part C found no defects (candidate/order/log/retry semantics unchanged; `min(len)` buffer guard is strictly safer than the old zone `len(chunk0)` indexing; no new I/O or threads; push loop untouched). Remaining gate is operator-only: menubar restart + live taste pass (worst deck-work pause should drop from seconds to a few ms). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-147 | Spectral calibration expansion (v4 corpus refresh + re-runnable calibration report) | `docs/plans/active/spectral_calibration_expansion_phase1_tasks.md` (delegate tasks), `docs/research/spectral_calibration_expansion_2026_07_08.md` (measured findings) | **Implemented/software-tested 2026-07-08** via the operator-sanctioned Opus tmux delegate (`claude5`; Codex down until Jul 11), adversarially verified by the Fable manager (independent re-run reproduced the by-genre numbers identically). Landed: whole-library sweep refresh (731 on-disk tracks, +10 extracted, 711 with v4 entries, same 19 `no_grid` + 1 known undecodable flac), permanent `tools/spectral_calibration_report.py` (F2 §3/§4/§5.1 rule pack from the design pins + §6.5 calibration metrics, per-drop records exported for spot-verification), 27 table-driven tests, findings doc, `spectral_analysis` contract extension. **Result: every calibration claim holds at 2.5× BY GENRE scale (545 tracks / ~3.3k drops vs the 219-track prior basis); frozen tier cuts land within 0.0005 of their original percentiles; all six named F2 anchors reproduce exactly (CSN 128 bass-forward pattern byte-identical). NO calibration constant changed — deliberate: every drift is inside noise and none has a listening gate yet.** Watch items: roll threshold 3.0 now exactly at corpus p90; grit even/odd Spearman 0.9016 vs 0.902 gate floor; F2 floor-returned-abort window edge semantics need pinning in the F2 spec (two honest readings give abort 116 vs 150 — named anchors reproduce under both). **Listening gate ANSWERED 7/7 same day (findings doc §6b):** family classifier + NEUTRAL safety net + bass-forward + COMET BPM gate ear-validated; two darkness-pack calls REJECTED (Hide and Seek capped-16 "warrants ~1 beat"; Wanna Go Dancin' 5-beat "wouldn't black out — balloon build") — then **round 2 (10 more labels, findings §6c) SUPERSEDED the level-term theory: blackout length is drop EMPHASIS, not gap mirroring** — quantized {1,2,4,8,16} beats (standing operator rule), long blacks only for monster WALL-grade drops or true stops (percussion done, vocals-only), groove contexts default 1-2 beats regardless of measured quiet, gap caps the length; onset-density AND monotonic-level rules both disproven at n=11. Marker hygiene is a first-class F2 input (3 early-track/fake-drop overshoots; consecutive chorus markers in one drop section = ONE drop, matching smart-drop main-vs-continuation; operator corrected 2 tracks' phrasing mid-review). NEUTRAL safety net validated twice; `lowmid_pulse` scrub gate stays open. Creative anchor recorded: Can I = "glitter with a groove" (sparkly/dancy, not strobey). Lane CLOSED — F2 spec inherits the quantized emphasis-blackout model + 11-label calibration set + dip-window/abort-edge pins + track-start damping. Hard checks green; suite green minus the known pre-existing environmental reds. SOFTWARE/ANALYSIS ONLY / HARDWARE-UNVALIDATED. |
| AWR-144 | LED solo pre-dark race fix (RC4) | `docs/plans/active/led_solo_predark_race_fix_spec.md` | Implemented/software-tested in the current worktree (2026-07-07). Fixes RC4: on a laser-solo drop the LEDs flashed a bright drop look ~0.6s before the solo blackout because the LED drop anchor (chorus phrase-start) fires before the smart-drop marker where drop-presentation sets the `drop_spotlight` solo blackout owner. `led_dispatch_policy.py` now imports `LASERS_ONLY`, adds `_led_upcoming_drop_is_lasers_only`, and in `_dispatch_led_automation` suppresses the drop look via `_gate_led_automation("solo_predark_hold", ...)` when the drop role resolves and `_drop_presentation_last_pending[0] == LASERS_ONLY`, holding the current look until the marker's `drop_spotlight` blackout owner takes over. LED-side only: solo length and the blackout are unchanged and the lasers still fire the solo; every non-solo drop (`leds_only`/`leds_plus_lasers`/plan-unavailable/disabled) dispatches its drop look byte-identically. Four focused tests in `tests/test_led_state_manager.py` (`LEDSoloPredarkHoldTests`) cover suppression + `solo_predark_hold` gate, non-solo pass-through, disabled no-op, and the pre-existing post-marker `emergency_blackout` mask. No live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-139 | Presentation is per-section, decided by the TRUE drop (up-runway) | `docs/plans/active/drop_presentation_true_drop_section_spec.md` | Implemented/software-tested in the current worktree. Only a drop with a non-empty runway (or a manual-arm / hot-cue override) opens or re-enters a presentation window; runway-less markers keep firing laser look bursts but cannot re-roll the section's fixture split. Raw impacts still feed planning/session/learned bookkeeping. Refines AWR-138's re-entry to true drops only. No live process, bridge restart, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-138 | Drop windows re-enter on new impacts; backstop above real sections | `docs/plans/active/laser_drop_window_impact_reentry_spec.md` | Implemented/software-tested in the current worktree. Operator scenario: 128-beat drop sections exist, and an already-open window ignored later true-drop impacts. New true-drop impacts now re-enter the window with their own planned presentation and re-stamp the cap from their own impact beat; `drop_window_cap_beats` is 192 as a pure hang guard. A `lasers_only` solo that re-enters mid-window skips the LED pre-dark countdown and fires at impact. Amends AWR-135. HARDWARE-UNVALIDATED. |
| AWR-135 | Laser dark for the WHOLE LED-only drop section | `docs/plans/active/laser_drop_section_gate_spec.md` | Implemented/software-tested in the current worktree; amended by AWR-138. `drop_window_cap_beats` 32→96 is superseded by 192 as a pure hang guard, so the existing role-leaves-drop/post_drop branch remains the normal release and later true-drop impacts can assert their own presentation mid-window. `base_suppressed` is reported as intended-dark native Autoloop status instead of `unsupported_layout`. Tracks without chorus/post-drop markers still release after the existing short post-drop hold. HARDWARE-UNVALIDATED until an operator watches an LED-only drop live. |
| AWR-136 | Govee health reporting fixes (silent mirror sends, stuck degraded_reason) | `docs/plans/active/govee_health_reporting_fix_spec.md` | Implemented/software-tested in the current worktree: mirror target send failures/recoveries now log once per transition and expose `mirror_send_ok`; a successful send clears stale `degraded_reason="circuit_open"`. Reporting-only: light output, send ordering, queueing, rate limits, blackout bypass, and the push loop are unchanged. No bridge restart, live Govee output, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-131 | Smart-drop marker collapse + true-drop impact gate | `docs/plans/active/smart_drop_marker_collapse_spec.md` | Implemented/software-tested in the current worktree: selected smart-drop markers collapse clustered raw ANLZ candidates to one drop-section entry, and laser/LED drop-impact gates fire on real smart-drop crossings. Annotated by AWR-140 on 2026-07-07: the capped two-hit chorus-label rule is restored while the marker collapse is retained, so one later chorus-to-chorus label boundary can re-fire drop and later labels stay `post_drop`. Focused suites passed. Full discovery was run and still has the known LED color Patch D and export-parity fixture failures, plus a pre-existing prior-theme SoundSwitch golden-frame failure from the staged laser-color worktree. No live process, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action was performed. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-113 | LED Pad + Template Lab (browser UI, banks/editor/lab, output-ownership takeover) | `docs/architecture/led_pad_template_lab_design.md` (authoritative design), `docs/plans/active/led_pad_template_lab_spec.md` (Codex implementation spec, Phases 0–3), `docs/guides/led_pad.md` (operator guide) | Phases 0–3 plus the 2026-07-03 same-day passes (menubar/LaunchAgent, QR access, vendored Alpine + draft/discard, iOS touch, hygiene, stage-console reskin + action hierarchy, editor unset-param defaults) are implemented/software-tested. Audit P2 keeps committed drop-look selection on the same DIY eligibility filter as normal tick automation. Audit P3 keeps realtime-to-cloud handoff transport calls on the Govee runner thread. Full feature ledger: the spec's phase table. All visual/hardware outcomes need operator sign-off. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-116 | 2026-07-03 audit fix queue | `docs/plans/active/audit_2026_07_03_fix_queue_spec.md` | Ordered Codex implementation queue P1-P4. P1-P4 are implemented/software-tested in current `main`: runtime-command truth cleanup, default-off OS2L injector/status truth, Govee handoff/ANLZ/timing/spectral fixes, and laser resilience plus Laser Pad live-toggle truth. Software-only; no bridge restart, live process, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action is authorized by the spec alone. |
| AWR-117 | LED dispatch extraction from `state_manager.py` (audit P5) | `docs/plans/active/led_dispatch_extraction_spec.md` | Implemented/software-tested Codex refactor P1-P2: the five copy-pasted LED trigger/accept/reject bookkeeping paths now route through one helper set, and LED dispatch policy lives in `led_dispatch_policy.py` as a mixin with all `_led_*` fields staying on the `StateManager` instance. Pure refactor — `led_status_provider` payloads, counters, gate reasons, role keys, and log lines are intended to stay byte-identical, covered by the unchanged 2762-test suite. No bridge restart, live process, SoundSwitch, Rekordbox, laser, LED/Govee, MIDI, DMX, Enttec, or hardware action is authorized by the spec alone. |
| AWR-118 | 2026-07-03 audit follow-ups F1/F2 + LED-slice audit (Sonnet session) | `docs/plans/active/audit_2026_07_03_followups_spec.md` | Operator-sanctioned Sonnet implementation lane (Codex quota exhausted). Task A (probe_live_bpm/live_bpm dependency inversion) and Task B (parity oracle relocation + energy_model doc fix) are code changes; Task C is a strictly read-only LED-slice audit report. See spec for status. |
| AWR-152 | LED color/look quality pass (white leakage + hue smoothness) | `docs/research/led_color_quality_audit_2026_07_08.md` (audit + veto-pending knob proposals), `docs/prompts/led_color_quality_pass_manager_kickstart_2026_07_08.md` (lane kickstart) | **Audit delivered 2026-07-08 (read-only; nothing implemented).** Operator taste verdicts are design inputs: too much white in every cue (buildups excepted — white by design), hues not smooth, RT skeletons look bad at runtime. Audit findings, verified at `c866c8c`: v2 engine is live-enabled; slot 5 is hardcoded pure white in both engines (`led_identity_v2.py:198`, `led_color_engine.py:805-847`); exempt legacy freestyle/white RT looks in the default bank carry baked cyan/white (`govee_frame_renderer.py:388, 505-529, 592-603`); cloud scene content is engine-unreachable (bank membership is the only lever); v2 DIY filtering runs on a session-frozen v1 palette while white-tagged looks always pass (`led_dispatch_policy.py:1666-1668`, `led_color_engine.py:537`) — white-favoring skew; additive slot overlap washes to near-white in split-family zones (computed sat 0.23 in VOLT); both operator-visible white knobs are dead (v1 `palette.white` all 0.0 + `ZoneRampConfig.white` parsed-never-consumed, `led_config.py:1391`). Hue churn root cause: slot cues map color to brightness (`intensity * 4.0` → slot_coord; one comet sweeps 173-310° in VOLT), plus per-cycle random re-rolls (`step_within_section.groove`). Deliverables in the audit doc: 10-row veto knob table, per-skeleton keep/change/cut verdicts, realtime-twins verdict (feasible/cheap via `substitute_realtime_drop`; sequence after white/hue knobs). **VETO ROUND CLOSED 2026-07-08 evening:** #1 VETOED — the three freestyle nebulas STAY in the default banks (do not re-propose); #2/#3/#6/#7/#8/#10 APPROVED → Codex implementation spec authored at `docs/plans/active/led_white_knobs_round1_spec.md` (**IMPLEMENTED 2026-07-08 — Codex was rate-limited; a Claude delegate-lane exception executed the spec instead, see ROUND 1 note below**; #6 approved shape = remove the stale v2 DIY tag filter so every banked cloud scene rotates evenly, NO zone-tag rewiring; #3+#10 = per-zone `slot5_white` RGB replaces the dead zone `white` key, v1 `palette.white` + `_blend_white` deleted; live-config mirror stays operator-gated); #4/#5/#9 + strobe subdivision are VISUAL-TEST-GATED — 13 Template Lab drafts registered on the pad (:8766, gitignored `config/led_lab/`) for the operator's next session: comet color-mapping A/B, hue hold-vs-re-roll A/B, head-width slider, strobe cadence sliders, and the 7 colorway strobes (blue, cyan, green, red, red+white, blue+cyan, cyan+white) — all preview-checked, none exceed the house 16th-note strobe ceiling; realtime-twins deferred until after this round lands. **Knob #4 DECIDED (operator chat verdict, mid-pad-session 2026-07-08): per-spawn single color wins for round 2 — "a multicolor cycling comet effect, which is cool"; the intensity-mapped sweep REJECTED — "rainbow should be RGB etc not a random mashup"; lab statuses flipped (comet_single_color accepted, comet_rainbow_current rejected). The hue A/B was rebuilt same-session after a demo flaw (both sides now share palette + start color; jump demo-compressed to 8 beats, production stays 32).** The white-strobe lag complaint split out as AWR-153 (code defect confirmed). **F2 DESIGN INPUT recorded from this lane (operator drop-revamp direction, 2026-07-08 pad session): drops must FILL THE ROOM on the hit; the deeper half — drop animation/texture SELECTED BY the track's sound profile/energy (spectral-profile-driven drop texture selection) — is F2 routing territory and must be designed there, not in cue code. SUPERSEDED IN SHAPE same session by the operator's cue-notes FIREWORK redesign (`drop_room_fill` rejected): phase 1 = off-white background surges dark→full-bright with rapid small sparkles on top (`drop_firework_explosion`), phase 2 = background dims to black while colored embers survive and decay (`post_drop_firework_remnants`). ROUND-2 BANK DESIGN INPUT (operator verbatim: "Current sparkling cues can play the role of the sparkling remnants, such as the rt_drop_chase looks"): the existing rt_drop_chase-class sparkle cues are REMNANT/post-drop material — recast them into the post_drop role, with the AWR-149 drop→post_drop pairing carrying the explosion→remnants arc. PROMOTION DESIGN INPUT from `groove_heartbeat_chase` (accepted with notes): four head color modes — solid both / gradient across the pair / color combo per head / gradient within each head — the mode param is the new part at promotion, colors come from the engine palette; its reported stutter was root-caused to un-normalized sub-pixel head falloff (peak dipped to 0.53× between pixels, measured) and fixed with peak-normalized weights — the same normalization production `_comet_frame` uses. New ordered-rainbow draft `comet_rainbow_ordered` (hue from position + time, never brightness) built per the operator's re-tap; the knob #4 mashup verdict stands.** **POST-RESTART CUE-NOTES BATCH (2026-07-08 night): (a) HUE VERDICT REVERSED — the operator PREFERS the periodic color jump; `groove_hue_jump` accepted, `groove_hue_hold` rejected, knob #5 is DEAD (`step_within_section.groove` stays `true`; production cadence stays 32 beats — the 8-beat demo compression is not a request). (b) `comet_rainbow_ordered` ACCEPTED with a pair design, built as `rainbow_drop` (1 beat/cycle, width 6) + `rainbow_post_drop` (8 beats/cycle, width 2), widths on sliders per his no-hardcoded-width note. (c) `drop_firework_explosion` REJECTED → round 2 built (`drop_firework_explosion_2`): flicker root-caused to the whole sparkle field re-rolling synchronously every bucket (~17 Hz hard pops, no per-sparkle envelope); rebuilt on a time-based ember field (independent per-sparkle lifecycles, sine envelopes, seconds-domain — extends the Hz-strobe ruling: SPARKLES ARE TIME-BASED, NEVER BEAT-TIED, a standing design rule for round 2/promotion) with palette-tied spark colors (engine slots at promotion). (d) `post_drop_firework_remnants` ACCEPTED with timing fixes applied: embers hold ~8 beats then 2-beat decay (done by ~beat 10); background dim default 8 beats — F2 DESIGN INPUT recorded: background-dim length TIED TO MUSICAL CONTENT (musical-content-driven dim routing), 8-beat default when ambiguous.** **ROUND 1 (#2/#3/#6/#7/#8/#10) IMPLEMENTED / SOFTWARE-TESTED 2026-07-08:** all 7 tasks from `docs/plans/active/led_white_knobs_round1_spec.md` landed on `main` — `ZoneRampConfig.slot5_white` (per-zone RGB tint) replaces the dead `white` knobs on `Palette` and `ZoneRampConfig`; `derive_dressing` uses the tint verbatim (no sat-floor/hue-shift); palate-reset now dims all 6 slots (was 5 + hardcoded pure white); `_blend_white` and its 7 call sites deleted; `groove_diy_bright_white_chase` moved from the groove bank to the buildup bank in the example config; every example-config zone carries an audit-taste `slot5_white` default; `_led_diy_eligible_predicate` returns `None` under the v2 latch (cloud looks rotate evenly, no more stale/white-favoring tag filter); `breakdown_star_twinkle` no longer draws slot 5 (matches `_slot_twinkle`'s 0-4 range). Unknown-key tolerance from Part A [confirmed]: the config loader has no allowlist rejection for palette/zone dicts, so legacy `white` keys in an un-mirrored live config load harmlessly (verified via `_validate_color_engine`/`_validate_identity_v2_block` — no `unknown key` style checks touch those dicts). Full suite: 3489 tests, 4 pre-existing unrelated reds (soundswitch pack-parity subsystem) + 1 newly-surfaced pre-existing red (`test_led_color_engine_m2_patch_d.test_drop_slot_color_smoke_and_snap` — was already broken against the v2 bloom/flip-fade fade-field semantics on the operator's local live config; previously masked by an unrelated `_blend_white` AttributeError that this round's Task 4 fixed, now visible; out of scope for this round, not caused by it). LIVE config untouched (read-only per spec); mirror + menubar restart remain operator-gated. This repo's auto-sync hook fired mid-implementation and committed most of the 7 tasks under generic "auto-sync" messages instead of the intended one-commit-per-task history — net code is correct and task-scoped, only the commit log is fragmented. Analysis + spec + lab drafts (#4/#5/#9 + strobe) still no bridge code changed for that remainder. SOFTWARE-TESTED / HARDWARE-UNVALIDATED. |
| AWR-153 | Full-strip white strobe hold/stutter (`rt_drop_white_aggressive`) | `docs/research/led_white_strobe_gate_diagnosis_2026_07_08.md` (mechanism diagnosis) | **DIAGNOSED 2026-07-08 (read-only; code defect confirmed).** Operator symptom: "turns full white, kinda holds for a bit, strobes kinda, very inconsistent." Mechanism, verified at `1b51c66`: the gate is a stateless 29 ms ON window (`(beat % 0.25) < 0.0625`, `govee_frame_renderer.py:511`, constants designed for 40 fps) sampled by a runner that holds the last frame through any stall with no catch-up (`govee_realtime_runner.py:251-265`) — under the AWR-151 throttle (28-33 fps + jitter) the offline sampling model reproduces his exact words: up to ~33% of strobe cycles show NO flash (runs of 5 = 0.6 s dead), and a stall on an ON frame holds full white ~165 ms (5.6× the designed flash). **Key finding: the ProcessType fix alone is NOT sufficient — at a jittery 60 fps the model still misses ~31% of cycles and holds white ~147 ms.** Robust fix direction (named, not specced): runner injects real frame timing into effect params (the `slot_colors` runtime-injection pattern) and the gate widens its ON window to ≥ ~1.5 frames so every cycle lands a flash; a stall cap keys off frame_index. **BINDING DESIGN INPUT (operator ruling 2026-07-08, mid-pad-session): the fixed gate is TIME-BASED (real Hz + duty), NOT beat/BPM-subdivided — identical feel at any BPM; lab-verified same session (Hz-clock lab strobes measure equal flashes/sec at 128 and 90 BPM).** The operator-requested 7-colorway full-strip strobe family (blue, cyan, green, red, red+white, blue+cyan, cyan+white; taste drafts live in Template Lab under AWR-152) rides this fix at promotion so the family is born robust. Post-fix live fps remains [unknown] (no fps fields in the 2026-07-08 15:32 log yet). NEXT: operator visual session on the cadence/colorway drafts → executive release → Codex spec for the gate fix + family under the `led_govee` contract. Diagnosis only; no code changed. SOFTWARE/ANALYSIS ONLY / HARDWARE-UNVALIDATED. |
| AWR-154 | LED pad blackout unlatch fix (`led_clear_blackout` reason-blind end-to-end) | — | **URGENT PREEMPTION, IMPLEMENTED / SOFTWARE-TESTED 2026-07-08 (live during the operator's mix).** Defect: LEDs latched dark, unremovable. Verified chain at HEAD before the fix: pad takeover sends `cmd led_blackout reason led_pad` (`tools/led_pad_playback.py:173`) so owner `led_pad` lands in `_led_blackout_owners` (`led_dispatch_policy.py:595`); pad `release()` sent `led_clear_blackout` with NO reason (`led_pad_playback.py:186-188`); the clear chain was reason-blind end-to-end (`runtime_status.py` zero-arg callback → `__main__.py` event with empty payload → `led_dispatch_policy.py` discarding only `legacy`) — the pad's own claim could never be discarded. Minimal three-task fix, one commit each: T1 `release()` now sends `reason=led_pad`; `parse_command()` accepts an optional non-empty `reason` on `led_clear_blackout`; `CommandReader` parses it and passes it to the callback; `__main__.py`'s `_led_clear_blackout(reason=None)` puts it in the `BridgeEvent` payload only when present — absent reason still resolves to an empty payload everywhere, so a bare clear is byte-identical to before. `led_dispatch_policy.py`'s discard line (`ev.payload.get("reason") or "legacy"`) was already correct and is untouched; no clear-ALL-owners fail-open behavior was implemented (that remains proposal-only elsewhere). T2 adds end-to-end test coverage: pad release removes exactly the `led_pad` owner while a bystander `legacy` owner stays blacked out; a bare clear still removes only `legacy`; existing blackout event tests stay green. T3 is this docs round under the `runtime_commands` and `led_govee` contracts. Full suite: 3495 tests (3489 + 6 new from T2), same known pre-existing reds as AWR-152 (4 unrelated soundswitch pack-parity + 1 pre-existing-unmasked LED test), nothing new. **RESTART IS THE ONLY UNLATCH for the currently-running process** — this fix changes code that only takes effect on the next bridge start; it does not reach into the live process. NO bridge restart, NO live-config edit, nothing touching the strip was performed. SOFTWARE-TESTED / HARDWARE-UNVALIDATED. **Superseded same evening by AWR-155 below: "a bare clear is byte-identical to before" and "no clear-ALL-owners behavior was implemented" were both true only through this round.** |
| AWR-155 | LED bare-clear fail-open (executive-approved, supersedes AWR-154's bare-clear scope) | — | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-08 (normal pace; bridge live post-AWR-154-restart).** Executive-approved shape: a bare `led_clear_blackout` (no reason) is operator authority and now clears EVERY owner in `led_dispatch_policy.py`'s `_led_blackout_owners` set at once — not just `legacy` as AWR-154 left it. A reasoned clear is unchanged: it still discards exactly the named owner, so machine surfaces (LED Pad, drop presentation, smart-drop) keep AWR-154's scoped-clear pattern. T1: in the `LED_CLEAR_BLACKOUT` handler (~`led_dispatch_policy.py:610`), when `ev.payload` carries no reason, the handler snapshots the owner set, clears it, and emits exactly one INFO outcome log (`[RGB] blackout-clear-all owners=...`) naming what was cleared — not a per-tick log. When a reason IS present the discard behavior is byte-identical to before this change. The `LED_BLACKOUT` add path is untouched. T2 replaces the AWR-154 test that encoded the now-superseded "bare clear only touches legacy" behavior with `test_bare_clear_blackout_clears_all_owners_and_logs_once` (three owners — `led_pad`/`drop_spotlight`/`legacy` — cleared together, log fires exactly once, all three names present in the line) and adds `test_restore_brightness_fires_when_clear_all_empties_owners`; the AWR-154 reasoned-clear-stays-scoped test is unchanged. T3 is this docs round under BOTH the `runtime_commands` and `led_govee` contracts. **Accepted consequence, not guarded against:** a bare operator clear during a lasers-only solo window also clears the `drop_spotlight` owner and lights the LEDs mid-solo — that is the intended operator override; the presentation window's own later release discard becomes a safe no-op against the already-empty set. Full suite: 3496 tests, same known pre-existing reds as AWR-152/154 (4 unrelated soundswitch pack-parity + 1 pre-existing-unmasked LED test), nothing new. **Effect begins at the NEXT restart after tonight's** — the bridge had already been restarted once this evening for AWR-154 and was live throughout this round. NO bridge restart, NO live-config edit, nothing touching the strip was performed. SOFTWARE-TESTED / HARDWARE-UNVALIDATED. |
| AWR-156 | LED round 2: strobe-gate rebuild + accepted-look promotion | `docs/plans/active/led_round2_promotion_spec.md` (Codex-format implementation spec) | **SPEC AUTHORED 2026-07-08 late night — AWAITING EXECUTIVE REVIEW; implementation gated on that review, nothing implemented.** Locked scope over the AWR-152 verdict trail: (a) AWR-153 strobe-gate rebuild — Hz-based, frame-timing-aware (`frame_period_s` runtime-injected by the runner, ON window ≥ ~1.6 frames), `drop_white_aggressive` rebuilt at the accepted hz 6.0/duty 0.3; (b) the 7 colorway strobes promoted on the fixed gate at the operator's dialed rates/duties (red_white side B restored to white — FLAGGED for his one-line veto; blue_cyan/cyan_white keep his custom color dials); (c) promotions: `rt_buildup_balloon_comet` (his dialed 6→0.8 over 32 beats, dim to 0.05, white), `rt_groove_heartbeat` (peak-normalized heads — the measured 0.53× stutter fix — plus the four color_mode variants as slot-fed promotion machinery), `rt_post_drop_firework_remnants` (time-based ember field, accepted 8+2 timing, slot-5 background); (d) knob #4: the intensity-hue mashup dies at all 8 renderer sites — per-spawn single-color slots; (e) knob #9 role widths: drop/post_drop comets 4, groove 2.5 (FLAGGED veto-able); (f) bank recast: `rt_drop_chase`/`rt_drop_nebula` → post_drop bank, their drop_pairs removed (AWR-149 pairing will carry explosion→remnants once the explosion round lands); (g) knob #5 explicit NO-OP (reversal recorded — `step_within_section.groove` stays true). Standing promotion rules recorded in the spec: strobes Hz-based never beat-subdivided; sparkles time-based never beat-tied. Out of scope (verdicts pending): `drop_firework_explosion_2`, the rainbow pair, `comet_rainbow_ordered` promotion, remnants feel re-check. All operator-dialed values pinned from the live pad drafts.json this session; all file:line cites re-verified at post-AWR-155 HEAD. **EXECUTIVE REVIEW 2026-07-08 late night: APPROVED, RELEASED to implementation, no required changes** (gate math verified to degrade toward more-light at low fps — correct failure direction; un-mirrored-config invariant and scope exactness verified; both taste flags one-line-veto-able). ROUND-3 RESIDUAL logged, not this round: `_slot_drop_center_burst` even-pixels-only gappiness (`govee_frame_renderer.py:1438`, audit skeleton verdict) rides with the explosion/rainbow batch. **IMPLEMENTED / SOFTWARE-TESTED 2026-07-08 late night (Tasks 1-9 + the T6.4 rename revision, one commit per task, by the delegate-lane exception).** All locked scope landed as spec'd: the Hz-based frame-timing-aware strobe gate (`_hz_strobe_on`, `frame_period_s` runner-injected EMA) plus the 7 colorway strobes; the 3 promotions (`buildup_balloon_comet`, `rt_groove_heartbeat`, `rt_post_drop_firework_remnants`) sharing the new peak-normalized `_head_weights` helper; knob #4's per-spawn single-slot mapping at all 8 sites; knob #9 role-scoped widths (3 of the 7 target cues — `rt_groove_chase`/`rt_groove_nebula`/`rt_post_drop_center_comet` — needed a small renderer wiring fix since they'd hardcoded their head width and never read `params` at all; wired with the same fallback so an un-mirrored config is unchanged, flagged as a deviation from the spec's config-only framing of that task); the bank recast, AMENDED mid-round by the operator from a plain move to a rename (`rt_drop_chase`/`rt_drop_nebula` → `rt_post_drop_remnant_chase`/`rt_post_drop_remnant_nebula`, scene_ref unchanged, propagated everywhere the look name appeared including the regression-lock test suites); knob #5's explicit no-op verified untouched; **knob #3 refined — tint scope narrowed to nebula whites by operator taste 2026-07-08 late** (Task 9 addendum: `BAKED_WHITE_SLOT5_EFFECTS`, firework bursts bake pure white, nebula comets and the remnants background keep the zone tint). Part A's [assumed] `local_t` wrap behavior is CONFIRMED: it advances on wall-clock seconds through pauses/seeks per the beat-sync engine; the gate is stateless in `local_t` so a backward jump simply re-phases it. Full suite: 3543 tests, same 5 known pre-existing reds (4 unrelated soundswitch pack-parity + 1 pre-existing LED live-config test, confirmed present at the pre-round baseline commit `b0bbcb3`), nothing new; the three hard doc checks and both contracts' test commands pass. Auto-sync fragmented several tasks' commits mid-flight (content verified byte-identical to each task's intended diff before treating any as landed; history was never rewritten). Two taste items are one-line operator vetoes: `strobe_red_white` side B (restored to white; his last pad dial was red) and the groove-chase/nebula width default (2.5; his `comet_width` dial never reported a final number). LIVE config untouched; the operator's mirror + menubar restart is the remaining gate for every look-level change — the two exceptions that need no config are the strobe-gate rebuild and the knob-#4 mapping change, both already live in code. **Manager adversarial review PASSED same night (independent suite: 3543 tests, exactly the six known environmental reds, zero new; gate/rename/addendum/banks/widths/flags all verified at the code and example config; the knob-#9 renderer-wiring deviation verified behavior-preserving via matching fallbacks).** ROUND-3 SET (accumulating): center_burst gappiness; `rainbow_drop` + `rainbow_post_drop` promotion (both accepted — rainbow_drop's beat-locked travel fix awaits his strip confirm); `drop_firework_explosion_2` (rejection RECLASSIFIED as a render regression — diagnosed white-on-white, fixed in the lab with bg_hold resolve-down + blend-replace embers, back to iterating); coupled-look preview pad feature (lab half live: `pair_firework` with true-zone palettes + `pair_rainbow`; pad-server half awaits the executive-gated spec); migrate the remaining beat-tied `int(beat*16)%2` strobe gates onto `_hz_strobe_on` — the five legacy sites (`govee_frame_renderer.py:456, :493, :514, :525, :660` at current HEAD) plus the same-idiom slot-cue siblings (`:1304, :1332, :1365` area) — **FEEL-CHANGING, several live inside the operator-protected freestyle nebulas: each migration ships as a pad A/B for his verdict, never silent**; the buildup strobes (`_buildup_zone_strobe`/`_buildup_half_strobe`) are ALREADY time-based sine gates with Hz ramps (4→12→20 through the build), correct by construction, excluded (executive-verified 2026-07-08 night). SOFTWARE-TESTED / HARDWARE-UNVALIDATED. |
| AWR-157 | Darkness fix round: blank-role hold + reader freshness (overnight R1) | `docs/plans/active/led_darkness_fix_round_spec.md` (implementation spec), `docs/research/deck2_reader_diagnosis_2026_07_08.md` (grounding diagnosis) | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-08 overnight (Tasks 1-7, delegate-lane exception).** All locked scope landed: (a) blank-role hold — new `blank_role_hold` config knob (top-level, default `true`) intercepts the automation dispatch path right before `_led_send_decision`: when the decision's look equals the configured blackout look while the deck is audibly playing and a prior automation decision was already accepted this session, the blackout dispatch is suppressed and the room holds its current look; knob `false` is byte-identical to pre-round behavior; emergency/manual/tactical blackout paths are structurally unreachable from this guard (it only runs inside `_dispatch_led_automation`, which those paths return before reaching), test-pinned. (b) Q-A instrumentation: edge-triggered `[RGB] blank-role-hold` INFO/DEBUG log with a best-effort source classification (`phrase_none`/`scripted_map`/`adapter_reject`/`other`) — Part A's exact blank-role origin stays an open question the diagnosis flagged; this instrumentation is meant to resolve it from a live session's logs, not a proof it already has the answer. (c) Part A's [assumed] external playing-hint plumbing verified TRUE, not newly built: `RBMemoryReader._deck_playing_hint` was already wired from `StateManager.get_deck_playing` (`__main__.py:1622`) and already computed once per tick as the existing `deck2_playing` local (used for the deck-2 ObjC scan trigger); this round threads that same value into `_read_decks_chain_first`/`read_live_pos_chain` via new keyword params instead of re-deriving or adding a second hint. Deck-2 chain health now requires freshness: raw frozen for `_CHAIN_FRESH_TICKS=5` consecutive reads while the hint says playing marks `chain_ok=False` (miss, not silent-healthy); frozen-while-paused is exempt (the FEIN case); deck 1 is provably untouched (`chain_ok` stays exactly `chain_snap is not None`). `skip2` inherited the freshness gate automatically since it already read `_chain_ok_last[2]` — `RBSS_POS_CHAIN_SKIP_OBJC=1`'s meaning refines from "never scan while a chain exists" to "never scan while the chain is actually working," the watcher env line itself untouched. (d) Q-B pause-vs-freeze DEBUG log (deck-2 playing→not-playing flip within 10s of the deck starting to play — "track load" isn't tracked anywhere in this module, so "started playing" is the closest available proxy, named as a deliberate substitution). Files: `led_dispatch_policy.py`, `led_models.py`, `led_config.py`, `led_look_director.py`, `rb_memory.py`. Tests: `tests/test_led_state_manager.py` (`BlankRoleHoldTests`, 8), `tests/test_led_config.py` (`BlankRoleHoldConfigTests`, 5), `tests/test_rb_memory_chain.py` (`ChainFreshnessTests`, 7). Full suite: 3563 tests, the known six environmental reds (5 failures + 1 error: laser color / soundswitch export-parity / one pre-existing `led_color_engine` M2 patch-D test), zero new regressions from the pre-round 3543-test baseline. Auto-sync fragmented several tasks' commits mid-flight into generic "auto-sync" messages (content verified byte-identical to each task's intended diff before treating any as landed; history was never rewritten). LIVE config untouched; the tracked example config gains `blank_role_hold: true`. The bridge stayed DOWN the entire round — no bridge start, no live-config edit, no restart, nothing touching the strip was performed. Contracts: led_govee + rekordbox_readers + config_schema, all three fully updated. SOFTWARE-TESTED / HARDWARE-UNVALIDATED — the operator's next menubar start + mix is the remaining validation gate. |
| AWR-159 | Laser Solo fix round: cancel wire + honest manual arms + feedback truth (overnight R2) | `docs/plans/active/laser_solo_fix_round_spec.md` (implementation spec), `docs/research/laser_solo_triage_2026_07_08.md` (grounding triage) | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 overnight (Tasks 1-7, delegate-lane exception).** All 4 triage-named defects landed, one commit per task. Task 1: the already-tested `manual_interaction` fail-open is wired — a pad press while the feedback state is `active` sets a one-tick request that `_drop_presentation_tick` consumes into `WindowInputs.manual_interaction=True` the very next tick (regardless of that tick's `impact_now`), fully additive to the existing automatic releases; the pad is now a clean four-state (arm/disarm/veto-pending/cancel-active). Task 2: a manual arm at impact with `plan.decision_for(beat) is None` now resolves `solo_manual` instead of falling to `leds_plus_lasers`/`plan_unavailable` (auto tiers keep the plan requirement byte-identical) — required broadening `presentation_impact` to fire on `armed` alone and moving the one-shot arm-clear outside the `decision is not None` gate. Task 3: a MANUAL arm's darkness guard drops the `laser_director.is_enabled()` term (base_live/unmasked/role only); when even that fails at impact it refuses visibly instead of silently downgrading — arm cleared, one INFO log naming the failing gate (`base_live_false`/`masked`/`role`), pad flashes a distinct `refused` state, LEDs/suppression never touched (refuses before darkening, or restores atomically if pre-dark had engaged). Task 4: the `predark → clear` reset now logs why (`passed_without_drop` vs. the specific `laser_visible` gate: `director_disabled`/`base_live_false`/`masked`/`role`) — closes the triage's open unknown for every future miss. Task 5: the arm-key staleness defect (tonight's new evidence — "blinked the entire mix after one arm") is fixed at the root: track-load on the armed deck, active-deck change, and stop all explicitly clear the stale arm with one log line each (`track_changed`/`deck_changed`/`stopped`), and the feedback recompute now runs every tick (not just at impact) so it can never latch; the auto-tier "solo pending" feedback literal is renamed `armed`→`pending` (distinct from a manual press; `led_palette_control` already expected it, state_manager just never emitted it) — five states total: `off`/`armed`/`pending`/`active`/`refused`. Task 6: `tests/test_drop_presentation.py` (+16 tests: `ManualArmVisibilityTests`, `PredarkResetReasonTests`, retargeted the pre-existing guard-downgrade test off `solo_manual` onto `solo_hotcue` since manual no longer downgrades) and `tests/test_state_manager_drop_presentation.py` (+9 tests: cancel isolated from role-exit, manual-arm-without-plan-match, manual-visibility-without-Director, manual-refusal-with-log, arm-cleared-on-track/deck-change, arm-cleared-on-stop, pending-vs-armed-feedback; fixed one pre-existing test's press-while-active assumption to the new cancel semantics) plus 2 in `tests/test_led_palette_control.py` for the new `pending`/`refused` UI-state mapping. Files: `drop_presentation.py`, `state_manager.py`, `led_palette_control.py`. Contract `drop_presentation` fully updated (`drop_presentation_authority.md` limitations #1 narrowed/#2 fixed with behavior description, `laser.md`, `led_govee.md`, `streamdeck_palette_control_design_spec.md`; contract's own test list gained `tests.test_state_manager_drop_presentation`, previously a gap). Three hard checks pass. Full suite: 3551 tests, 13 failures + 4 errors, all pre-existing/unrelated and independently attributable — 11 in unrelated modules already tracked elsewhere this round (LED color-engine strobe-gate family, SS pack parity/export, SS laser player golden test slot=16, led_pad_controls/lab/service import errors), 2 are the AWR-158-tracked flaky pair (`test_fire_enqueues_to_a_background_writer_not_the_tick_path`, `test_arm_fire_learn_disarm_round_trip_via_real_event_dispatch`) — traced this round to a specific, reproducible cause worth folding into AWR-158: a stale `local/state/laser_solo_learned.json` entry (`content-1:64`, predating this session) makes `LearnedStore.record()` a no-op for any test that fires a real solo at beat 64 without redirecting the store, which is deterministic given that file's contents, not machine-load flakiness — confirmed via a clean-environment re-run with the same code. Zero new regressions from any of the 7 tasks (each verified against the untouched pre-task baseline before committing). Auto-sync fragmented several tasks' commits mid-flight into generic "auto-sync" messages and once merged unrelated parallel-lane files into a task commit (Task 2 picked up one incidental line each of `config/led_look_director.example.json`, `govee_frame_renderer.py`, `tests/test_govee_frame_renderer.py` from a concurrent lane) — content verified byte-identical to each task's intended diff before treating any as landed; history was never rewritten. The bridge stayed DOWN the entire round — no bridge start, no live-config edit, no restart. Operator summary in plain words: the Solo pad now always does something you can see — it fires, or it tells you why not; pressing it during a live solo brings the lights back; arming then changing tracks un-arms visibly instead of blinking forever. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED — the operator's next live pass is the remaining gate. |
| AWR-160 | Phantom track-load gate (overnight R1.5, sequenced ahead of the laser rounds) | `docs/plans/active/phantom_load_gate_spec.md` (implementation spec), `docs/research/phantom_load_palette_triage_2026_07_08.md` (claude5 triage, log-measured) | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 overnight (Tasks 1-4, delegate-lane exception).** Root cause (triage-confirmed): the reader emitted 168 phantom track loads in one mix — library browsing leaks through the load path while the position path correctly reads the deck empty; every phantom repainted palettes ("palettes don't remember" = reader bug; v2 identity store EXONERATED, zero drift over 122 keys; AWR-152 not implicated). Fix landed in `rb_state_reader.py:_tick_deck`: a 3-consecutive-tick STABILITY gate (`_LOAD_STABLE_TICKS`) on load emission (deliberately NOT a playing/position requirement — a load-never-play track like the FEIN case still emits; stability suppresses browse churn while preserving every stable real load), ANLZ-before-TRACK_LOADED ordering preserved (a late-resolving ANLZ path can still catch up on its own once a title is confirmed), phantom-suppression instrumentation (`[RBSR] phantom-load-suppressed` throttled DEBUG, `[RBSR] phantom-storm` edge-triggered 60s INFO) fingerprints the leaking read for the true source fix (the triage's named unknown, still open). `tests/test_rb_state_reader.py` covers the gate, FEIN case, browse storm, A-then-B, unload, and deck-1/2 symmetry; `tests/test_rb_memory_chain.py` (AWR-157) re-verified untouched. Contract: rekordbox_readers. Operator's next mix = the live-verification gate (does the phantom flicker actually stop). |
| AWR-161 | LED round 3: Hz-gate migrations + rainbow/firework promotions + center-burst fix (overnight, parallel Track C) | `docs/plans/active/led_round3_spec.md` (implementation spec) | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 (Track C).** All 6 tasks landed, one commit per task (explicit paths; auto-sync fragmented some into its own sweep commits — verified via `git log`, not a failure). Task 1: all ten remaining beat-tied `int(beat*16)%2`/`int(cue_beat*16)%2` strobe gates (5 frame-effect fns + 5 slot-cue fns, 18 effect names total) migrated onto the AWR-156 `_hz_strobe_on` (hz 6.0/duty 0.3 defaults; buildup strobes and `_hz_strobe_on` itself untouched); `hz`/`duty` added to all 18 names' `REALTIME_EFFECT_PARAM_KEYS` (C5 guard). Task 2: `rainbow_ordered` promoted (ordered hue-by-position, beat-locked `travel_per_beat` when present else legacy `loop_beats` pace); `rt_rainbow_drop`/`rt_rainbow_post_drop` added to the example config, paired via `drop_pairs`. Task 3: `drop_firework_explosion` promoted — **contrast gate PASSED**, measured post-surge ember deviation 101/255 against the >=60/255 bar (the lab's own prototype measured 164/255 under a different sampling; both clear the bar); `rt_drop_firework_explosion` added, paired via `drop_pairs` to the existing `rt_post_drop_firework_remnants` (the AWR-149 explosion->remnants arc, now real). Task 4: `_slot_drop_center_burst`'s even-pixels-only gate (`if idx % 2 != 0: continue`) removed; every pixel in the pulse now renders, main/accent slot-band split unchanged. Task 5: comprehensive test coverage added (`tests/test_govee_frame_renderer.py` — BPM-independence sweep across all 18 migrated names, hz/duty dialable checks, C5-guard allowlist test, rainbow beat-lock/legacy-pace/hue tests, the firework contrast-gate test; `tests/test_led_color_engine_m2_patch_d.py` — center-burst all-pixel-coverage fixture replacing the old even-pixels-only fixture). Task 6 (this row + `docs/subsystems/led_govee.md`, `docs/subsystems/config.md`, `docs/setup/configuration.md`, `docs/status/feature_status_matrix.md`, `docs/status/support_matrix.md`, `docs/status/validation_matrix.md`, `docs/validation/hardware_validation_log.md`, `docs/validation/software_test_inventory.md`). Files: `govee_frame_renderer.py`, `config/led_look_director.example.json`. Parallel-lane files (`rb_state_reader.py`/`rb_memory.py`, `state_manager.py`/`drop_presentation.py`) were never touched. Suite: the pre-existing 6-red baseline in the `led_color_engine`/slot-effect family (`test_strobe_off_frames_are_dark`, `test_drop_chase_strobe_off_frames_are_dark`, `test_drop_nebula_strobe_gate_can_go_dark`, `test_post_drop_nebula_strobe_gate_can_go_dark`, `test_strobe_gate_can_go_dark`, `test_drop_slot_color_smoke_and_snap` ERROR) is unchanged and unrelated to this round's code — verified against the untouched pre-session HEAD before starting. Full-suite totals fluctuate turn to turn because three parallel Track A/B/C sessions commit into this shared tree concurrently; `govee_frame_renderer.py`/`config/led_look_director.example.json` and their own test files run clean against that fixed 6-red baseline at every checkpoint in this round. Contracts satisfied: `led_govee` + `config_schema`, three hard checks pass. The live, gitignored `config/led_look_director.json` was read-only; migrated gates run at code defaults on an un-mirrored config (the operator's intended overridden change), the two new looks need the mirror to become selectable. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED — hardware sign-off on the Hz feel, rainbow pair, firework explosion, and center-burst fix is the remaining gate. **AWR-161-FIX (2026-07-09 micro-round, fix-forward applied):** three items, one commit each by explicit path. (1) `led_pad_controls.py` registered `rainbow_ordered` + `drop_firework_explosion` in `RENDER_GROUPS` (Drop) and added their 8 novel params (`cycle_beats`/`rainbow_span`/`travel_per_beat`/`surge_beats`/`bg_level`/`bg_hold`/`spark_a`/`spark_b`) to `CONTROL_META`, un-breaking the module-level completeness assert (:194) that would have crashed the pad server at its next restart (`controls_for` now resolves every allowlisted key). (2) The five legacy strobe-off tests (`test_strobe_off_frames_are_dark`, `test_drop_chase_strobe_off_frames_are_dark`, `test_drop_nebula_strobe_gate_can_go_dark`, `test_post_drop_nebula_strobe_gate_can_go_dark`, `test_strobe_gate_can_go_dark`) were repinned from the retired beat-parity gate to the Hz contract — each sweeps `local_t` across one full 1/6 s period at hz 6.0/duty 0.3 and asserts BOTH lit and fully-dark frames occur (mutation-verified: a stuck-on gate fails them; no deletions/weakening) — so five of this row's original six reds are now GREEN. (3) `post_drop_center_comet_blue_cyan` registered in `REALTIME_STROBE_EFFECTS` (pre-existing gap: it gates on `_hz_strobe_on` but was unflagged) + a membership test; example/live looks already carry `allow_strobe=true` so config validation stays green. Files: `led_pad_controls.py`, `tests/test_led_color_engine_m2_patch_{c,d,e1,e2}.py`, `govee_frame_renderer.py`, `tests/test_govee_frame_renderer.py`. Post-fix full suite: 6 failed / 3608 passed / 6 skipped / 1 xfailed / 4664 subtests passed — the remaining six reds are all unrelated pre-existing (`test_drop_slot_color_smoke_and_snap` patch-d live-config ERROR, 2 export-pack parity self-heal, SS golden slot=16, laser-color-map, parity-oracle); three hard doc checks pass. No live config edited, no pad/bridge restart. |
| AWR-162 | Laser Energy Ladder (A-D approved; implements WITH the F2 round) | `docs/plans/active/laser_energy_ladder_spec.md` (implementation spec), `docs/research/laser_ch8_ch9_operator_map_2026_07_08.md` (chase-class ground truth) | **SPEC AUTHORED 2026-07-09 overnight from the executive relay of the operator's verdicts** (retiring-seat transcript is the only source — no prior repo doc; the relay + the operator map are the citable record). (A) APPROVED — energy-gated activation: small drops LEDs-only (lasers silent), standard+ bring lasers; REPLACES the `ceil(laser_ratio*N)` personality rule for tiered tracks (tier-less tracks keep the legacy ratio, byte-identical); tier source = F2's per-drop energy tier from the 41-desk-verdict calibration. (B) APPROVED — density rides the tier: per-tier chase divisions in the menus (red+white 100→116→140 seeded; other pairs single-value until the operator supplies divisions; colors stay the zone's). (C) APPROVED PENDING LIVE LOOK — impact burn-down ships FLAGGED OFF (`impact_burndown.enabled=false`): CH9 starts at tier max at the hit and eases down; existing settle finishes; haze session tunes or kills. (D) APPROVED + OPERATOR ADDITION — lasers dark for exactly the v2 emphasis-blackout beats (room breathes together) AND for the 4 beats before every chorus phrase start. Pre-drop tease: NO VERDICT — explicitly out of scope (executive parked as a morning item). Contracts: drop_presentation + laser_color + config_schema. **EXECUTIVE REVIEW 2026-07-09: APPROVED WITH TWO REQUIRED SPEC-TEXT CHANGES, both applied same-session — (1) the once-per-drop invariant restated to match AWR-138 impact re-entry (decision once PER DROP at its impact; no re-decision without a NEW impact; re-entry re-decides on the new drop's OWN tier — a small-tier next drop lands LEDS_ONLY on re-entry; Task 5 pins both sides and forbids the literal never-re-decides test); (2) the D.2 pre-chorus seam renamed honestly (quantized 4-beat lookahead against phrase_roles boundaries, interface owned/delivered by F2 — the LED pre-dark is an impact-beat countdown, nothing at HEAD looks ahead to phrase starts). RELEASED to ride the F2 round.** **IMPLEMENTED 2026-07-09 inside the F2 round (Task 4): (A) energy gate DELIVERED+tested — `lighting_moments_v2.laser_tier(family,tier)` (NEUTRAL/thin→small, T1/T2/T3→standard/intense/monster) feeds `drop_presentation.plan_track(laser_tiers=…)`; small→LEDS_ONLY (lasers silent), else→LEDS_PLUS_LASERS, replacing the personality-ranked `laser_ratio` for tiered tracks; tier-less→legacy ratio byte-identical; finale/hotcue/learned/manual precedence untouched; once-per-drop + AWR-138 both-sides re-entry hold by construction (each drop's own tier sets its presentation). (D.1) one-darkness DELIVERED via the shared transition window (F2's per-drop length drives LED+laser masking together). (C) impact burn-down config-seeded (`f2.impact_burndown`, ships `enabled:false`) — operator-live-tuned at the haze session per this spec's own text. **MANAGER REVIEW CORRECTION 2026-07-09: (B) per-tier chase divisions did NOT land — no 100→116→140 seed exists in `config/laser_color_map.json` (single-value `chase:100` entries only) and no tier-aware consumer exists in code; the lane's "config-seeded" claim covered only (C). (B) is DEFERRED alongside (D.2) 4-beat pre-chorus laser blackout (which needs a phrase-role lookahead mask). **EXECUTIVE RULING at the F2 gate 2026-07-09: (B)+(D.2) = ONE named follow-up riding a SMALL DEDICATED LASER ROUND after F4 (not folded into F4 — texture containment stays pure); LED manager specs it, executive reviews as usual.** NEUTRAL→small stays the flagged one-line veto (exec condition 5). **(B) + (D.2) NOW IMPLEMENTED 2026-07-09 as AWR-170 (the named follow-up laser round, dispatched after F4 landed + gated): (B) per-tier chase divisions live in `laser_color_engine` + `config/laser_color_map.json` (crimson + v2:EMBERCORE seeded 100→116→140, tier plumbed one hop from the f2_plan); (D.2) 4-beat pre-chorus laser blackout live as the new `pre_chorus` mask owner (window off the RAW uncollapsed chorus markers, `f2.pre_chorus_laser_beats` absent=0/example=4). See the AWR-170 row for the full delivery + suite numbers.** Bridge DOWN; laser output hardware-unvalidated. |
| AWR-163 | LIGHTING ENGINE v2 F2: moments, darkness ladder, drop typing (overnight critical path) | `docs/plans/active/lighting_engine_v2_f2_spec.md` (implementation spec; normative annexes: `docs/architecture/LIGHTING_ENGINE_V2_DESIGN.md` §§3-5/7-9 + `docs/research/spectral_calibration_expansion_2026_07_08.md` C§6b-6f) | **SPEC AUTHORED 2026-07-09 overnight — AWAITING EXECUTIVE REVIEW.** The binding model applies the 41-verdict desk-calibration SUPERSESSIONS over the locked design: family classifier D§3.1 verbatim (desk-validated); tier scorer redesigned family-aware with track-start runway damping (the six documented failure cases are the fixture — D§3.2 alone is on notice); **the quantized emphasis-blackout ladder REPLACES D§4.1's gap-mirror sizing** (0/1/2/4/8/16 or balloon-shrink; 4 = default emphasis; 8 = intense trap/true stops; 16 = collapse + hard/dark monster ONLY; balloon/black split pinned at build perc_full 0.35 with the six labeled builds as fixtures; measured gap CAPS; abort edge pinned entry-inclusive; perc-cut flick verbatim); marker hygiene load-bearing (AWR-131 collapse reuse + runway damping; grid-health flag recorded not implemented); white-share/rungs/bass-forward/simmer/moves verbatim from D§5/§9 + BALLOON as the fourth move; cloud-drop gap resolved by AWR-150 RT-first impact; tonight's inputs folded (drop_look_routing family×tier table, remnants dim routing 8-default, post-drop width decay); **AWR-162 implements inside this round (Task 4)** with the tier mapping NEUTRAL/damped→small, T1/T2/T3→standard/intense/monster; F2 kill switch ships example-ON/absent-OFF so the un-mirrored live config stays byte-identical. Contracts: led_govee + laser + drop_presentation + config_schema + spectral_analysis (consumer-only). **EXECUTIVE REVIEW 2026-07-09: APPROVED WITH TWO REQUIRED SPEC-TEXT CHANGES, both applied same-session — (1) the config-default bullet's drafting self-talk replaced with the final rule only (example-ON / absent-OFF / mirror+restart activates); (2) the NEUTRAL→small and damped-T1-thin→small laser mappings FLAGGED as one-line-veto operator taste items (NEUTRAL is a typing outcome he never ruled on, ~27% of corpus drops; default kept — fails toward fewer unwanted laser fires) in the spec, this row, and the operator summary. RELEASED. Pre-authorized dispatch (no further round-trip): claude4 may implement F2+AWR-162 once (a) these edits are committed, (b) AWR-159 + AWR-161 sentinels have landed, (c) the quiescent-tree full suite reproduces the known-six-reds baseline; model pinned explicitly at dispatch.** FLAGGED DEFAULTS (one-line veto): NEUTRAL-family drops → lasers silent; early-track damped-thin drops → lasers silent. **IMPLEMENTED 2026-07-09 (Opus orchestrator; Tasks 1-6, one-commit-per-task, several fragmented by concurrent auto-sync — verified in HEAD): T1 `lighting_moments_v2.py` pure engine (family classifier 0/4211 mismatch vs the report tool; C§6f quantized ladder; TIER-INDEPENDENT per exec condition 4 — 16 keys on family-grade WALL/COMET + collapse length, `darkness_ladder` takes no tier arg, a test asserts the signature); T2 plan on the async worker → `meta.f2_plan` per (deck,load_gen), F2-off computes nothing; T3 darkness drives the SHARED pre-drop transition window + drop_look_routing look bias; T4 AWR-162 (A) energy gate + (D.1); T5 `/f2` config example-ON/absent-OFF (live config untouched); T6 tests incl the MANDATORY F2-off byte-identity kill test + scripted stand-down. 37 F2 tests green; 744 tests across every touched-file suite green. CODE-vs-SPEC CORRECTION (verified): the general per-drop predark is the SHARED `transition_window`/`pre_drop_blackout_beats` consumed by BOTH LED and laser masking, NOT the laser-solo-only `led_predark_beats` the spec named — so F2's variable darkness darkens LED+laser together (= AWR-162 D.1 for free).** **EXEC CONDITION 1 — family-percentile tier redesign is DEAD, do not resurrect (verbatim evidence, measured on `/tmp/awr147/report.json`, 4,211 drops): recomputing per-family violence p55/p85 satisfied 0 of 5 acceptance fixtures — Satisfaction (Hardwell&Maddix) six COMET drops stay T1 (viol 0.46-0.59, all < COMET p55=0.60); ONE CHANCE @2:36 stays T1 (NEUTRAL, viol ~0.47-0.52); Age Of Love @1:30 stays T3 (0.698 = WALL p85) — AND it BREAKS two validated holds: I COULD BE THE ONE @1:18 HOUSE T3→T2 (0.709 < HOUSE p85=0.728) and DROP EM's spread first drop T2→T1 (0.619 < WALL p55=0.6245). Root cause: the operator's tier = perceived hardness, which the violence formula reads LOW on hard-techno/big-room/older masters; percentile-within-family can't fix a loudness/era problem (those drops sit low within their own family too); track-start damping only pushes tiers DOWN so it can't lift the under-reads. Ships on the CURRENT corpus-absolute tier; redesign hypothesis of record = era/loudness normalization (C§6d). EXEC CONDITION 2: the six failure cases are PINNED known-under-read in `tests/test_lighting_moments_v2.py::TestKnownTierFailures` so any future tier change shows as a diff.** **EXEC CONDITION 3 (operator one-notch-low knock-on): with F2 on after your mirror the hardest old-school-techno / big-room drops may read one aggression notch low until the tier redesign — lasers STILL fire (standard tier), chases one class chunkier, blackout SIZING UNAFFECTED. EXEC CONDITION 5: NEUTRAL→small (lasers silent, ~27% of drops) stays the flagged one-line veto.** F2 work adds ZERO reds — **manager review 2026-07-09: independent full suite 3688 tests / EXACTLY the known-six-red baseline by name (parent-dir form). Red-accounting corrections vs the lane's self-report: the "govee `slot_colors_from` KeyError" IS the baseline `test_drop_slot_color_smoke_and_snap` (its error is literally `KeyError: 'slot_colors_from'` at `tests/test_led_color_engine_m2_patch_d.py:259`); the `replay_event_source` reds were mid-flight AWR-122 work fixed by `0ae7f3a`; the two transient `test_soundswitch_pack` byte-identity reds seen at the executive desk are a test-infra race, not a product defect — the pack manifest embeds `git rev-parse HEAD` at export time (`tools/export_soundswitch_pack.py:175`, `soundswitch_pack.py:597`), so any commit landing between the class-level reference export and a test-time export fails both byte-compare tests exactly once (3/3 green in isolation; proposed hygiene fix: pin `_generator_commit` via mock in `CurrentProjectPackTests`).** **EXECUTIVE GATE 2026-07-09: PASS (executive spot-verified the ladder signature, both kill-test classes, and the rev-parse embed; commit-race mechanism accepted — matches the desk red-run evidence). F4 dispatched on the standing pre-authorization; balloon gray-zone knob added to the operator morning veto list.** Bridge stayed DOWN; rendered look/laser output hardware-unvalidated. |
| AWR-164 | LIGHTING ENGINE v2 F4: texture layer (STRICT F2-first dependency) | `docs/plans/active/lighting_engine_v2_f4_spec.md` (implementation spec; annexes: design D§5/§7 + calibration semantics) | **SPEC AUTHORED 2026-07-09 overnight — AWAITING EXECUTIVE REVIEW; dispatches only after AWR-163 lands + gates (F2-first build order).** S-2 containment is the constitution: texture classes (all existing `spectral_profile.py` pure functions — consumer-only, zero analysis changes) season WHICH variant/params of an already-selected look play, never scheduling/darkness/family/tier/routing; the containment byte-identity tests ARE the acceptance. Calibration semantics locked: `sustained_synth` = clean-euphoric proxy (counts vocals, never surfaced as "synth"); `lowmid_pulse` ships COMPUTED-NOT-CONSUMED behind an experimental flag (breadth + slow-wub false negative recorded); kick-prominence/thick-wall limitations consumed as weak signals only; bass-forward B/K grain density live-gated. Consumption: drop-variant seasoning through the runtime param injection, euphoric bright-weighting through the EXISTING `look_preference` seam, simmer upgrade of the F2 sparse-dim floor trigger, `bottom_gone` texture-darkness only. Kill: `v2.f4` example-ON/absent-OFF; F4-off ⇒ family defaults; F2-off ⇒ F4 no-ops. Contracts: led_govee + config_schema + spectral_analysis. **EXECUTIVE REVIEW 2026-07-09: APPROVED WITH ONE REQUIRED SPEC-TEXT CHANGE, applied same-session — the Task 3 seam cite was stale-line/wrong-range (AWR-157's insertions drifted it); re-cited at HEAD to both ends of the real seam: producer `_led_look_preference_predicate` (`led_dispatch_policy.py:1762`, passed :1196/:1743) + consumer chain in `led_look_director.py` (:311/:326/:355-356, context :196), with a re-verify-at-implementation-HEAD note. RELEASED under the F2-first gate; same pre-authorized dispatch structure as AWR-163 once F2 has landed and gated clean; model pinned explicitly at dispatch.** **IMPLEMENTED 2026-07-09 (Opus orchestrator; Tasks 1-6; verified in HEAD): T1 `lighting_moments_v2.build_track_plan` also records per drop the majority-vote texture vector (stab/sustained-bass/growl/kick-prominence/thick/bright-tilt + onset density for the WALL trap-vs-dense split) and per track the euphoric (sustained-synth >=8) + simmer (attack_low<2.5 & onset<0.5, >=8-run) beat ranges + the EXPERIMENTAL lowmid_pulse busy-pulse duty — pure, fail-soft, on the async worker; all fields default-empty/unread unless F4 on. T2 `led_dispatch_policy._led_inject_f4_seasoning` merges texture-selected variant PARAMS into the already-selected drop look's `decision.params` (family+texture key → `f4.variant_seasoning` cell; HOUSE growl-bar bass-forward mask scales sparkle density). T3 euphoric bright-weighting through the EXISTING `_led_look_preference_predicate` (AND-narrow, director-fail-open) + simmer param-seasoning of quiet role cues. T4 `/f4` config block (example-ON/absent-OFF, fail-closed, legacy-tolerant). T5 22 containment/semantics tests. T6 this row + led_govee/config docs + D§7. **CONTAINMENT PROVEN: F4 seasoning changes ONLY `decision.params` (LEDLookDecision equality with params compare=False shows look/scene_ref/backend/routing/family/tier byte-identical); F4-off/F2-off/scripted → no-op; predicate F4-off byte-identical to prior composition (proven).** **HONEST DELIVERED-WITH-CAVEATS (named, NOT folded into a clean label): (a) seasoning VALUES are TUNE-LIVE config defaults — cloud DIY looks IGNORE the param merge, only realtime looks render it, so the felt result is an operator live-tune gate; (b) the within-drop per-beat bass-forward ALTERNATION is a SCALAR sparkle-density stand-in (renderer can't time-vary params inside one dispatch) — the mask drives the scalar (CSN BKBB→0.5), NOT literal per-beat windows; (c) the simmer "upgrade" seasons the quiet role cue's PARAMS during a measured simmer — F2 never wired a section-rung floor (zero runtime consumers of `section_rate_rung`), so there is NO rung-2→rung-4 machinery to flip; the sparse-dim treatment is whatever params the selected floor look honors, NOT a new floor renderer; (d) `euphoric_bright_looks`/`simmer_seasoning` are config-driven, example seeds white-end drop looks + sparse-dim params; (e) busy-pulse ships COMPUTED-NOT-CONSUMED behind `busy_pulse_experimental:false` — renders nothing (C§6d).** DEFERRED / OUT OF SCOPE (untouched): AWR-162 (B) per-tier chase divisions + (D.2) pre-chorus laser blackout — named follow-up AWR-170. **Commit fragmentation (noted, never rewritten): T1 `37637d6` + T5 `83a121f` landed clean; T4/T2/T3 folded into auto-sync commits `a75bf18`/`dd68f81`/`8e3d28c` by the live auto-sync hook racing the index mid-commit — code verified intact in HEAD.** SUITE: 3716 tests parent-dir form, EXACTLY the known-six-red baseline by name (test_drop_slot_color_smoke_and_snap error; absent_fixtures / stale_venue_sha / ddj_slots_8_16_17_24 / loader_ships_calibrated_fixed_band / autoloop_capture failures) — ZERO new reds; the two `test_soundswitch_pack` byte-identity race-tests stayed green this run. Three hard checks green. Bridge DOWN throughout; rendered look output hardware-unvalidated.** |
| AWR-165 | Track identity move-invariance (no path as primary key) | `docs/plans/active/track_identity_move_invariance_design.md` | **DESIGN COMPLETE 2026-07-09 (operator directive verbatim: "the bridge should recognize the files even if i move them").** Chosen design: content fingerprint `fp:sha1(size + first/last MiB)` as the ONE primary key, computed once per track load in the resolver worker (never per-tick — tick-path placement verified against `state_manager.py`/`rb_state_reader.py`); spectral v3+v4 caches re-key to `fp + beatgrid_fp` (dropping realpath/mtime/size — moved files stop re-analyzing, replaced files still do); LED v2 IdentityStore + laser LearnedStore re-key from `content_id` to `fp` (content_id demoted to provenance); scripted SSID matching unchanged; F2 plans inherit invariance via the same identity worker (interaction named; F2 spec NOT edited). One-time remigration FLAGGED (716 v4 + 488 v3 cache entries + 301 identity records; corrections never deleted by migration). Premise correction recorded from live-store inspection: the identity store was ALREADY content_id-first (all 301 live entries; zero path-fallback keys) — the genuinely path-keyed store is the spectral cache, and the deeper fragility is content_id resolution itself (2 of 3 resolver paths are path/UUID-dependent; USB-playback content_id behavior `unknown`, live-verify at implementation). Move-scenario acceptance table in design §9 (same-disk move, rename, USB copy, replaced-content, mtime-only touch). Contract gap flagged as REQUIRED first implementation task: `anlz_reader.py` appears in ZERO contracts, `filepath_resolver.py` missing from `rekordbox_readers`. Operator taste calls open (§11): RB export-transcode setting, migration timing, corrections-follow-content ruling. Design only — no code changed, no migration run; implementation parked behind the executive gate with AWR-122. **IMPLEMENTATION SPEC AUTHORED 2026-07-09 evening (specbank lane): `docs/plans/active/awr165_move_invariance_spec.md` — Part A-E Codex spec with every design seam independently re-verified at `57d514f` (the F2/F4/laser/AWR-176 rounds had moved `state_manager.py`/`spectral_cache.py` cites; AWR-176's `growl_centroid_frames` folded into the migration's verbatim-payload rule); adds the `_cache_key_from_fp` split (migration derives keys from stored `beatgrid_fingerprint` — payloads hold no raw grid), required-positional `track_fp` params (missed callers fail loud, never silently always-miss), and the deploy-ordering rule (run migration at first post-upgrade restart; skipping costs one re-extraction per track, no data loss). Implementation target = the Codex window 2026-07-11 18:28+. Interim mitigation tonight = `tools/spectral_stick_sweep.py` (AWR-183, usb lane) path-keyed pre-warm; fp keys make that sweep unnecessary for byte-identical exports once landed. Spec only — still no code/config/store changes.** |
| AWR-166 | Audio spectral analysis upgrade audit (spectral lane, read-only) | `docs/research/spectral_upgrade_audit_2026_07_09.md` (decision brief), kickoff `docs/prompts/active/spectral_upgrade_manager_kickoff_2026_07_09.md` | **AUDIT DELIVERED 2026-07-09 overnight (Fable manager, claude7; operator ask verbatim: keep-as-is vs change, STEMS on the table, 5 proposals + 1 recommendation).** VERDICT: KEEP v4 as the backbone — change by addition, never replacement (41 AWR-147 ear verdicts + corpus proofs anchor to v4's stored measures; architecture already separates raw measures from re-tunable interpretation). Code-verified brief correction: runtime is not strictly cache-read-only — at-load extraction on cache miss for grids <=900s on the ANLZ thread (`state_manager.py:249-253,290`); true invariant = never per-tick. Five proposals priced: P1 frame-rate growl-band CENTROID series (the App-E named unlock; additive field + one overnight re-sweep, ~+19%/entry cache, no new deps, closes the operator-heard formant-wobble blindness); P2 slow-wub class (derived-only, You&Me 0/32 false negative); P3 sub4 slot-0 kick/untz signature (derived-only, App-F named); P4 STEMS per-stem envelopes (seriously evaluated + web-researched: HTDemucs via MLX port feasible on THIS M2/8GB, extrapolated ~3-6h/700 tracks, torch-CPU segmented fallback ~35-50h resumable, torch 2.13 ships cp314 arm64 wheels, MIT; only path to a vocal axis; ranked the right SECOND move — no consumer reads vocals yet + every stem class starts ear-unvalidated); P5 embedding replacement REJECTED (burns the calibration bedrock, opaque axes). **RECOMMENDATION: P1**, with P2/P3 flagged as near-zero-cost riders on any future round. Read-only lane — zero code/cache/config changes; implementation gates on executive + operator. Plain-English operator summary in the report §5, written for verbatim relay. |
| AWR-167 | XDJ standalone playback — Pro DJ Link reader feasibility | `docs/plans/active/xdj_link_reader_feasibility.md` | **FEASIBILITY DELIVERED 2026-07-09 (operator directive verbatim: "USB needs to have XDJ support… This may be an ambitious ask but it is important to know").** Web-researched (Deep Symmetry dysentery/beat-link/crate-digger, cross-checked, per-claim confidence labels) + repo event-model fit verified at current code. Honest verdict: **moderate ambition on XDJ-XZ / XDJ-1000-family / CDJ hardware (documented protocol, decade of show-control precedent via beat-link-trigger), IMPOSSIBLE in standalone mode on XDJ-RX/RX2/RX3 (no Pro DJ Link — Link Export only; confirmed by beat-link README + official AlphaTheta docs), high-effort + degraded on Opus Quad.** **PINS ANSWERED 2026-07-09 (executive relay, operator's words): model = XDJ-RX3, connection = USB cable → the P0 STOP gate FIRED (RX3 has no Pro DJ Link). SHELVED-WITH-TRIGGER: reopen if/when the operator plays on link-capable gear (club CDJs, XDJ-XZ, XDJ-1000-family); the feasibility doc stays the pickup artifact. Bridge-compatible RX3 usage that works TODAY, no build: rekordbox performance mode over the same USB cable — laptop rekordbox is the player, the existing memory reader covers it; the stick isn't the player in that mode.** Architecture (shelved pickup artifact): `LinkReader` sibling of `RBStateReader` (own threads, `put_nowait` events — push loop untouched by construction; ANLZ-before-TRACK_LOADED preserved by matching identity FIRST, then emitting the LOCAL library twin's paths so the whole downstream lighting brain runs unchanged); identity via precomputed library link-index {title, artist, duration, beatgrid_fp} → AWR-165 content fingerprint (Deep Symmetry's SignatureFinder independently validates content-shaped identity over rekordbox ids, which are `unknown`-stability across exports). Known losses in link mode: intra-beat position (beat-anchored interpolation; breaks under scratch/reverse/tight loops on everything except CDJ-3000), mixer authority (F3 blend has NO signal source — memory-read-mode-only), laptop rekordbox must be CLOSED while the link reader runs (port conflict, beat-link README). Phasing: P0 pins + packet capture (STOP if RX-family) → P1 passive beat spike → P2 identity + status + StateManager integration (core build) → P3 interpolation parity + hardware validation (the long pole). Paper only — no code, no network capture, no hardware contact; not implementation-authorizing. |
| AWR-168 | STEMS PILOT built + run (spectral lane; operator green-light, executive-dispatched) | `docs/plans/active/stems_pilot_spec.md` (spec), `docs/research/stems_pilot_run_2026_07_09.md` (run report), evidence base `docs/research/spectral_upgrade_audit_2026_07_09.md` (AWR-166 P4) | **BUILT + RAN 2026-07-09 (Opus orchestrator claude9, executive-dispatched after operator verbatim "disk freed. green light full stems"; run report `docs/research/stems_pilot_run_2026_07_09.md`, commit 6037a88+).** Result: **separation quality PASSED decisively** (33/33 tracks, 0 errors, reconstruction 0.04 dB median @ 100% within 1.5 dB, peak RSS 1.52 GB @ segment 7.8, ~2.25 min/track ≈ 27 h/library torch-CPU) — the technical risk the pilot existed to test. **Frozen gate returned FAIL** on 3 criteria, all measurement/label limits NOT separation: sidechain (only 2 anchors resolved, 1 ducked a clean 9.7 dB where v4 read 1/28, the other 1.5 — need ≥2 of ≥3); vocal (ghost needs operator-labeled instrumental windows — auto "loud=instrumental" is wrong in EDM where drops carry vocals; `pair_separates`=True though); named-element floor (5/9 PASS: rolls/screeches/808s/distorted-kicks clear with real per-stem advantage v4 can't make; wobble PASS is a FALSE POSITIVE — modulation concentration clears every dense track; claps buried in industrial walls; offbeat kick-confounded). **Spec CALL-OFF conditions all NOT met** (RAM/reconstruction fine, ghosting bounded, ≥4 elements advantaged) — the run landed in the gap the binary gate didn't cover. **Teardown done per spec: venv (1 GB) + htdemucs weights (80 MB) deleted, disk refunded; the 33 per-stem envelope JSONs + scorecard kept as the record, re-evaluable via `tools/stems_pilot.py --report` with NO re-install.** Recommendation (not implementation-authorizing): re-score with operator moment-labels against the cached stems, OR executive green-lights the sweep on the operational+reconstruction+cleared-elements proof; fine rhythmic/vocal proxies = F4-consumer design work (finer than coarse per-beat). Env: python3.11 venv outside repo, torch 2.13.0 / demucs 4.0.1 / librosa 0.11.0 / pyrekordbox 0.4.4. Contract `spectral_analysis` extended (code_globs + inspect); 16 metrics unit tests green; zero runtime change; v4 cache + 41 AWR-147 verdicts untouched by construction. Original spec context: operator green-lit stems minutes earlier (verbatim: "i green light stems to be installed, as long as you believe this will massively improve lighting choreography"); executive accepted under the honest framing: the PILOT proves separation quality on his own brickwalled masters first (30-50 tracks), full-library commitment only on pilot pass. Spec shape: pure metrics module + gated runner (`tools/stems_pilot_metrics.py` / `tools/stems_pilot.py`), htdemucs single model CPU-segmented for the 8GB box, venv OUTSIDE the repo (`~/.venvs/rbss-stems/` — auto-sync hazard), stems never hit disk (in-memory, envelopes-only JSON to the stems_pilot namespace), decode = the corpus-proven librosa/soundfile stack. Pilot corpus pins the operator anchors per named element (sidechained subs under walls / offbeat hats / industrial claps-snares / screeches / wobble bass / 808s / rolls / distorted kicks + the AWR-166 vocal axis). Falsifiable frozen gate (6 PROCEED criteria incl. reconstruction sanity, pump visibility, ghost margin, slow-wub readability; explicit CALL-OFF + teardown that refunds the disk). **EXECUTION GATES STAND: pip install/model download/any run only after operator disk cleanup (8.3GB free measured; 4GB hard floor tool-enforced) AND executive word.** Code tasks are implementable + fully testable with no install (lazy heavy imports; --dry-run works today). Consumer home on pass = F4 texture layer (AWR-164), specced separately. Contract: spectral_analysis (extend code_globs in-spec). Zero runtime change; v4 cache + all 41 AWR-147 verdicts untouched by construction. |
| AWR-170 | Laser round: per-tier chase divisions + pre-chorus blackout (AWR-162 B + D.2 follow-up) | `docs/plans/active/laser_tier_chase_prechorus_spec.md` (implementation spec; annex: `docs/plans/active/laser_energy_ladder_spec.md` B/D.2 text) | **SPEC AUTHORED 2026-07-09 overnight — AWAITING EXECUTIVE REVIEW; dispatches only after F4 (AWR-164) lands + gates (executive ruling at the F2 gate: one named follow-up on a small dedicated laser round, NOT folded into F4).** Seams verified at HEAD (read-only sweep + manager spot-verification): chase selection is tier-blind today (`laser_color_engine.py:281-305` picks by LED-color + is_drop; `_f2_laser_tiers` collapses to the binary LEDS_ONLY gate at `drop_presentation.py:315-316`, nothing reaches chase choice); laser dark windows are named mask owners (`laser_executor.py:346-382`, sole owner `"breakdown"`); chorus phrase starts are the RAW ANLZ drop markers (`smart_phrasing.py:523-548`) so the AWR-131 marker collapse removes drop DECISIONS but not phrase boundaries — (D.2) covers the collapsed mid-section choruses F2's window never darkens. Plan: per-tier chase dict form (`standard/intense/monster`, red+white menus `crimson`+`v2:EMBERCORE` seeded 100→116→140, all other menus byte-identical single-value, fail-closed parsing), one-hop tier plumb at the existing per-frame snapshot call (dict lookup only — push-loop safe), new `"pre_chorus"` mask owner off a new `beats_to_next_chorus_marker` series with the existing edge-flag pattern, `f2.pre_chorus_laser_beats` example=4 / ABSENT=0=off (mirror rule), every `breakdown` cleanup path also releases `pre_chorus` (AWR-154 latched-dark failure class named). **IMPLEMENTED 2026-07-09 (Opus orchestrator; Tasks 1-6, one-commit-per-task, Task 5 tests swept into the concurrent AWR-122 commit `122be01` by the auto-sync race — content verified intact in HEAD, never rewritten). (B) per-tier chase: `laser_color_engine._parse_menus` accepts `chase` as int (byte-identical `("chase",...)` tuple) OR a `{standard|intense|monster}` dict → `("chase_tiered", {tier:ch8}, names)`; missing keys fall back standard→first-present, all-junk fails closed to skipping the entry; `_target`/`_pick_menu_entry` resolve CH8 by tier (None/unknown/small→standard). `config/laser_color_map.json` seeds `crimson`+`v2:EMBERCORE` 100→116→140; all other menus single-value byte-identical. `state_manager._laser_color_drop_tier` plumbs the tier one hop (push-loop-safe `f2_plan.for_drop` lookup, folded into the re-sync signature) — Task 1/2 ordered PARSER-FIRST since the old loader silently skips dict-form chase (verified), so the seed never vanishes. (D.2) pre-chorus: new `pre_chorus` mask owner off a `smart_phrasing` window computed from the RAW (uncollapsed) chorus markers (`pre_chorus_beats` snapshot input, rising/falling edges), consumed by `smart_rearm._pre_chorus` (hold on arm, release on marker crossing OR any leaked exit — AWR-154 guard); `OutputState.pre_chorus_active` latch cleared alongside `breakdown_active`, mask itself cleared by `_release_all_masks` on every lifecycle reset. `F2Config.pre_chorus_laser_beats` absent=0 / example ships 4. Held-static ducks dark for the window + restores after (Part C chosen behavior, pinned test). Contracts: laser + laser_color + config_schema + led_govee (F2Config). SUITE: 3742 tests parent-dir form, EXACTLY the known-six-red baseline by name (test_drop_slot_color_smoke_and_snap error; absent_fixtures / stale_venue_sha / ddj_slots_8_16_17_24 / loader_ships_calibrated_fixed_band [red for the cwd/relative-path reason, unchanged by the crimson seed — fails at `assertTrue(loaded.enabled)` before any menu assertion; the blue_cyan menu assertion it checks was untouched] / autoloop_capture failures) — ZERO new reds; the two `test_soundswitch_pack` byte-identity race-tests stayed green this run. Three hard checks green. Bridge DOWN throughout (0 processes); laser output hardware-unvalidated.** |
| AWR-171 | SHIP-BLOCKER D3-F1: idle-no-audible leaks the drop_spotlight LED-blackout owner | `docs/plans/active/idle_no_audible_drop_window_release_spec.md` (fix spec; the QA repro is the acceptance) | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 (D3-F1 fix landed): one-line mirror of the `_do_stop` release into `_enter_idle_no_audible` (`state_manager.py`, `self._drop_presentation_release_on_stop()` appended after the laser resets, before the idle-ambient dispatch). QA repro flips exit 2→0 with owners cleared to `()`; 3 pinned tests added in `tests/test_state_manager_drop_presentation.py` (twin-path state equality vs `_do_stop`, no-window idempotency, `enabled:false` byte-identical); full drop-presentation module green; 3 hard checks pass. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED — the operator's live pass is the remaining gate.** History: **FIX ROUND AUTHORIZED 2026-07-09 ~05:4x (executive superman3; found by the QA showcase loop, triple-verified: QA + executive + LED manager each reproduced exit=2 at HEAD).** Root cause (code-verified): `_enter_idle_no_audible` (`state_manager.py:2086`) resets LED/laser/autoloop state but never calls `_drop_presentation_release_on_stop()` (which `_do_stop` calls at `:5118`), and `_push_tick_inner` early-returns when `active_deck` not in (1,2) — so a laser-solo `drop_spotlight` owner latched mid-solo-window LEAKS when the resolver lands on 0: room stuck dark up to the 192-beat cap on the live Director-enabled rig. Latent pre-2026-07-09; tonight's rounds raised exposure. RULED FIX SHAPE (executive, live reasoning on record — fail-open beats fail-dark; early lights-return on an idle blip is visible and recoverable, stuck-dark is the showcase failure): mirror `_do_stop` by calling the already-idempotent release helper (idempotent, policy-gated, pure/in-memory — verified at `:2865`) at the END of `_enter_idle_no_audible`; one shared release for every idle-no-audible entry. Acceptance = QA repro exits 0-family with owners cleared + pinned twin-path test (idle releases EXACTLY like `_do_stop`, asserted as state equality not hand-picked fields) + repo-root five-red baseline. |
| AWR-172 | Showcase QA review loop (operator mandate: intensive adversarial review of everything shipped overnight) | `docs/research/qa_showcase_review_2026_07_09.md` (findings report), kickoff `docs/prompts/active/qa_showcase_review_kickoff_2026_07_09.md` | **DELIVERED 2026-07-09 pre-dawn (Fable QA manager claude10; 5 Opus finder dimensions + 5 independent Opus refuter rounds + Sonnet subagents; every finding default-to-refuted).** Surface: AWR-157/159/160/161+micro/laser-config/AWR-163 F2/AWR-164 F4 at frozen pin `b2af2bf` vs pre-tonight `f4bfd06`, plus AWR-170 post-landing at `53a4513`. TALLY: 1 SHIP-BLOCKER (D3-F1 idle-no-audible `drop_spotlight` blackout-owner leak — escalated real-time via signal file, executive-verified, FIXED same hour as AWR-171, fix refuter-verified: repro exit 2→0) + 1 CONFIRMED MAJOR open (D1-F1: AWR-160 gate re-fires TRACK_LOADED on a transient unreadable `track_info` → full mid-song lighting reset with no preceding ANLZ_PATH; empirically rare — 543 logged loads ≈ 0 same-title reloads on the gate-less PRE reader; fix shape = None-vs-empty distinction at `rb_state_reader.py:405-409`; **OPERATOR DEFERRED the fix 2026-07-09 morning** — do not implement without his word) + 6 CONFIRMED MINOR (D2-F1 `abort_at` dead field ≤3 extra pre-drop dark beats; D2-F2 `markers_changed`-gated stale plan; D4-F1 govee tool provenance over-abort, fail-safe; D4-F2 unbounded anlz-worker spawn under a cache-miss load storm; D4-F3 export double HEAD-read, inert; D4-F4 three untrimmed per-load dicts, ~tens-of-KB per set) + 1 REFUTED (D2-F3 partial laser_tiers dict — unreachable: smart drops are a pure raw-drop subset) + the executive-directed flake RESOLVED (the 2 `test_state_manager_drop_presentation` reds = load-dependent TEST flake, 1-in-5 loaded full suites, always failing as a pair; test 1 is a 1.0s starvation deadline by construction; no runtime defect; the AWR-170 d189a79 timeline attribution NOT supported). Proven clean: kill-switch matrix identical-when-off cell-by-cell (F2 absent-OFF, F4 same-object containment, scripted stand-down, tier-less/cache-less fail-toward-today); F2 plan compute p50 1.7-2.9ms / max 10.4ms on the daemon worker, ≤5.4µs/tick push-loop consumption, 16s cache-miss extraction GIL-releasing; plan lifecycle leak-free over 400 loads; AWR-160 gate storm-proof (ANLZ→TRACK order held); 12,268-tick real-capture replay + 12-track synthetic set clean under in-harness invariant tripwires; AWR-170 all-clear at all six lenses (470/470 targeted green, `pre_chorus` mask provably releases on every exit path, held-static duck+restore pinned end-to-end); the F2 emphasis-ladder LED dispatch driven end-to-end by the refuter (named coverage gap CLOSED). Final quiet suite at the fixed head `d634471` (repo-root): 3745 (=3742+AWR-171's 3) / 9F / 6skip / 1xfail — all 9 attributed by name (5 of the known-six; `loader_ships_calibrated` is parent-dir-form-only, its repo-root complement = the 2 `smart_transitions` env reds; patch_c/d live-vs-tracked expected while the applied mirror diverges from the example), AWR-169 pack pair GREEN, ZERO new reds. Live safety held program-wide: no bridge/pad starts, live configs read-only (2 reads: Director enable, smart_drop_mode), all execution on frozen scratch trees + example configs + test seams. Harness-integrity note: the AWR-171 fix drifted into the QA pin tree mid-program (`state_manager.py` only) — caught by QASR3, contained, no verdict rests on the drifted file. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED — the operator's morning mix is the live gate. Commit-race note (pattern #3 tonight, work preserved, never rewritten): the AWR-172 report commit `fab5be9` swept 3 test files a concurrent lane had staged (`test_led_color_engine_m2_patch_c/d.py`, `test_smart_transitions.py` — the environmental-reds fix work); that content belongs to that lane's round, not AWR-172. |
| AWR-173 | CFX filter-sweep LED behavior (low→high only; first runtime consumer of the mixer RE evidence) | `docs/plans/active/cfx_filter_sweep_spec.md` (implementation spec), kickoff `docs/prompts/active/filter_knob_manager_kickoff_2026_07_09.md` | **SPEC AUTHORED 2026-07-09 (filter-lane Fable manager; operator final ruling binding: clockwise/high-pass sweeps only, counterclockwise does NOTHING).** Contract-first done: `rekordbox_readers` forbidden assumption added (CFX is tracking-only; a failed CFX read must never invalidate mixer authority — the `_tick_mixer` whole-snapshot invalidation at `rb_state_reader.py:552` is the trap the spec isolates against). Design (code-verified at `02588d7`): passive CFX read mirrors the mixer path (named 7.2.11 chain group in `rb_offsets.py`, own `_tick_cfx` + `Ev.CFX_STATE` + `CfxFilterSnapshot`, per-deck validity incl. selected-effect-id + unit-channel checks per the RE doc), LED overlay is frame-space in the frame-engine child (flood = lerp toward `Dressing.slot_rgbs[0]` darkest slot, dim = knob-tracked scale; rides the existing ~20 ms `BeatAnchor` pump with `.get`-default wire fields for frozen-child skew), pure-function envelope seam, `cfx_sweep` config block ships `enabled: false`. Blackout/emergency/F2-darkness structurally win (inert at the dispatch gate AND bypassed on the child's blank/emergency paths). Direction mapping (param0>0.5 = clockwise) is the one [assumed] claim — killed by the Task 1 probe at the desk before any LED behavior is trusted. Desk calibration runbook (Part F): probe direction check → ear-bloom MARK capture → pin `bloom_threshold_norm` → menubar-only restarts + single-process check. Routing DECIDED 2026-07-09: operator authorized a Claude implementation round (Opus 4.8 implementer under the filter-lane Fable manager; adversarial review + executive gate before close). **IMPLEMENTED 2026-07-09 (Opus 4.8, Tasks 1–8): `probe_cfx_filter.py`, `rb_offsets.py` CFX 7.2.11 chain group + shared fail-closed parser, `CfxDeckReading`/`CfxFilterSnapshot` + `Ev.CFX_STATE`, `_tick_cfx` (isolated from `_tick_mixer`; `CFX_STATE` routed to StateManager but never in `_authoritative_kinds`), `StateManager` store-only + `_compute_led_cfx_sweep`, pure `cfx_sweep_envelope` + `CfxSweepConfig` (ships `enabled:false`) + `BeatAnchor` cfx fields, frame-engine child overlay `scale(lerp(px,rgb,mix),dim)` with `.get`-default wire fields, read-only `cfx` status dict.** Tests: `tests/test_led_cfx_sweep.py` (envelope/gating/anchor/child/config), CFX groups in `tests/test_rb_offsets.py` + `tests/test_rb_state_reader.py` incl. the mixer-isolation pin. Suite: zero new failures vs the environmental-red baseline; three hard checks green. **ADVERSARIAL REVIEW DONE 2026-07-09 (Opus 4.8): 1 HIGH found+fixed this round — the smart-drop pre-drop tactical blackout sets no blackout owner and rides the child's permitted compose path, so `_compute_led_cfx_sweep` was NOT gating on it and the dark-hue flood could re-light F2's intended-black drop moment; fixed by adding the `bool(self._led_smart_drop_blackout_key)` OR-term to the dispatch gate (the ruled one-line parent-gate shape) plus a new gating test that pins it inert + resets `prev_mix`. 2 LOW: gate comments reworded to name exactly what is checked (blackout/emergency/solo/drop owners; F2 smart-breakdown; smart-drop tactical blackout — NOT pre_chorus, which is laser-only), and a desk-flagged hard-snap dim-leads-flood transient added to Part F step 3 feel-pass. All other attack points clean (authority isolation, push-loop hygiene, neutral-below-half, fail-toward-today, frozen-child skew, offsets parser, contracts); five-red baseline held.** **software-tested / hardware-unvalidated. EXECUTIVE GATE PASSED 2026-07-09 (superman3, independent verification at its desk: foreground suite 3803 = exactly the named five environmental reds in original shapes, zero new; `98c82ca` precedence gate + prev_mix reset read and confirmed pinned; ships-OFF proven — example `enabled:false`, live config carries no cfx keys, absent means off; `_tick_cfx` isolation verified tracking-only; three hard checks green). ROUND CLOSED (build round). **DESK SESSION 2026-07-09 afternoon (manager present): direction assumption CONFIRMED at the desk — live `probe_cfx_filter.py` probe read param0 = 0.000 full-CCW / 0.500 at 12 o'clock / 1.000 full-CW, `[VALID]` throughout, so the one `[assumed]` param0>0.5=clockwise claim is now CONFIRMED (no mapping flip needed). Ear-bloom MARK captures 0.661 / 0.635 / 0.642 → pinned median 0.642 goes in the LIVE `config/led_look_director.json` (the example stays `enabled:false` with the 0.75 placeholder). Operator RE-RULED the past-threshold behavior at the desk: (1) trigger-not-hold — crossing the bloom fires a one-shot timed drain (dim 1.0→`dim_floor` over `drain_ms`, then holds), the dim is NOT knob-tracked and holding the knob does nothing extra ("peak then drain", the flood is the swell); (2) one-way resume — riding back below the threshold releases the WHOLE overlay (mix→0 and dim→1.0 together over `release_ramp_ms`), no staged refill journey.** **TRIGGER-SEMANTICS ROUND IMPLEMENTED 2026-07-09 (Opus 4.8, claude6 lane): parent envelope rewritten to the IDLE/FLOOD/FIRE/FIRED/RELEASE-AFTER-FIRE machine in `cfx_sweep_envelope` (now carries a frozen `CfxEnvState(mix,dim,armed)` across ticks); the knob-tracked dim mapping DELETED (not left behind a flag); `CfxSweepConfig` gains `drain_ms=800.0` + `rearm_hysteresis=0.02` (validated `drain_ms>0`, `0<=rearm_hysteresis<0.2`); example config carries the two new keys, still `enabled:false`. Edge-triggered fire (single-tick jump below-deadband→above-threshold still fires); re-arm only once knob < thr − `rearm_hysteresis` so threshold jitter can't machine-gun. Reader side, child overlay, anchor wire tuple shape, and every gating condition UNTOUCHED; `_compute_led_cfx_sweep` now hard-resets the `CfxEnvState` on all inert/gating paths (incl. the smart-drop tactical blackout). Tests: `tests/test_led_cfx_sweep.py` envelope tests rewritten for the trigger semantics (fire-once, hold-no-effect, single-tick-jump, release-together, re-push re-arm, jitter-no-refire) + two new config-validation tests for the new fields; gating/anchor/child tests kept, constructor calls adjusted to `CfxEnvState`. `test_led_cfx_sweep` (44) + `test_led_state_manager` (128) green; three hard checks green; full suite from repo root = exactly the five named environmental reds, zero new. The Part F hard-snap dim-leads-flood transient note is now MOOT (both flood and drain are timed ramps) — replaced with "`drain_ms` is the drain feel knob". software-tested / hardware-unvalidated.** **FEEL PASS 2026-07-09 evening (operator live, dim_floor re-ruled 0.0 = full black): caught a ride-home RE-BLOOM. Fix round 1 (`389aceb` marker; re-arm moved to the 12-o'clock deadband, `rearm_hysteresis` REMOVED as config) was correct but INSUFFICIENT — the symptom persisted. Root-caused as TWO legs (numeric repros in-lane): (1) the release ramped mix↓ and dim↑ CONCURRENTLY, so from the drained black room the visible product mix*dim flared the flood color to ≈0.25 for ≈400 ms right after crossing back under the bloom (the operator's deterministic symptom); release is now SEQUENTIAL — mix fades to 0 under black first, THEN dim rises, tests pin the visible product curve ≤0.05 while mix>0 (state-only assertions provably missed this — perceptual bugs need product-curve pins); (2) every inert/gating path hard-reset `CfxEnvState` and RE-ARMED mid-ride, so a one-tick gating/validity/v2 blip re-flooded persistently; inert paths now preserve a fired engagement (`CfxEnvState(fired=prev.fired, released=prev.fired)`, `state_manager.py:4984`) — only knob-at-neutral re-arms. One pre-existing test's meaning deliberately flipped with rationale (it pinned the exact wipe that caused post-blackout re-bloom; now pins latched-release). BOTH LEGS STAGED at mix-done park (operator ended the mix, bridge off; live-session staging protocol — no restart) — they auto-load at the next bridge start alongside AWR-185. Side-fix `b1f360c` (executive-routed): `v2_darkest_rgb` added to the laser-color fake engine (5 ERRORs → 28/28; 967ea15 commit-race fallout). Fix diffs scattered by commit races (`39713f1` carried led_dispatch_policy.py; auto-syncs carried the rest) — judge by tree + this row, never commit titles. 211 targeted tests + three hard checks green at park (manager-run, HEAD `d47e332`). Park brief: `docs/prompts/active/filter_lane_state_2026_07_09.md`. REMAINING GATE = operator ride-home retest at the next session (expect black → normal rise, no color flare, no re-flood), then taste knobs.** |
| AWR-174 | Fable legacy transfer verification (legacy lane, operator's last Fable-access day) | `docs/agents/multi_agent_org_workflow.md`, `docs/agents/codex_resume_state_2026_07_09.md`, `docs/agents/opus_seat_harness.md`, kickoff `docs/prompts/active/legacy_manager_kickoff_2026_07_09.md` | **DELIVERED 2026-07-09 (Fable legacy manager, tmux `legacy`).** Adversarial review of the three org docs: every incident cite in org-doc §3/§4/§5 now traces to a written record (`led_manager_state_2026_07_09.md` + the AWR-172 row; the §3 tally line now carries its pointers), dispatch/watch tooling claims verified against the scripts, the D1-F1 fix-shape cite verified correct at its pin `cafd88e` and re-anchored BY SYMBOL in the resume doc (it had already drifted +8 lines the same afternoon when the CFX round landed). COLD-BOOT DRILL PASSED (fresh Sonnet/medium lane, `/clear`, resume-doc kickstart only): seat ladder, dispatch/watch mechanics, the 6-reds procedure, and AWR-173 + D1-F1 state all answered correctly from repo docs alone, D1-F1 deferral included; ONE real transfer gap surfaced and fixed — the lane judged "CFX not landed" from auto-sync commit TITLES (they are opaque and carry real code); the resume doc now says judge by registry row + `git log --stat`. Suite baseline RE-CERTIFIED at this desk: 3801 tests from repo root, the five named repo-root environmental reds reproduce BY NAME (names now written into the resume doc instead of a pointer), plus `test_fallback_second_rename_failure_restores_old_pack` as a full-suite-load flapper (green in isolation — the org-doc §4 class, not chased). CODEX PARITY VERIFIED LIVE (codex-cli 0.142.5, operator's machine): the repo is now itself a plugin marketplace (`.claude-plugin/marketplace.json` + `tools/agents/.claude-plugin/plugin.json` wrapping the footgun-guard hooks file); registration (`codex plugin marketplace add <repo>` + `codex plugin add rbss-agent-hooks@rbss`) and the TUI hooks-review trust flow proven end-to-end with trust hashes landing in `[hooks.state]`; PreToolUse confirmed a first-class 0.142.5 event (review panel, Installed→Active on trust); a trusted plugin hook EXECUTES with Claude-schema JSON stdin (session_start marker fired end-to-end); two Codex-only traps found live and fixed — the parser hard-rejects unknown top-level JSON fields (`_comment` removed from the hooks file) and the plugin cache stays stale after edits until remove+add; the hook is now matcher-less on purpose (Codex's internal shell-tool name unverified; the guard no-ops safely on non-shell tools). NOT PROVEN (Codex model quota returns Jul 11 18:28): `pre_tool_use` firing on a real tool call and exit-2 actually blocking — the retest one-liner is in org-doc §10; until it passes, AGENTS.md rules + `approval_policy` stay the documented Codex-side enforcement. The Codex-seat cold boot is quota-blocked the same way; its boot command is in the resume doc, run it Jul 11. `dispatch_lane.sh` hardened after two bugs hit live during the drill: the `/effort` picker ATE the pasted task text (now: ack-wait before pasting + one re-paste backstop), and reused sessions were never `/clear`ed (now cleared for claude lanes; codex reuse prints `REUSED-SESSION-NO-CLEAR`). Memory-store transfer sweep: standing operator rulings audited item-by-item against repo docs; all but two already had repo homes; the two memory-only ones added to the resume doc (Govee DIY recreation = deferred + Opus-manager-seat-only; bridge↔SS control plane = OS2L transport / MIDI selection). Operator's `~/.codex/config.toml` restored byte-clean after testing (sole residue: a benign ponytail marketplace snapshot refresh; installed ponytail 4.8.4 + its trust entries untouched, hooks file verified byte-identical). SOFTWARE/DESK-VERIFIED ONLY — the Jul-11 quota-window retest is the remaining Codex gate. |
| AWR-175 | F3 blend design (LIGHTING ENGINE v2 Feature 3 — the within-mix blend painter) | `docs/architecture/f3_blend_design.md` (design authority), `docs/plans/active/f3_blend_spec.md` (Part A–E skeleton, gated), kickoff `docs/prompts/active/f3_design_kickoff_2026_07_09.md`, evidence `local/sessions/f3_live_feedback_20260709.jsonl` (gitignored, recorded from the operator's live 2026-07-09 afternoon mix) | **DESIGN AUTHORED 2026-07-09 (f3design lane, Fable/HIGH) — planned; IMPLEMENTATION NOT AUTHORIZED. EXECUTIVE REVIEW PASSED AS BANKED at day-close 2026-07-09 (superman4): preconditions stand unchanged; lane PARKED with written state.** Presence/β blend scalar over the existing upfader+LOW mixer reads (zero reader work; CFX store-only isolation discipline copied one level up), 5-state machine (settled/incoming/blending/commit/abandon; bar-quantized 8-step ownership staircase with slam bypass; the authority flip is NOT the commit — resolve requires crossed-and-held), color-slot-only painting (accents-first, near-glide/distant-trade, dipless/single-axis, white never blended, same-zone hold-tightness), F2 drop-mid-blend interaction table (8 cases; shortened-remainder blackout for a handover landing inside the incoming pre-drop window; freeze-during-claims), fail-toward-today matrix (stale mixer ⇒ F1 soft-flip stand-in; scripted pair ⇒ stand-down; no intensity authority except the brighten-only rank-4 bloom), operator taste list T1–T6 (design D§11) for his desk. Every current-behavior cite verified at `f95a53b`; named integration requirement: F1 identities install lazily on first active dispatch (`led_color_engine.py:1077-1078`) so the incoming deck needs pre-derivation at load. Evidence appendix from the live recording (design §12): FILLED same session after the mix ended — 84,247 mixer snapshots / 54.4 min / 25.8 per s; 7 blend episodes, 5 completed handovers (median 38.5 s, range 12.4–76.5 s), 2 long low-β layering bailouts (24 s / 71 s), 2 slam events; the authority flip lands at β≈0.73–0.74 (4 of 5) with 12–41 s of dual-audible tail after it (flip ≠ commit confirmed on his real hands), and the bass swap precedes the flip by 0.4–4.1 s in the swap-led handovers (the LOW presence term is doing real work). Activation gates: F2 (AWR-163) + F4 (AWR-164) live-gate, operator authorization, Part A re-verification at then-HEAD. |
| AWR-180 | RT-look phase + firework-ember visibility fix round (operator live-feedback session) | `docs/plans/active/rt_phase_ember_visibility_spec_2026_07_09.md` | **LANDED + LIVE 2026-07-09 (ledtune lane): bug-1 grid-quantize executive-gated line-by-line, live at the 16:25 bounce; embers flavor-b (80ebb81) + the riding directives live at the 16:25/17:45 bounces. software-tested, hardware-unvalidated.** Bug 1: continuous-sync looks phase-lock to dispatch instant (no `sync_mode` in params ⇒ `beat_sync_engine.py:124` continuous; renderers get spawn-relative `ir.local_beat`, `govee_realtime_runner.py:487`) — fix = grid-quantize the continuous beat origin (local_beat = abs_beat − floor(spawn abs_beat)); LIVE-CRITICAL (frame-engine child path), executive line-by-line gate required. Bug 2: firework-remnants embers write random palette slots 0–4 (`govee_frame_renderer.py:2008`) — invisible in dark zones (operator loop-tested on two DEEP_POOL tracks) while the slot-5 white background dims to black over 8 beats; fix flavor = operator desk verdict (luminance floor / accent slots / slot-5 white embers). Also riding the round per operator directives: `drop_look_routing` population for low-energy drops (NEUTRAL family + damped-T1 → gentle legacy-chase set; seam verified empty at `led_dispatch_policy.py:2009`), legacy drop_diy demotion verdict (all 8 still in the drop bank — parked cloud-retirement slice re-raised), rt_drop_center_burst off-beat pulses (bug-1 class, covered by the grid-quantize fix). Activation = the executive-batched menubar restart. |
| AWR-181 | Paired post-drop: rejected SEND still burns the queued pair reservation | `docs/plans/active/rt_phase_ember_visibility_spec_2026_07_09.md` (Part J lane finding) | open — batch-3 row (executive-accepted 2026-07-09 at the batch-2 gate). Part J moved pair QUEUEING to accepted drop dispatch (write side, `ce235df`), but on the consume side the director clears the queued pair at decision time (`led_look_director.py` `_automation_decision_for_role` post_drop branch) even when the coordinator then REJECTS the send (e.g. min-dwell) — the configured pair is lost for that drop and the retry falls to normal rotation. Latent, small, not live-urgent; fix = defer the consume/clear until the send is accepted (mirror Part J's write-side semantics), with a min-dwell-rejection test. |
| AWR-176 | Spectral P1: frame-rate growl-band centroid series (the AWR-166 audit's recommendation) | `docs/plans/active/spectral_growl_centroid_p1_spec.md`, source audit `docs/research/spectral_upgrade_audit_2026_07_09.md` | **IMPLEMENTED 2026-07-09 (implementer p1impl, Opus/HIGH) — software-tested; awaiting manager adversarial review + executive gate + operator scrub.** Commits: Task 1 `a474472` (extraction field), Task 2 `8401725` (tolerant cache read + strict shape), Task 3 `bdf5aa6` (derived movement measure), Task 4 `3a95b52` (backfill-aware sweep skip). Tests: `tests/test_growl_centroid.py` (all 10 Part D items green — `test_field_present_and_aligned_to_growl_band`, `test_existing_fields_unchanged_by_new_field`, `test_detects_synthetic_wobble_while_level_is_flat`, `test_static_centroid_is_no_signal`, `test_silence_gate_suppresses_moving_centroid`, `test_absent_or_mismatched_field_is_no_signal`, `test_flags_persistence_drops_isolated_blips`, `test_legacy_payload_without_key_round_trips`, `test_present_but_wrong_length_is_a_miss`, `test_round_trip_preserves_the_field`, `test_entry_with_field_is_skipped`, `test_legacy_entry_without_field_is_backfilled`, `test_absent_entry_is_extracted`); neighbors `test_spectral_profile`/`test_spectral_cache`/`test_audio_spectral_features` unchanged-green. Additive extraction field (harmonic 60–500 Hz centroid per STFT frame, same pass as `growl_band_frames`), tolerant cache read + strict shape validation (no schema bump), pure derived movement measure (computed-not-consumed; operator scrub gates any consumer), backfill-aware sweep skip (the tolerant-read/skip trap at `tools/spectral_sweep.py:81-82` is closed). Zero existing calibration constants change; all 41 AWR-147 verdicts preserved by construction. Remaining: overnight backfill sweep (never against a mix, `caffeinate -i python3 tools/spectral_sweep.py --jobs 2`) + named-track falsifiable check (capochino 1:01.7, Girl$ 1:16.1/2:25.6 vs App-E negatives) + operator scrub gate. |
| AWR-177 | LED white-share drop consumer (first consumer of the F2 plan's per-drop `white_share`) | `docs/plans/active/led_white_share_consumer_spec.md` | **SPEC AUTHORED 2026-07-09 (specbank lane, Fable/HIGH) — planned, nothing implemented; awaiting executive review + dispatch.** Params-only white blend on the drop cue's engine colors at the F4-injection seam (`led_dispatch_policy.py:1508` precedent), mapped from `DropPlanEntry.white_share` (producer verified consumer-less at `d93f047`); kill-switched `f2.drop_white` sub-block (ABSENT-off, **example ships OFF** — activation is an operator live-tuning call), byte-identical-when-off pinned by tests, mask precedence and full-scale law untouched, push-loop-safe (plan lookup + arithmetic only), scripted stand-down reused. Mapping defaults (`lo 0.40 / hi 0.95 / max_blend 0.85`) are provisional TUNE-LIVE anchors from the D§5.2 corpus distribution. |
| AWR-178 | Drop-echo theme-and-variation (creative proposal #4, the sole survivor of the 2026-07-09 ruling) | `docs/plans/active/drop_echo_theme_variation_design.md` (design note, NOT an implementation spec) | **DESIGN NOTE AUTHORED 2026-07-09 (specbank lane) — planned; build QUEUED behind the 2026-07-09 ledtune F2/F4 live-tuning outcomes per the operator's sequencing ruling.** Captures the operator's load-bearing design input (raw ordinal is WRONG: variation ordinal counts within comparable drops (family + tier band), theme = first drop in the track's PEAK tier band, early groove-entry drops never consume the theme slot, escalation via aggression axes never brightness), the mechanism sketch over the existing F2 plan surface (plan-time echo labels on `DropPlanEntry` + params-only dispatch escalation; white escalates through the AWR-177 consumer's input — one owner), the `f2.drop_echo` config sketch, and a 7-item named taste-call veto list the operator rules on before any Part A–E spec is written. |
| AWR-179 | QA minors cleanup round (the six CONFIRMED MINOR findings from AWR-172) | `docs/plans/active/awr179_qa_minors_cleanup_spec.md`, kickoff `docs/prompts/active/qaminors_kickoff_2026_07_09.md`, findings `docs/research/qa_showcase_review_2026_07_09.md` | **ROUND CLOSED — PASS 2026-07-09 18:21 (superman4): consolidated suite 3871 tests at the executive desk, expected red set reconciled BY NAME exactly (4F+1E environmental; zero LaserColorStateManagerHoldTests errors — CFX-fake fix held); the one extra red (patch_f × Part-C bank policy collision) attributed to `f0b40ba`, NOT this round, routed to ledtune.** Build: one Opus lane (claude11, ~51 min), commits `8a90796` (D4-F3) / `80ebb81` (D4-F1) / `56118cd` (D4-F2) / `28b37c7` (D4-F4) / `3b513dd` (D2-F1); T3/T4/T5 CODE swept into concurrent auto-sync commits (D2-F1 code in `32b64b7`) — content-at-HEAD manager-verified line-by-line; build report `docs/prompts/active/qm179_build_report_2026_07_09.md`. Manager review at own desk: 459 scoped tests green across the 7 touched files, hard checks 3/3, all five fixes re-derived at HEAD. EXECUTIVE RULINGS: D2-F1 = KEEP (hunks read at exec desk; 0.0-on-every-path-but-blackout-with-abort, F2-off/scripted gated, fail-open confirmed — matches darkness-tracks-real-voids doctrine); `80ebb81` bare-commit sweep = no-harm misattribution (swept renderer content was ledtune's already-gated embers flavor-b; ledger note, this sentence). The 5 `LaserColorStateManagerHoldTests` divergence errors RESOLVED mid-round by the CFX lane's fake fix (manager-verified 28/28 isolated). All six findings re-verified at `d93f047`: D2-F2 (markers_changed-gated plan) already FIXED at HEAD by `f95a53b` (attach hoisted + 2 regression tests) — dropped from the round with evidence; the remaining five are Tasks 1-5 (D4-F3 single HEAD read; D4-F1 govee provenance gates on freshness+branch with commit drift demoted to warning, contract-first led_govee glob extension; D4-F2 BoundedSemaphore(2) in-flight cap + stale-gen skip, push loop untouched; D4-F4 trim all three per-load structures with no-mask/loop-consequence tests; D2-F1 abort_at wired as EARLY LEVEL DEACTIVATION riding the existing falling-edge mask clear — NOT a shortened window length, which would wrongly move the darkness START). D2-F1 is BEHAVIOR-AFFECTING and DROPPABLE by executive ruling: last task, one isolated commit; delta = with F2 on, hard-family collapses with an early sub-floor return release pre-drop darkness at the abort beat, up to 3 fewer dark beats, fail-open direction (room re-lights early, never darker), F2-off/scripted byte-identical, no new config key. Suite-baseline note (manager desk, `d93f047`, repo root, 3812 tests, 4F/6E): the five NAMED environmental reds reproduce, PLUS 5 new ERRORs in `test_laser_color_engine.LaserColorStateManagerHoldTests` NOT in the certified baseline — attributed: the CFX lane's `_compute_led_cfx_sweep` calls `engine.v2_darkest_rgb()` (real on `LedColorEngine:1281`, missing on the test file's `_FakeLEDColorEngine`), landed via `967ea15` (2026-07-09 12:31, commit-race sweep); test-harness-only, runtime unaffected, owned by the CFX/ledtune workstream, reported to the executive at this round's gate. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-182 | Operator spectral labeling corpus (evening labels session — ear-truth → verified labeled data) | `docs/research/operator_track_labels_2026_07_09.md` (committed corpus), `local/labels/operator_track_labels_2026_07_09.jsonl` (gitignored machine layer, same entries), kickoff `docs/prompts/active/spectral_labeling_manager_kickoff_2026_07_09.md` | **SESSION 1 CLOSED 2026-07-09 19:45 (labels manager, Fable/MAX; operator-attended) — 14 entries (AGREES 5 / PARTIAL 5 / BLIND 1 / MISREAD 2 / VERDICT 1) + same-evening resolution amendments; label-driven round AWR-184 shipped and seam-verified the same evening; 707-track blast-radius sweep cleared all three suspect classes (executive: NO AWR-186); successor batch pinned in the handoff FINAL-STATE (Scary Monsters tier misread · REWIND post-grid-fix re-measure — ANLZ has 0 drop markers until Rekordbox re-analysis, operator expects "full energy throughout every drop" · 32-beat cap-raise datum · ANLZ intro-marker quality note); sweep→operator-verdict→bounded-round is the standing protocol for successor label batches.** Operator lists tracks + timestamps + plain-language descriptions; each statement is recorded verbatim and measured read-only through the real seams (`read_anlz_drops` grid/drops, `spectral_cache.get_cached_v4`, `led_identity_v2.identity_scores`/`assign_zone`, `lighting_moments_v2.build_track_plan`) and classified AGREES / PARTIAL / BLIND / MISREAD against what the analysis sees. Seams smoke-tested end-to-end at `f0b40ba` on Sexy (Extended Mix) 3:38: reproduces the executive's AWR-176 Part E desk measurements (8 drops, growl_band flat ~27.0 dB, `growl_centroid_frames` absent pre-backfill) — corpus seeded with that case as entry 1 (BLIND, missing dimension = P1 growl centroid). Growl labels tonight are amplitude-only (backfill runs after 20:00) and become the P1 acceptance set. Stems-relevant entries are tagged `stems` and relayed to the stems session (which owns the stems re-score); behavior verdicts relay to the executive seat. Zero behavior change — labels are calibration/acceptance evidence only. |
| AWR-183 | USB Saturday-readiness pass (foreign-Mac test prep: explainer + fresh DMG + checklist) | kickoff `docs/prompts/active/usb_saturday_manager_kickoff_2026_07_09.md`, runbook `docs/setup/usb_launcher_runbook.md` | **DELIVERED 2026-07-09 evening (USB Saturday manager, Fable/MAX; operator-attended).** Full plain-language explainer + Saturday checklist delivered in chat. **DMG REBUILT from HEAD `c030540`** — the 03:36 M1 build predated the entire day's commits, so it was stale by ~40 commits: rebuild via `packaging/rbss_launcher.spec` (PyInstaller 6.21.0 × Py 3.14.6) → ad-hoc re-sign (`packaging/sign.sh`; 0 valid signing identities on disk, re-run when the cert exists) → `hdiutil` UDZO. Result `dist/RBSS Bridge.dmg` 111MB / app 252MB (17:59); zero `.py` drift between the build's base commit and commit-time HEAD (`git diff --name-only` empty). Scoped packaging suites green post-build: `tests.test_usb_launcher` + `tests.test_launch_profile` + `tests.test_launch_agent_plist` + `tests.test_govee_frame_engine` = 65/65 OK (scoped only — live sessions on the machine, no full suite). Runbook drift fixed: frozen frame-engine re-exec bullet removed from "deliberately NOT built" (landed `122be01`, verified `govee_frame_engine_client.py:41-51`). Saturday pins re-verified at HEAD: RB offsets table carries 5 arm64-only versions but **only 7.2.11 is confirmed live** (`config.py:24`; unsupported version ⇒ direct reads disabled cleanly, `rb_state_reader.py:977`); the bundle itself is arm64-only ⇒ the friend needs Apple Silicon + RB 7.2.11, confirmed BEFORE Saturday (checklist #1); XDJ-RX = performance mode only (standalone stick playback invisible, AWR-167); spectral cache is path+beatgrid-keyed ⇒ one-time ~15s per-track extraction on the foreign Mac (identity/palettes content-keyed, survive); Test-the-Lights recordings are machine-local `/tmp` ⇒ the friend's Mac starts with none (record there first; refuses while Rekordbox runs — by design). Foreign-Mac memory read under a bundled ad-hoc app = the top open risk (design §5), memory-read STOP rule in force. The operator §2 parity run (his Mac, ideally pre-Saturday) remains the M1 gate. **SAME-EVENING ADDENDUM (operator directive "everything happens today and now"; superman4 PROCEED ruling): `tools/spectral_stick_sweep.py` BUILT + PILOTED** — pre-warms the friend's Mac so all stick tracks are full-strength from first beat (kills the ~15s first-load gap across the whole export). Enumerates the mounted export via `PIONEER/USBANLZ` PPTH tags (567/569 ANLZ resolve to on-stick audio), reuses `spectral_sweep._sweep_one` verbatim, writes v4 entries keyed by on-stick paths; contract glob + AGENTS.md row + research-doc entry added first (anti-drift), unit tests 4/4 (`tests/test_spectral_stick_sweep.py`, pure enumeration seam), hard checks green, throttled pilot per executive constraint (`nice -n 19 --jobs 1 --limit 3`) 3/3 ok at ~22s/track, and desk-proven key parity: runtime-style `get_cached_v4(on-stick path, ANLZ beatgrid)` HITS the sweep-written entry (growl field present). **FULL RUN = operator after-20:00 list (executive-staged next to the P1 backfill, never while he mixes): `caffeinate -i nice -n 19 python3 tools/spectral_stick_sweep.py /Volumes/MINK --jobs 2` (~2-2.5h unattended) — **FIRED ~20:52 by the usb lane** (the superman4 volume-watch re-fire was lost in the seat transition; P1 backfill confirmed running alongside per the approved pairing): nohup chain sweep → payload auto-export to `/Volumes/MINK/RBSS_payload/spectral_cache` → signal `usb.SWEEPMINK.done`, log `/tmp/rbss_stick_sweep_mink.log`, survives session purge; launch verified (scope: 567 stick tracks keyed under /Volumes/MINK). Accidental operator unplug ~21:25 interrupted run 1 (35 min in; 413 clean no_anlz skips, zero loss — every completed extraction persists in the local cache; payload export never ran, CHAIN FAILED logged as designed); relaunched 21:27 after remount, resume PROVEN by cached-skip ([50/567] all cached at 0.0m). **COMPLETE 22:28**: run 4 clean full pass (567/567; the 221 no_anlz class = disconnect artifacts, re-read fine), payload merged over the executive's 22:02 partial and fired `usb.SWEEPMINK.done`. FINAL STICK LAYOUT (operator reorganized): everything lives in `/Volumes/MINK/RBSS BRIDGE USB/` — `RBSS Bridge.dmg` + `install.command` + `purge.command` + `RBSS_payload/spectral_cache` (3,588 JSONs); root duplicate reconciled (stale 3,142-file partial replaced by the complete set, count-guarded). install.command resolves siblings, so the folder layout needs no code change. Saturday risk flags standing: flaky stick connection (two silent drops — different port + backup stick, clone must also be named MINK). Saturday manual copy is superseded by the on-stick payload + install.command; fallback copy `~/Library/Application Support/RBSS Bridge/spectral_cache` to the same path on the friend's Mac.** Cross-machine caveat: keys assume the stick mounts as `/Volumes/MINK` on the target (operator renamed the stick USB→MINK 2026-07-09 ~20:10; helpers+DMG verified intact on MINK) (volume-name collision ⇒ misses ⇒ falls back to the 15s path, never darker). Labeled INTERIM with the root cause scheduled the same breath: AWR-165 content-keying spec authored tonight (specbank dispatch per superman4 ruling — code round denied at the 35-min model cutoff with live lanes on those files; banks for the Jul-11 Codex window). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-186 | USB launcher M2 Codex spec (operator directive: native install / payload / home-parity / menubar PURGE) | `docs/plans/active/usb_bridge_launcher_m2_codex_spec.md`, directive capture `docs/plans/active/usb_launcher_m2_operator_directive_2026_07_09.md` | **SPEC AUTHORED 2026-07-09 evening (USB Saturday manager, Fable/MAX; superman4 SPEC-TONIGHT-BANK-FOR-JUL-11 ruling on the operator's ‘everything happens today and now’ directive) — planned, nothing implemented; executes in the Jul-11 Codex window after executive re-verification at then-HEAD.** Part A–E + checklist: Task 0 verify-first inventory (config read points, frozen local/state landing — BLOCK on divergence), `packaging/make_stick.sh` one-command stick builder (staged DMG carries app + RBSS_payload incl. secrets default-YES, operator veto open; honest security line: lost stick = key exposure), native menubar install (DMG-run detection, shared file-level manifest interoperable with the AWR-122 interim helpers, App-Support config overrides via EXISTING seams), frozen-only `local/state` → App Support state dir, menubar PURGE (explicit confirm button, stop-child-first, three allowed roots only, honest TCC-residue line), runbook/design/contract closure. R5 (XDJ-RX3 stick reading) EXCLUDED per settled AWR-167. Live safety: install never auto-starts the bridge; push loop gains nothing; flock discipline unchanged. **OPERATOR APPROVALS 2026-07-09 ~22:40 (in-chat, verbatim “I approve of secrets on stick” + approval of the full CANNOT-DO-YET build list except physical-validation items): secrets-on-stick decision CLOSED (risk accepted), the AWR-186 + home-parity-payload + AWR-165 build scope operator-endorsed; the foreign-Mac memory-read test and the parity walkthrough remain physical gates, unapproved-able by design.** **BUILD DISPATCHED ~22:45 same evening on a further operator override (verbatim: “no, i approve for it to be done NOW” — supersedes the Jul-11 banking; executive mailbox notified with countermand path, none received at dispatch): Opus/HIGH orchestrator in tmux usbm2 (TAG USBM2) executes the spec under docs/prompts/active/usb_m2_build_orchestrator_brief_2026_07_09.md — hard fence excludes all AWR-165 files + runtime-critical files; STAGED-PENDING-RESTART; no bridge/app launches; make_stick.sh built but NOT run against MINK tonight. AWR-165 dispatches as a second round only after the M2 manager review + gate.** **EXECUTIVE SEQUENCING (superman4, ~22:50, quota override not countermand): usbm2 booted at 91% session-limit (resets 12am) — the ~22:45 dispatch was STOOD DOWN CLEAN (lane idle, zero commits, watcher killed) and the round is APPROVED + SCHEDULED to fire 00:05 with a full window via the executive's armed trigger; seat corrected to Fable/XHIGH per the standing operator order “FABLE XHIGH FOR EVERYTHING” (brief + kickstart updated); chain unchanged (usbm2 builds → usb manager reviews → executive gates).** **M2 BUILD EXECUTED 2026-07-10 00:05–00:5x (usbm2 orchestrator, Fable/XHIGH): Tasks 0–5 all landed — Task 0 inventory (17ba826, every Part A cite re-verified; one recorded divergence: config.py holds no config loads, the seams live in the subsystem config modules), Task 1 make_stick.sh (content in auto-sync 20d3833/5b8b2ad + 276808c; NOT run against the stick per the fence), Task 2 native install + config wiring (af8a47b/828c2bd/bdbd964 + 3ba9951; new install_controller.py; one new env RBSS_LASER_COLOR_MAP_CONFIG at the laser_color_engine default seam), Task 3 frozen state dir (78e3a1b, launch_profile.resolve_state_path; led_config + drop_presentation seams only), Task 4 menubar PURGE (a31bde9, confirm-gated, stop-child-by-handle-first, three-root allowlist), Task 5 docs/contract closure (this commit). Scoped suites green per task; hard checks green; secrets grep clean. STAGED-PENDING-RESTART: every M2 surface is frozen-bundle-only; no bundle rebuilt, no bridge/pad process touched. Evidence report to the usb manager — MANAGER REVIEW + EXECUTIVE GATE PENDING; the operator walkthrough (runbook parity table, now incl. install→run→purge rows) remains the physical gate.** **EXECUTIVE-GATE FIX 2026-07-10 (USB-CROSSPLATFORM-GATE, Opus/XHIGH solo): the crossplatform add-on's two root-cause defects fixed — (1) `enttec_dmx_pro.find_enttec_port` now returns a device only on POSITIVE ENTTEC identity (manufacturer/product/description strings), rejecting a lone generic FTDI (VID 0x0403) or a bare `usbserial` device name and failing closed to the configured port (two identified Enttecs = ambiguous None; detection metadata-only, no port opened); (2) `packaging/make_stick.sh` now fails closed on a malformed/unreadable pack config or a declared non-empty `pack_path` that is not a readable directory (an absent/empty `pack_path` stays backward-compatible no-pack). Focused tests added to `tests/test_enttec_dmx_pro.py` + `tests/test_make_stick.py`; no new AWR ID; auto-detect is positive-identity-only and may still require the explicit configured port; all physical Enttec/DMX behavior hardware-unvalidated.** **FROZEN-APP FOREIGN-MAC FIX ROUND 2026-07-10/11 (branch `claude/rbss-bridge-install-debug-59yrn6`, Opus 4.8/XHIGH; commits `3fd59ce` + `8da5f01` + follow-up): the first foreign-Mac run of the M2 bundle failed across the board — all traced to packaged-but-never-adapted-for-standalone. Fixed 8 of 9 defects (#9 unresponsive-menu REFUTED — hashing already off-main-thread): python.org universal2 low-target build (fixes the macOS-12 `_mkfifoat` crash; SPECTRAL REQUIRED, fail-loud, never silently dropped), flock menubar singleton (replaces the pgrep/`MENUBAR_PATTERN` guard), surface silent child crashes + `sanitized_system_env` DYLD_ strip (Enable-Rekordbox-Reads + bridge start), Export disabled when frozen, pads spawn-if-down AND wired to the App Support live config (never the code-signed bundle), portable `ICON_DIR` + bundled icons, install/purge `.command` full-payload parity. Verified by two fan-out workflow passes (9 confirmations + 8 adversarial skeptics; 3 residual bugs fixed pre-commit) then re-gated by a read-only Opus 4.8/XHIGH seat (`sol46usbgate`) which found + got fixed two more blockers (frozen pad config wired to the bundle not App Support; `make_stick` half-built-venv reuse) plus this contract/docs drift (`rekordbox_patch.py` added to the `usb_launcher` contract). Tests: touched modules green; full discover = same pre-existing reds, 0 new; 3 hard checks green. ALL frozen/macOS behavior operator-unvalidated — gate = operator build (python.org 3.13) + run on the macOS-12 Apple Silicon Mac.** **ROUND 2 (gate sol46usbgate 15-agent ultracode audit; verified per-item first): P0 — mutagen added to `[bundle]` + spec hiddenimports (guest scripted/autoloop reads), pad Save never writes into the signed bundle (`_pad_config_path` frozen→App Support), arm64-honest build + fail-closed `lipo` arch assertion, `LSMinimumSystemVersion=11.0` + real version. P1 diagnosability — a `rekordbox` status block (`unsupported_version` / `reads_blocked`) surfaces on the menubar BRIDGE row so a guest sees WHY, not silent no-lights (rb_memory attach health → runtime_status pure `rekordbox_status` provider → menubar; additive, no 200Hz-loop touch). P2 — numba cache→App Support, reinstall keeps tuned config, Govee cloud failure no longer kills the LAN strip, installer disk-guard+rollback, user-cancel not misclassified as a codesign failure, install.command DMG-detach trap moved before the mount check. Local-Network-TCC-denied recorded as a known limitation (no Python-visible API). `pyproject.toml` added to the usb_launcher contract. Tests per fix; same pre-existing reds, 0 new; 3 hard checks green.** SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-184 | Deep sub-void blackout rung — real-void labels batch-1 (operator-directed, ledfix3 lane) | code + comment `lighting_moments_v2.py` `darkness_ladder`; evidence `docs/research/operator_track_labels_2026_07_09.md` UT-3/UT-6; contract key `led_govee` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 evening (ledfix3 build lane, executive-directed via AWR-182 labels; STAGED — activates at the executive-coordinated bounce).** Operator ear-truth (Utopia/Dombresky): 1:27 = "2 bar blackout" (drop b192), 3:00 = "1 bar blackout" (drop b384) — both genuine musical voids the balloon rung was answering with a melodic shrink (perc_build 0.27/0.29 < 0.35). **Evidence gate PASSED against the real spectral cache** before any code: a deep sub void ALONE does not discriminate (Caramelle's −14 dB melodic swell — the canonical balloon case — voids its sub just as deep, run min −37 dB); the separator is the GROWL/tonal band — it SUSTAINS ≈18 dB through Caramelle's filtered swell but COLLAPSES to ≈−4 dB through Utopia's voids (everything cuts). One additive rung placed AFTER the Part H true-silence rung and BEFORE the balloon split: when the sub is voided (`sub_db < SUB_VOID_DB −10`) for ≥`VOID_MIN_BEATS` (2) beats ending at the drop AND the growl band is dark over that run (`min growl_band_db < GROWL_DARK_DB 5.0`), blackout for the void's length rounded UP to bars {4,8,16}, ending at the drop. Real-cache decisions through the edited ladder: Utopia b192 → blackout 8 (2 bars, win 184-192), Utopia b384 → blackout 4 (1 bar, win 380-384); Caramelle @2:07 → still balloon; Utopia b128 (perc 0.18 melodic, shallow sub) → still balloon. Three new named constants, no existing constant moved (`BALLOON_PERC_BOUNDARY`, Part H thresholds, all AWR-147-frozen constants untouched). Tests: `tests/test_lighting_moments_v2.py::TestDeepSubVoidBlackout` (5 cases: 2-bar/1-bar blackout, growl-alive-stays-balloon, shallow-sub-stays-balloon, single-deep-beat-not-enough); file suite 55/55; full `discover` at the known-six environmental-red baseline (all in soundswitch/laser-color/LED-Patch-D/export-pack/parity-oracle, none import lighting_moments — zero new reds); the three hard doc checks green. Part H true-silence pins (Killa 513-521), Sexy 1:27 snare-wall demote, and the Caramelle balloon control all unchanged (existing tests green, rung placed after silence + gated by growl so it can't steal those cases). No config edit, no restart, no process signal — F2-off and scripted-deck paths byte-identical (ladder only consulted by F2 planning). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-185 | Deep sub-void rung yields to the true-stop rung — AWR-184 parked-finding fix (ledfix3 lane) | code + comment `lighting_moments_v2.py` `darkness_ladder`; spec `docs/prompts/active/awr185_guard_dispatch_2026_07_09.md`; sweep evidence `docs/research/awr184_stop_ambiguity_sweep_2026_07_09.md`; contract key `led_govee` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 evening (ledfix3 build lane; STAGED — activates at the executive-coordinated bounce).** ledtune's post-AWR-184 finding, confirmed REAL by the executive sweep: AWR-184's rung 0b reads no full-band audibility, so a vocal-stop shape (deep sub void + dark growl band, but vocals sitting above the 60–500 Hz growl band so the full band is still audible) was answered by 0b's run-length rounding instead of the calibrated true-stop length — resolving e.g. a 2-beat void to blackout 4 where the stop rung gives 8. Taste-LENGTH only (both outcomes are blackouts); 55/716 v4 cache tracks carry the ambiguity class. **Fix = ONE precedence guard, no thresholds moved:** the true-stop predicate (`perc_build ≤ STOP_PERC_MAX AND lift_build ≥ STOP_LIFT_FLOOR`) is hoisted above rung 0b and 0b is gated on `not stop`, so when the stop rung would fire its calibrated 8-beat length wins. Utopia is NOT in the class (its voids kill the full band below ref−10 ⇒ `stop=False`), so b192 → blackout 8 and b384 → blackout 4 are PROVEN unchanged (explicit pins). New pin `test_vocal_stop_yields_to_stop_rung` (Cruel Summer 418-shape: 51-beat void, growl 0.3, full ref−9.4, perc done → resolves via STOP = 8, not 0b's round-up-to-16); the existing `TestDeepSubVoidBlackout` suite, Killa 513-521 true-silence, and Caramelle balloon all unchanged. File suite 56/56; three hard doc checks green. No config edit, no restart, no process signal — F2-off and scripted-deck paths byte-identical. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-187 | Firework explosion redesign — strobing multi-hue explosion + quick dim + aggressive embers (operator visual spec, superman4 final round; ledfix3 lane) | brief `docs/prompts/active/final_round_b_firework_2026_07_09.md`; code `govee_frame_renderer.py` `_drop_firework_explosion_2`/`_ember_env`; apply-script `tools/apply_firework_redesign.py`; contract key `led_govee` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 evening (STAGED — live look flips at the executive gate).** Operator acceptance (verbatim): 'the firework background explosion should strobe with sparkling hues and then when the firework explosion background quickly dims, the embers continue to aggressively spark.' New effect `drop_firework_explosion_2`: explosion window (2×`surge_beats`, default 0.25) strobes a per-pixel multi-hue field on `_hz_strobe_on` (hues re-dealt each flash from the injected palette tints — `color_a`/`color_b` engine multi-inject with `color_source: engine`, plus `spark_a`/`spark_b`); background quick-dims to `bg_hold` 0.25 (floor 0.0; v1 held 0.7/floor 0.2); embers go fast-in/exp-out (`_ember_env` sharp, `sparkle_life_s` 0.15, `sparkle_density` 0.5) and blend-replace at full intensity over the dimmed bg — measured post-dim contrast 200/255 vs the AWR-161 ≥60 bar, same measurement. Registered as a STROBE + C5 allowlist (hz/duty/color_b added); time-based embers stand (AWR-153); no constants outside this effect (v1 + the slot `_ember_field` byte-unchanged, sine path pinned). **Deliberate shape: v1 `drop_firework_explosion` stays registered (non-strobe) so the pre-apply live config keeps validating** — same-name strobe registration would have hard-failed live config load at the next bridge start (C5 requires `allow_strobe=true`) and turned the patch_d live tripwire red until the gate; the AWR-161-FIX precedent (register-only-when-configs-already-carry-the-flag) rules. Example config flipped (`rt_drop_firework_explosion` → v2, `allow_strobe: true`, engine palette, new params + hz 6.0/duty 0.3). LIVE config untouched this round: the executive gate runs `tools/apply_firework_redesign.py` (CFIX2 precedent — flips exactly the one look, atomic write + one-time `.pre_awr187.bak`, refuses without `safety.allow_strobe`, idempotent) BEFORE the next bridge start. Post-gate cleanup queued: retire v1 + its registration pin, collapse the patch_d two-state tripwire, re-point pad defaults if v1's CONTROL_META globals go with it. Tests: rewritten `DropFireworkExplosionTests` (7), `tests/test_apply_firework_redesign.py` (4), pad-defaults pins extended; renderer file 84/84, patch_d two pre-existing live-config reds unchanged (rt_rainbow_drop bank membership + `slot_colors_from` KeyError — both reproduce byte-identically on the HEAD test version, logged for the executive). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-191 | Fleet cleanup — pad-catalog wiring completion + live-tripwire re-pin (final-board reconciliation; ledfix3 lane) | code `led_pad_controls.py`; tests `tests/test_led_pad_controls.py`, `tests/test_led_color_engine_m2_patch_d.py` | **DONE / SOFTWARE-TESTED 2026-07-09 night (staged-only, no process contact).** Item 1: AWR-188's `palette_comet` was registered in the renderer but never wired into the pad catalog, failing the module-level RENDER_GROUPS completeness assertion and killing all three `led_pad_*` test modules at import (AWR-187's wiring had landed in `0c6cdbf` — the board's 'both unwired' was half-stale). Wired: `palette_span` CONTROL_META row (hand-extracted `params.get("palette_span", 1.0)`), Drop-group placement beside `rainbow_ordered`, NO `PARAM_DEFAULT_OVERRIDES` row (all its fallbacks match the CONTROL_META globals, the rainbow_ordered precedent), plus the AWR-156-pattern audit literals for BOTH effects (AWR-187's four divergent v2 fallbacks + palette_span) and the `palette_span` default pin in `LedPadControlDefaultsTests`. All three `led_pad_*` modules import and pass. Item 2: `test_tracked_and_live_configs_validate` re-pinned to the APPROVED post-gate live state — firework two-state pin collapsed to the post-apply literal (gate ran `tools/apply_firework_redesign.py` `353dfa7`; backup `.pre_awr187.bak` on disk), the rainbow pull pinned protectively (role None ⇒ assertNotIn the drop bank — an unapproved re-add turns red), the two AWR-188 palette-comet looks pinned (drop + post_drop; rows now carry per-row safety_class since the post_drop look is class post_drop). Tripwire stays exact-literal for everything else. patch_d module green except the long-standing pre-existing `test_drop_slot_color_smoke_and_snap` KeyError (`slot_colors_from`, named in the AWR-156 baseline). Three hard doc checks green. AWR-187's post-gate cleanup queue partially discharged (tripwire collapse done; v1 effect retirement still queued). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-189 | Continuous-look sustained-divergence BPM re-anchor (born_bpm fix, operator-ratified shape; ledtune build lane) | code + comment `beat_sync_engine.py`; diagnosis trail in the successor handoff addendum; contract key `led_govee` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 late evening (ledtune lane, superman4 dispatch; STAGED — activates at the executive-coordinated bounce).** Live-diagnosed same evening (attended): a CONTINUOUS look spawning while the live-BPM feed was mid-transition (127↔160 churn; one wrong 150 held >10 s) froze that stale rate in `born_bpm` forever — "beatsynced looks do not look beatsynced, inconsistent even when bpm is stable" — and the Bug-1 grid-quantized origin made the drift visible with no snap-back. Fix (ratified shape): `_maybe_reanchor` in the continuous `on_tick` branch — when |live anchor bpm − current rate| exceeds `REANCHOR_BPM_DELTA` (2.0) CONTINUOUSLY for `REANCHOR_SUSTAIN_S` (3.0 s), the instance's beat-phase origin re-bases to an atomic `(monotonic, abs_beat, bpm)` snapshot: one phase snap onto the real grid, live-rate accrual from there; a still-wrong feed re-arms against the new rate and converges. One in-band sample resets the timer — NO raw tracking, AWR-141 wrap/jitter immunity untouched (its pins stay green). `local_t`/bucket/progress stay born-based (time-based ember layers and comet sweeps never restart); retrigger/overlap modes untouched; never-diverged rendering is byte-identical (None-snapshot falls through to the exact born expression). Both knobs per-look config-overridable via params (`reanchor_bpm_delta`/`reanchor_sustain_s`) through the existing spec.params plumbing — runner unchanged (it already passes `anchor.bpm` every tick). Tests: `tests/test_beat_sync_engine.py::Awr189ReanchorTests` (8 cases incl. the heartbeat shape); module 33/33; `tests/test_govee_realtime_runner.py` 43/43; three hard doc checks green. Operator acceptance look on record: `rt_groove_heartbeat` at next play. Pure logic, no I/O, no config edit, no restart. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-188 | Palette-cycling comet — rainbow-class generalization, Part G build + re-bank (ledfix2 lane, superman4 final round) | code `govee_frame_renderer.py` (`palette_comet` slot effect + C5 allowlist); `config/led_look_director.example.json` (`rt_drop_palette_comet`/`rt_post_drop_palette_comet` + `drop_pairs`); apply-script `tools/apply_partg_palette_comet.py`; tests `tests/test_partg_palette_comet.py`; design authority Part G in `docs/plans/active/rt_phase_ember_visibility_spec_2026_07_09.md`; contract key `led_govee` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 late evening (operator overrule: rainbow-class looks get FIXED, not pulled). STAGED — live-config banking is NOT applied in-round.** New `palette_comet` SLOT effect = the accepted `rainbow_ordered` movement + position-plus-time color phase quantized onto the runtime-injected palette slots (a palette, not a renderer branch): rainbow-classified tracks render rainbow via their rainbow palette, every other track cycles its own colors. Slots 0-4 only (slot 5 = reserved white accent), any palette length, deterministic seeded start offset, not a strobe. Example-config pair mirrors the pulled `rt_rainbow_*` movement params with `color_source engine`; `rt_rainbow_*` stays DEFINED but unrouted (kill = later cleanup after live acceptance). Live-config banking (look defs + pair, drop look → COMET tier-2/3 pools + `banks.default.drop`, post_drop look → `banks.default.post_drop`) ships as `tools/apply_partg_palette_comet.py` — add-if-missing, loader-gated, timestamped backup, atomic write, self-verifying, idempotent; THE EXECUTIVE runs it after the Round A `f2.drop_look_routing` rewrite lands (config-collision fence; script refuses a pre-Round-A config). Tests: `tests/test_partg_palette_comet.py` 15/15 (determinism, 3/5/6-slot palette sweep, slot-5 discipline, C5 registration guard, pair wiring, apply-script end-to-end on the example config incl. refuse-path); four SLOT_EFFECTS registry guards extended (16→17); scoped suites + three hard doc checks green. Open: executive apply + operator live acceptance, then the rt_rainbow cleanup round (incl. the patch_d live-config tripwire row now red from the ~20:35 rainbow pull). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-192 | Menubar menu overhaul — structure/naming/grouping/glance-states refactor of `scripts/bridge_menubar.py` (menubar lane manager, Fable/HIGH, operator 00:05-wave order; superman4 dispatch) | design `docs/plans/active/menubar_overhaul_design_2026_07_10.md`; build spec `docs/plans/active/menubar_overhaul_build_spec_2026_07_10.md`; brief `docs/prompts/active/menubar_overhaul_brief_2026_07_10.md`; code `scripts/bridge_menubar.py`; tests `tests.test_bridge_menubar`; contract key `bridge_menubar` | **DONE / SOFTWARE-TESTED 2026-07-10 ~01:05 (menubar lane; manager verdict PASS after one required-fix round; STAGED — activates at the operator's next menubar restart; executive gate pending).** Fence honored: build dispatched only after AWR-186's menubar items landed (`a31bde9`); build lane = menubuild tmux, dispatched Opus/HIGH then executive-corrected to Fable/XHIGH pre-work (pins verified on-screen). Landed (commits `34bb41e` LED row, `60485f7` auto-sync-swept blueprint literal, `9a432b8` regroup, `f83529f` docs, `599ede3`+`0eec665` fix round): (1) LEDs glance row 10 in both `compact_status_lines` branches via pure fail-soft `led_row_fields()` (On/Off/—, achieved fps while realtime-active, effect, palette, orange degraded suffix — the AWR-151 blind spot closed); (2) `MENU_BLUEPRINT` pure-data layout + `_build_menu_entry` walker — five blocks (status / toggle / LIVE / AUTHORING+CHECKS / MAINTENANCE), laser blackout + clear promoted to top level (same selectors + gating; retitled "Laser Blackout"/"Clear Laser Blackout"), "Quit Menu"→"Quit Menubar (bridge keeps running)"; (3) M2 purge item in the maintenance slot, M2 install offer kept PRIMARY at menu top per the AWR-186 spec (manager-caught defect in this round's own build spec — the build lane flagged it first in NOTES; fix round restored M2's index-0 mechanism verbatim). Payload-diff 0 lines (`append_command` set unchanged), `toggleBridge_`/`_toggle_bridge_frozen`/watcher semantics byte-identical, M2 handler bodies untouched, source-run carries no install/purge items (frozen gate). Tests 91/91 (7 new BY NAME in the software-test-inventory row; two re-pinned in the fix round), neighbor `tests.test_ss_bridge_watcher` 7/7, three hard checks green — all re-derived at the manager desk, not taken from lane reports. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-193 | LED Pad + Template Lab overhaul — ten operator-verified defects (accept-what-you-hear, decoupled preview, promotion retirement/unbrick, honor `fn`, single-source param bounds, color pickers + regime badges, operator-clean surface, labels, restart-surviving pages, never-stale freshness contract) (pad lane manager, Fable/XHIGH, operator 00:05-wave order; superman4 dispatch) | brief `docs/prompts/active/pad_overhaul_brief_2026_07_10.md`; spec (= design note) `docs/plans/active/led_pad_overhaul_spec.md`; code `tools/led_pad_web.py`, `tools/led_pad_lab.py`, `led_pad_controls.py`, `tools/led_pad_assets/**`; tests `tests.test_led_pad_service`, `tests.test_led_pad_lab`, `tests.test_led_pad_controls`; contract key `led_pad` | **BUILT 2026-07-10 ~00:50 — all ten Part B tasks implemented + software-tested by the padbuild lane; labeled commits `4ad17c2` (T1), T2/T3/T4 (`71cb036`, `75dfb54` + T2 in the commit before T3), `aa2ac08` (T6 css), `5aedb5c` (T7), `5b09459` (T8); Tasks 5/9 and parts of 1/2/6 were swept into interleaved auto-sync commits (`bdbd964`, `ca221b8`, `5b067a1`, `baf1458`, `951fb5d`, `c85cd4a`, `60485f7`) by parallel-lane Stop hooks — all content verified at HEAD. New/updated tests: +4 lab-collision/archive, +2 fn-fallback, +2 accept-snapshot, +1 preview-overlay, +3 effective-specs (+1 service decoration), +6 freshness pure, +1 live_changed lifecycle, +1 no-cache, +1 archive route (pad test files 80 green scoped). MANAGER REVIEW 01:0x: PASS w/ two manager-applied fixes (`5816575` + lab.py diff swept into `bc0bbaf`): fn-resolver memoization per reload (kills per-frame registry I/O on the playback tick thread) + dead `cue_beats` preview key removed; fixes flagged for executive scrutiny. Independent desk evidence: full suite 4030 reconciled BY NAME to exactly the five baseline reds; scoped pad modules 81 green; JS/HTML reviewed line-by-line (no JS harness — code-review evidence class); fence clean incl. one-line .gitignore. Build lane's no-bump call on the global contracts marker RATIFIED (spec's per-contract assumption was wrong). ACTIVATED 01:0x (brief-authorized, mailbox-announced; ownership free + nothing playing + bridge OFF verified first): launchctl kickstart onto new code; smoke green: exactly 1 process, /lab 200, Cache-Control no-cache (JSON+static), live_changed in /api/config, both live-bricked drafts collision-flagged with archive path live, stale strobe_blue hz/duty bounds CONTROL_META-corrected with conflicts surfaced, never-stale watchdog PROVEN end-to-end (touch renderer → logged freshness restart → new PID in 8s). OPEN: executive gate on the review-fix commits + operator morning browser pass (archive the two flagged drafts, pickers, reconnect banner). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-190 | SS-MIDI port-gone WARNING spam — edge-trigger (haze lane, superman4 dispatch; parked Jul-7 regression: ~12/min WARNING with the SS app closed) | code `soundswitch_midi_input.py` `_worker`; tests `tests/test_soundswitch_midi_input.py` `TestPortGoneLogEdgeTrigger`; executes Task 1 of `docs/plans/active/logging_edge_trigger_warning_spam_spec.md` (AWR-125 UX item 11; Tasks 2–6 remain open); contract key `soundswitch_pack_player` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-09 late evening (haze lane; STAGED — the running bridge keeps the old cadence until the executive-coordinated restart).** WARNING once per gone-episode via `bf.log_changed("ss_midi_port_gone:<port>", ...)` (logging.md rule: edge guards never per-instance), DEBUG on each retry between, INFO `input port connected` once on rebind; key primed False at worker start so a healthy first boot logs no spurious connected-edge. `_mark_port_gone()` gating and both retry intervals byte-identical (2026-06-30 slow-retry protection untouched); no behavior change to what the rig outputs. Tests 71/71 module (3 new: never-seen warns once then DEBUG; rebind INFO once + WARNING re-arms across episodes; healthy first boot emits no connected-edge). Neighbor scoped run's only red = named env baseline `test_ddj_slots_8_16_17_24_exact_ch1_ch19` (pre-existing, resume-doc five-red list). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-194 | LOOKS wave-1 authoring campaign — ~25 lab drafts filling measured pool gaps vs CURRENT axes (operator "author a bunch of new looks" order, executive wave-1/wave-2 split; looks lane, Fable/MEDIUM, authoring-only) | brief `docs/prompts/active/looks_wave1_brief_2026_07_10.md`; drafts + effects live in `config/led_lab/` (gitignored, never committed); written through the running Lab save API (`/api/lab/save`, atomic registry writes — pad-lane fence respected, no format changes, append-only module edit) | **AUTHORED / SOFTWARE-TESTED 2026-07-10 (25 drafts, ALL status `iterating` — nothing self-accepted, nothing promoted; operator accepts at his desk).** Coverage per the brief: COMET t2/t3 +5 (collision burst, trio orbit, ripple-out, pingpong dual, quad split), NEUTRAL t2/t3 +5 (two nebula evolutions, soft ember sparkle, soft bloom, gradient weave — zero strobes per the taste anchor), WALL t3 +3 strobe-class (duo flip, slot-5 white punch, split strobe — all on the time-based `_hz_gate` law, 10 Hz/0.5-duty caps), HOUSE t2/t3 +4 (groove stab, dual offbeat, block groove, punch bounce), post_drop +4 pair-compatible tails (echo/remnant/decay/ripple), buildup +2 ramps, breakdown +2 (ocean swell, starfield). All SLOT kind (palette-fed; slot-5 white used deliberately only in wall_white_punch + the rare starfield accent), single-slot-per-pixel enforced, peak-normalized heads, time-based sparkles (AWR-153). Self-check 25/25 green (range, motion, determinism, segments==0, flip-rate ≤ house ceiling) + server reload + end-to-end preview render. Wave 2 (full phrase×family×energy matrix) stays BANKED until the spectral decision-layer refactor lands. Report `/tmp/rbss_lane_signals/looks.WAVE1.report.md`. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-201 | SOL2 launch-blocker fixes 2-5 — LED beat-feed grace, continuous-effect re-anchor, family/tier shuffle pools, and F2/F4 narrowing | `docs/plans/active/sol2_blockers_2_5_fix_spec_2026_07_10.md`; commits `ae6f410`, `9e73d6d`, `565c60d`, `32f1424`; contract key `led_govee` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-10; STAGED-ONLY.** Ten regression tests reproduce and pin all four SOL2 findings. Scoped LED/beat suites: 268/268 green. Full discover: 4142 tests, exactly the nine named pre-existing reds from the spec (2 export parity self-heal; patch_b tracked config; patch_c live smoke + tracked/live config; patch_d drop smoke + tracked/live config; SoundSwitch slot-16 golden; parity-oracle capture rows), zero new reds. No bridge/process contact, config write, restart, or hardware action. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-202 | LED Pad commit merge — fix the live config data-loss that fired 14:29 today (a pad Apply overwrote the operator's live lighting config with a stale draft, wiping `f2`/`f4`/`cfx_sweep`/`drop_presentation`/`scripted_mode`/`blank_role_hold`, 17 looks, and six `loop_beats`) (padfix lane) | `docs/plans/active/led_pad_config_merge_fix_spec_2026_07_10.md`; code `tools/led_pad_web.py` (auto-sync-swept `14e2170`); tests `tests/test_led_pad_service.py`; contract key `led_pad` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-10; STAGED-ONLY.** `LedPadService.commit()` is now read-modify-merge: it re-reads the live config fresh at Apply time, keeps every top-level block from LIVE (including blocks the pad has never heard of), and overlays only what this pad session actually changed — the touched/created looks, their role-bank placement, deletions, and the three per-look `color_engine` maps. Touched/deleted tracking lives in `_pad_meta.pad_session` (persisted with the draft so it survives a pad-server restart) and rebases to empty after each commit; the merged result (not the raw draft) is validated before any write, and the pre-write `.bak-*` backup stays. A stale draft can no longer clobber f2/f4 or looks added by other tools. Four regression tests added (unknown-block preservation, untouched-look preservation, deletion applies, stale-draft params survive); scoped `tests.test_led_pad_service tests.test_led_pad_lab tests.test_led_pad_controls` 74/74 green. **GATE FIX (2026-07-10, padgate lane, code+tests auto-sync-swept `ff2c098`):** the executive gate found the merge treated every `touched` look as a bank-placement change, but `save_look` marks a look touched on a params/color-only edit too — so if LIVE had moved that look to a different bank after the draft went stale, Apply removed the LIVE placement and replayed the stale draft placement. Fixed at root cause by tracking look CONTENT and role-bank PLACEMENT separately in `_pad_meta.pad_session`: `touched` (content: look body + the three `color_engine` maps), `moved` (bank placement), `deleted`. `save_look` marks content only; `move_look` marks placement only; `duplicate_look` marks both; `delete_look` clears both positive lists and records `deleted`. `_merged_commit_config` now keys looks/color-maps off `touched` and banks off `moved`/`deleted`, so a params-only edit preserves the LIVE bank while an explicit pad move still applies. `history_restore` now marks restored looks both `touched` and `moved` so a restore brings back the backup's look content AND bank placement (unmanaged live blocks and live-only looks still survive). Three regression tests added (params-only edit preserves live bank move; explicit pad move wins over live bank; history restore restores bank placement); scoped 77/77 green, and the pre-fix banks logic was shown to reproduce the reported clobber. Full discover: 4154 tests, the same 9 named pre-existing reds only, zero new reds. Hard checks (`check_docs_metadata`, `check_agent_contracts`, `check_docs_drift`) green. No bridge contact, no live config write, no manual restart or process command issued. NOTE: editing `tools/led_pad_web.py` tripped the AWR-193 freshness watchdog, which auto-restarted the :8766 pad server (idle + not pad-owned, by design) so :8766 now serves this new merge code; the fix still writes nothing to the operator's live config until the operator clicks Apply, and lighting behavior changes only after a bridge restart. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-203 | Intrinsic-hardness OFFLINE shadow descriptor — SOL3 three-path candidate (Increment A): pure module + read-only ablation evaluator, ZERO runtime importers, never routes live (sol46 hardness build lane) | plan `/tmp/rbss_lane_signals/sol46spectral_hardness_plan.HARDNESS_PLAN.report.md`; code `hardness_v0.py`, `tools/hardness_ablation.py`; tests `tests/test_hardness_v0.py` (28 tests); contract key `spectral_analysis` (extended) | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-11 (Opus 4.8/XHIGH, ULTRACODE, solo).** `hardness_v0.py` computes the frozen SOL3 candidate: four landed-sound terms (persistent body B, sustained high-band abrasion A, distorted-growl duty R, mid-high onset density N) over a first-8 + following-8 landed view averaged per term, a marker ±2 alignment search (Rekordbox marker stays the cue — the module never times/triggers a cue), a per-term-median track baseline over genuine drops, three transparent paths (`repeated_wall`/`abrasive_hammer`/`growling_hammer`), `H = max(paths)`, and a frozen `HardnessResult`/`TrackBaseline` exposing per-path values, winning path/offset, local/track terms, n_drops, baseline-stability, reducer, and marker-shift range. Selection-bias ablation is a call not a rewrite (`center`/`median`/`q75`/`max` reducers, deterministic tie-break). Pure: no I/O, no cache/DB/ANLZ access, reuses `spectral_profile.percentile` so Q25 stays bit-identical to the anchor calibration. **Named honestly:** `H>=1.0` is path-threshold firing, NOT a validated boundary; in-sample and binary-T3 only (no T1/T2 split). `tools/hardness_ablation.py` is read-only: resolves content_id → current filepath → current ANLZ beatgrid → `get_cached_v4`, and (defense-in-depth over the forward key) re-reads the current-key payload to assert its `audio_filepath`+`beatgrid_fingerprint` match — never enumerates raw cache JSON, never extracts/writes. Read-only run over 40 tracks: 36 scored / 206 drops, 0 identity rejects, T3 ≈ 18.9%, manufactured-T3 0.97%, boundary ±2 flip 6.25% — descriptive evidence only (no taste-accuracy/held-out/readiness claim; group bootstrap + accuracy scoring skipped: no structured gold). 28 unit tests green (term/window math + clipping, 8+8 boundaries, reducer/tie-break, path math + winning-path, track median + MIN_STABLE_DROPS, marker-shift range, malformed/empty/non-finite (NaN/±inf)/unshaped → no result never a phantom T3, and the AST-based zero-runtime-importer invariant). **Edge-fix lane (2026-07-11, Opus 4.8/XHIGH, ULTRACODE):** the shared `_valid_v4` gate now abstains (`None`, never a phantom T3) on a non-finite (NaN, +inf which else clip01-saturated to a false T3, −inf) or unshaped/non-numeric required series — a finiteness gap the AWR-203 9-lens review had missed; +1 regression test, frozen constants/formula parity/end-of-track clamping/path-reducer semantics untouched. Adversarially reviewed via a 9-lens ULTRACODE workflow (term/window math, arrival leakage, reducer semantics, cache identity, fail-safe, determinism, zero importers, SOL3 parity, docs honesty): ZERO module defects; two test-guard gaps fixed (AST importer detection replacing a line-anchored regex; reducer-ordering asserted on the composite align score not a coincidental single term). Three hard doc checks green; `git diff --check` clean. Bridge core OFF (0 `python3 -m rb_ss_bridge_v2` processes) before/after; no hardware/network/MIDI/DMX/laser/Govee/SoundSwitch/Codex contact. AWR-200 kept unchanged (candidate-planner hook deliberately NOT built — hardness is a binary shadow descriptor, not a production `DropDecision`). **Queued (operator taste, NOT decided here):** `repeated_wall`'s per-track stamp on soft drops; the T1/T2 boundary (does not exist, cannot be invented from this formula); tier-loss weighting; darkness bar-vs-beat loss; and any live threshold/routing — `violence`/`.tier` remain the sole live authority. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-204 | OFFLINE raw approach-descriptor layer — the SOL2 finding-1 measuring surface (pre-drop go-dark-vs-balloon-vs-stop read from the approach SHAPE over time, not a one-sample growl gate). Pure module only; ZERO runtime importers; decides nothing (sol46 spectral approach lane, finished by the sol46 approach-finish lane) | code `approach_features_v0.py`; tests `tests/test_approach_features_v0.py` (27 tests); reports `/tmp/rbss_lane_signals/sol46spectral_approach_features.APPROACH_FEATURES.report.md` + `/tmp/rbss_lane_signals/sol46spectral_approach_finish.APPROACH_FINISH.report.md` + `/tmp/rbss_lane_signals/sol46spectral_edgefix.EDGEFIX.report.md` + `/tmp/rbss_lane_signals/sol46spectral_sufficient_fix.SUFFICIENT_FIX.report.md`; contract key `spectral_analysis` (extended) | **EXPERIMENTAL / SOFTWARE-TESTED (OFFLINE) 2026-07-11 (Opus 4.8/XHIGH, ULTRACODE, solo).** `approach_features_v0.py` is one pure function `approach_features(v4, drop_beat, ...)` returning a frozen `ApproachFeatures` bundle over the four AWR-195 charter views of one drop: (a) track-wide quantiles + an across-`genuine_drops` first-landing reference; (b) an explicit current section (`section_start` XOR `section_len`, honestly unavailable when neither given); (c) the pre-drop approach (`approach_start` XOR `approach_len`); (d) `landed_first8`/`landed_next8` kept SEPARATE. Per window per series (`full/sub/growl_band/sustain_mid/sustain_high/perc_full`) a `SeriesStats` carries robust quantiles `p05..p95` (p05 is the robust floor — NOT `min`), `mean`, early/late half means + `delta`, an OLS `slope`/beat, and finite coverage. Plus `run_curves` (longest below-floor run in the approach at a reference view's own p10/p25/p50, precedence track→section→self; no musical floor baked in) with a `longest_run_below(values, floor)` primitive (a non-finite beat BREAKS the run); `depth_vs_track`/`depth_vs_section` = approach FLOOR (p10) minus the reference's TYPICAL level (p50), median-referenced so a long void does not contaminate it; and the approach + both landed bundles recomputed at drop ±2 for marker robustness (a length window slides, an explicit-start window moves only its end; `descriptor_range` gives (min,max) across offsets). Pure: no I/O, reuses `spectral_profile.percentile`, deterministic (repeated calls compare equal). Fail-safe: missing series → `present=False`; non-finite filtered before any percentile, an all-hole half → None never 0; short window (or an all-hole/single-finite window) → `sufficient=False`/`slope=None`; out-of-range clamped with `n_requested` vs `n_available`; empty track → `available=False` with a reason and NO exception; only inconsistent CALLER inputs raise `ValueError`. **DECIDES NOTHING** — no class, threshold, darkness length, or live authority; missing/short/non-finite data can never fabricate a darkness event; ZERO runtime importers (only `tools/`+`tests/` may import it; a static AST+text test enforces it). 27 unit tests green (trajectories, ringing-layer independence, depth floor semantics, section-relative depth, separate landed windows, track/drop-landing baselines, void run curve + primitive, ±2 offsets + `descriptor_range`, boundary clamp, short-window insufficiency, window `sufficient` requiring finite data not just beat slots, missing series, non-finite filtering, top-level availability requiring ≥1 finite approach descriptor, empty-track unavailable, caller-input raises, determinism, zero-runtime-importer). **Edge-fix lane (2026-07-11, Opus 4.8/XHIGH, ULTRACODE):** top-level `ApproachFeatures.available` now requires at least one finite approach descriptor (all-missing/all-non-finite → `available=False` with an honest reason; per-series partial availability unchanged), and the stale `ApproachFeatures` depth field comment was corrected to approach p10 vs reference p50 (matching `_depth`); +1 regression test, no measured-math change. **Sufficient-fix lane (2026-07-11, Opus 4.8/XHIGH, ULTRACODE):** the same-class hollow in the per-window flag — `WindowStats.sufficient` was `n_available >= 2` alone, so an all-non-finite window with ≥2 beat slots reported `sufficient=True` though no OLS trend is computable. It now requires ≥2 in-range beats AND ≥2 finite samples in at least one series, with a reason distinct from the too-few-beats case (`"insufficient finite data for trend"`); +1 regression test (all-non-finite → False, one-finite → False, two-finite-in-one-series → True), no measured-math change, per-series stats and top-level availability untouched. An earlier independent ULTRACODE review (window semantics, raw-only/hidden-threshold, finite-data, offset behavior, purity/determinism, test adequacy → refute-by-default verify) reported zero module-logic defects; a later executive review found two finiteness-blind honesty gaps (top-level availability, then `WindowStats.sufficient`) that the edge-fix and sufficient-fix lanes each closed with a regression test — so code and tests are no longer unchanged. Three hard doc checks green; `git diff --check` clean. Bridge core OFF (0 `python3 -m rb_ss_bridge_v2` processes) before/after; no hardware/network/MIDI/DMX/laser/Govee/SoundSwitch/Codex contact; no branch/worktree. AWR-199 / the one-sample gate / `lighting_moments_v2.py` / AWR-200 benchmark files / the local label file / the exec re-gate doc were NOT touched. **SOL2 finding-1 is NOT fixed** — this is only the raw measuring surface: **no classifier, no tool (`tools/approach_feature_report.py` deliberately NOT built), no live wiring, no hardware validation.** Queued operator-taste choices (NOT decided here): class boundaries, darkness lengths, the uncertain fall-back, marker-radius policy, rules-vs-model promotion, the ringing-layer cut boundary. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |

| AWR-205 | Spectral gold-label intake — the structured per-drop gold layer + strict loader for the AWR-200 Stage-1 ear benchmark (hybrid unit, operator ruling 2026-07-11). Offline tooling only; ZERO runtime behavior change | spec `docs/plans/active/spectral_gold_label_intake_spec_2026_07_11.md`; code `tools/spectral_ear_benchmark.py` (new `--emit-gold-template` / `--gold` modes + pure seams `enumerate_markers`/`build_gold_template`/`validate_gold`/`score_accuracy_axes`); tests `tests/test_spectral_ear_benchmark.py` (32 → 56); contract key `spectral_analysis` (extended) | **IMPLEMENTED / SOFTWARE-TESTED (OFFLINE) 2026-07-11 (Opus 4.8/XHIGH, ponytail).** Two new READ-ONLY modes on the ear-benchmark harness. `--emit-gold-template OUT.json` (requires `--resolve-db`) reuses the SAME resolved-marker enumeration the marker-sensitivity pass iterates and writes a deterministic, all-null, provenance-keyed **hybrid** template: one row per enumerated drop marker keyed by lineage/content_id/title/beat/mm:ss, each with `is_genuine_drop: null` and a nested `drop` field-set (tier/family/family_matches_track/darkness{shape,start_beat,end_beat,bars}/growl{start_beat,end_beat}|"none"/laser/confidence/notes) all null — filled ONLY on genuine drops; refuses to clobber a file that already carries labels. `--gold PATH` loads a filled file through a strict fail-closed validator: unknown/typo'd fields (positive allowlists mirroring the `call_planner` discipline), unmatched provenance keys (named), bad enums, and insane beat ranges are HARD errors listing every offending row; `is_genuine_drop: null` = unlabeled (excluded from scoring, counted in a `labeled/total` coverage line); reports `gold sha256[:16]`. Scoring flips an accuracy axis AVAILABLE only when BOTH ≥1 genuine-drop gold example exists AND a real scorer compares it to a per-marker model output verified in `DropDecision`. Only **tier** (int) and **family** (str) are cleanly exposed, so only they score this round; **darkness** (bar↔beat / shape-vocab / start-end alignment unverified), **growl** (no model span), **laser** (no model suitability), and the yes/no **drop_classification** layer (planner exposes no genuine-drop/blackout classifier) all stay UNAVAILABLE with a named blocker — fail toward UNAVAILABLE. Gold NEVER crosses `call_planner` (positive allowlist + the gold field names added to `LEAK_FORBIDDEN_FIELDS`; a leak test proves it). `is_partial` untouched — AWR-200 stays PARTIAL until every REQUIRED accuracy axis has gold + a scorer (tier+family available alone does NOT complete Stage 1). Real-DB read-only spot run: **21 lineages / 158 markers** enumerated (label sha `a584dcb1e0293b24` unchanged); a synthetic 3-row fill (scratchpad only, never committed) drove tier+family AVAILABLE end-to-end through the real `build_track_plan`. Template + filled gold live beside the AWR-182 label layer under `local/` (gitignored, never committed); AWR-182 file read-only (byte-identical). 56 ear-benchmark tests green (was 32; +21 gold cases, +3 review-fix cases); hardness (28) + approach (27) suites still green; three hard doc checks green. **Adversarial review (ultracode dynamic workflow, 47 agents, 3 loop-until-dry rounds, refute-by-default verify) 2026-07-11:** verdict leak-safe / spec-conformant / is_partial respected; it confirmed 2 real fail-closed holes (one root cause = Python `True==1`/`False==0` aliasing at the gold validation boundary): `tier: true` passed enum validation and scored as tier 1, and `is_genuine_drop: 1` was counted in coverage but silently dropped from scoring. Both fixed with explicit bool guards (tier + is_genuine_drop + family_matches_track) at validation plus a defense-in-depth scorer guard; 3 regression tests added; 7 other findings correctly rejected. Bridge core OFF (0 `python3 -m rb_ss_bridge_v2`) before/after; no runtime module edited, no bridge/network/hardware/Codex contact. **Filling the template later unlocks:** real per-drop tier/family accuracy scoring now, and the darkness/growl/laser/drop_classification axes once Stage-2 builds their scorers. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-206 | Laser pre-drop blackout relaxed arm gate — split blackout arming from the strict scene gate (drop the `autoloop_ready` sub-condition for the blackout note only) so the 4-beat pre-drop blackout survives autoloop re-arm churn during real mixing; INFO skip-reason logging (operator GO 2026-07-11; triage: 30 arm intents / 0 sends in the live 13:23 session, 3/3 in the idle 13:19 session, gate proven at `laser_executor.py:586-594` + `state_manager.py:5204-5210`) | spec `docs/plans/active/laser_blackout_gate_fix_spec_2026_07_11.md`; review `docs/research/sol_awr206_review_2026_07_11.md`; code `laser_models.py` + `laser_director.py` + `laser_executor.py`; tests `tests/test_laser_executor.py`; contract key `laser` | **FIX-ROUND LANDED / SOFTWARE-TESTED 2026-07-11 (LZFIX2 lane).** Round 1 (impl lane) FAILED independent review: its relaxed arm lived in the executor's `auto_gate_blocked` branch, which is UNREACHABLE — the director returns idle at its `autoloop_ready` gate one layer above, so the branch never carried a live arm signal (0 sends on the real chain, proven by a pure production-path repro at HEAD). Fix round: the director tags its `autoloop_not_ready` idle decision `blackout_arm=True` (`laser_director.py` priority-7, no scene fabricated); the executor's idle path arms the manual blackout on `_passes_blackout_gates` (strict gate MINUS `autoloop_ready`), scene MIDI stays strict-gated shut, INFO skip throttled once per changed failing-condition tuple (arm is level-held at 200 Hz). TEST-FIRST: added `test_integrated_director_executor_arms_blackout_under_autoloop_churn` (real director→executor chain) proven to FAIL at pre-fix HEAD, then pass. Release paths re-audited incl. StateManager no-drop-decision safety net (`state_manager.py:4882-4893`). Scoped laser suites green (test_laser_executor 91, _lifecycle 10, _blackout_rewire 7, _director 180, _reset_wiring 6; test_smart_transitions 100). STAGED-ONLY: running bridge unaffected until the operator restart. HARDWARE-UNVALIDATED — the operator mix is the real gate. |
| AWR-207 | USB-export track resolution — local-twin match by BPM/duration + beatgrid when device UUID lookup misses; local ANLZ/PSSI payload handoff; lsof mis-match guard | spec `docs/plans/active/usb_track_resolution_spec_2026_07_11.md`; code `filepath_resolver.py`, `state_manager.py`; tests `tests/test_filepath_resolver_usb_twin.py`, `tests/test_filepath_resolver_beatgrid.py`, `tests/test_filepath_resolver_hotcue_tags.py`, `tests/test_smart_transitions.py`; contract key `rekordbox_readers` | **IMPLEMENTED / SOFTWARE-TESTED 2026-07-11; STAGED-ONLY.** Local UUID lookup is unchanged. A device-export miss filters local DB rows by BPM ±0.05 and duration ±2 s, reads only those candidates' ANLZ files, and accepts exactly one beatgrid match (count ±1; first + 17 sampled times within 10 ms; exact fingerprint fast path). The payload carries the local identity, local grid, SoundSwitch ID, laser tags, and optional `local_anlz_path`; StateManager prefers that path for the resolved-time phrase/spectral/F2 worker because USB exports carry no PSSI. Zero/ambiguous matches remain unresolved; device misses skip lsof. The payload field is the extensible AWR-208 source seam; no sidecar/foreign-laptop logic is built here. Running bridge untouched; activates only at operator restart. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-208 | USB lighting PORTABILITY program — stick must light correctly on FOREIGN laptops with no local collection (Rekordbox exports no PSSI → lighting data must travel on the stick: build-time sidecar via make_stick + runtime fallback chain behind the AWR-207 seam). Operator mandate 2026-07-11: careful planning, implementation, and review loops — five gated stages D1..D5 | charter `docs/plans/active/usb_lighting_portability_program_2026_07_11.md`; builds on AWR-186 M2 make_stick + AWR-207 seam; E2 validation depends on the pending foreign-Mac frozen-app operator gates | **CHARTER AUTHORED 2026-07-11 (executive). Design-only; D1 independent design review dispatches post quota reset 2026-07-15. NO implementation authorized.** |
| AWR-195.S2 | Spectral Stage-2 DESIGN (GPT 5.6 SOL/xhigh one-shot, executive-gated 2026-07-11): shadow `Stage2ShadowDecision` bundle contract, per-axis exposure + grading semantics (IoU/±1-beat spans, exact enums), two-head rules-first finding-1 classifier w/ nested grouped LOLO + synthetic one-low-growl-sample pin, 7 staged falsifiable gates; design only, zero implementation | design `docs/plans/active/spectral_stage2_design_2026_07_11.md` (commit `5955e82`); handoff `docs/prompts/active/sol_stage2_design_oneshot_2026_07_11.md`; parent program AWR-195 | **DESIGN DELIVERED 2026-07-11; implementation NOT authorized — gate 1 (contract normalization in AWR-205) is the next implementable round, pending executive dispatch after the operator gold labeling lands.** |

**Closed 2026-06-29:** AWR-101 (LED color engine M2.5), AWR-102 (LED color engine core), AWR-103 (realtime comet), AWR-104 (beat-sync runtime) — operator hardware sign-off on Home Govee. Software work landed (specs in `docs/plans/completed/`); evidence in `docs/validation/hardware_validation_log.md`. AWR-114 (Graphify repo-map integration; renumbered 2026-07-03 — the prior ID was accidentally reused for two workstreams) — local Graphify CLI installed as `graphifyy` 0.9.2 via `pipx`; repo graph is code-only (`258` manifest entries, including root code, `scripts/`, `streamdeck/`, `tests/`, `tools/`, and 3 example JSON configs) with `graphify-out/` gitignored. Hook decision remains manual query only: no PreToolUse/read-interception hooks and no post-commit graph hook. Current workflow is `docs/setup/graphify.md`; completed install prompt moved to `docs/prompts/completed/graphify_install_prompt.md`. No bridge restart, live process, config, device, output, or hardware action was authorized or performed. AWR-105 and AWR-106 remain active (software-done, hardware-pending).

**Closed 2026-07-02:** AWR-112 (SoundSwitch DMX cue parity) — RESOLVED; cue association root-caused (exact-key resolution + precede-associated venue values, `5bb3a5b`) and autoloop divergence root-caused to the window anchor (selection-beat anchor restored). DD42028C remains a negative-control witness. Specs: `docs/plans/completed/soundswitch/soundswitch_pack_parity_root_cause_spec.md`, `.../soundswitch_pack_render_defect.md`, `.../soundswitch_dmx_cue_mismatch_spec.md`; RE evidence: `docs/research/soundswitch/soundswitch_perfect_parity_ghidra_evidence.md`.

## Documentation system follow-ups

| ID | Area | Status | Next action |
| --- | --- | --- | --- |
| AWR-001 | Old-doc classification + archive pass | done | `docs/architecture/doc_index.md` classifies all docs. 2026-06-29 sweep: completed SoundSwitch RE-edgecase + publish-sidecar specs → `docs/plans/completed/soundswitch/`, chorus drop-lifecycle revision → `docs/plans/completed/`, RE-edgecase findings → `docs/research/soundswitch/`, deferred laser SM-net spec → `docs/archive/plans/`; spent review/handoff prompts deleted (Git history preserves them). |
| AWR-002 | Untracked govee spec | done | Verified 2026-07-03: file has been tracked since `50f469e`; the registry/index 'untracked' claims were stale and are fixed. |
| AWR-003 | Stray root output files | done | Relocated `cues_output.md` / `cues_timing_output.md` to `docs/data/` (2026-06-29). |
| AWR-004 | First agent-workflow dry run | open — standing follow-up; not yet exercised as of 2026-07-03 despite qualifying feature PRs | Use this system on the next real feature change on `main`; record missing routes, unclear contracts, token-waste points, or hidden branch-state risks. |
| AWR-005 | Runtime command test inventory | open — standing follow-up; not yet exercised as of 2026-07-03 despite qualifying feature PRs | Map parser/handler tests and add missing ones only in a separate code/test PR. |
| AWR-006 | Contract coverage audit | open — standing follow-up; not yet exercised as of 2026-07-03 despite qualifying feature PRs | After a feature PR, tighten `docs/agents/change_contracts.yml` around any files agents had to rediscover. |
| AWR-007 | Active-doc lifecycle check (orphan + stale) | done | Implemented in `tools/check_agent_contracts.py`: orphan coverage (Codex, `574247d`/`1420aad`) + stale-classified flag (direct, 2026-06-29). Record at `docs/plans/completed/docs_orphan_check_spec.md`; tests in `tests/test_docs_orphan_check.py`. Resolved the one live stale instance by deleting the superseded rekordbox continuation prompt (Git preserves). |
| AWR-115 | Agent lessons store | done | Seeded 2026-07-03 with 7 evidence-cited lessons (`docs/agents/lessons/`); add new lessons only with commit-cited evidence. |
| AWR-169 | `test_soundswitch_pack` byte-identity tests race concurrent commits | open — hygiene backlog, not urgent (manager finding at the F2 gate, executive-approved 2026-07-09) | The pack manifest embeds `git rev-parse HEAD` at export time (`tools/export_soundswitch_pack.py:175` → `soundswitch_pack.py:597`); `CurrentProjectPackTests` compares a `setUpClass`-time export against test-time exports, so any commit landing mid-suite fails `test_two_exports_are_byte_identical` + `test_publish_pack_first_replace_and_repeat_match_fresh_export` exactly once (3/3 green in isolation; fired at the executive desk during the 2026-07-09 overnight commit storm). Fix whenever convenient: mock `_generator_commit` in that test class (or export the reference pack per-test). Not a product defect; does not gate anything. |
| AWR-158 | Flaky laser decision-log timing tests under machine load | open — hygiene backlog, not urgent (executive finding 2026-07-08 night) | `test_fire_enqueues_to_a_background_writer_not_the_tick_path` and `test_arm_fire_learn_disarm_round_trip_via_real_event_dispatch` flickered across two back-to-back runs under live-mix + build load; untouched by any 2026-07-08 round. Small load-tolerance fix (relax timing assumptions or fake the clock), whenever convenient. **AWR-159 addendum (2026-07-09):** traced a concrete, reproducible contributor while debugging these two during that round — `local/state/laser_solo_learned.json` on disk already contains `content-1:64` (predates this session), and neither test redirects `LearnedStore`'s in-memory entries the way `PlanAndTickIntegrationTests._sm_with_plan` does; a real solo firing at beat 64 then makes `LearnedStore.record()` a no-op (nearest-within-tolerance already matches), which failed deterministically (5/5) in this session's testing, not intermittently. Confirmed via a clean-environment re-run (no `local/state/`) with the SAME code: both pass. Likely the actual "timing" component is really state accumulating across repeated local runs (the background writer legitimately persists new beats to that real file every time these tests fire a solo that isn't already recorded) rather than a race — worth folding into whatever fix lands here: redirect the learned-store path/entries the way the other `PlanAndTickIntegrationTests` tests already do, rather than tuning timing assumptions. |

## Future roadmap

| ID | Area | Goal |
| --- | --- | --- |
| ROAD-001 | Rekordbox compatibility | Validate and document supported/unsupported versions with evidence. |
| ROAD-002 | OS compatibility | Decide whether non-macOS support is out of scope or requires new reader architecture. |
| ROAD-003 | Hardware validation | Create repeatable validation logs for SoundSwitch, laser, and Govee paths. |
| ROAD-004 | Usability | Improve local setup, config UX, status/debug visibility, and operator workflows. |

## Ideas / experiments

| ID | Idea | Promotion criteria |
| --- | --- | --- |
| IDEA-001 | Broader Govee device support | Device-specific tests, config examples, and validation logs. |
| IDEA-002 | More lighting outputs | Explicit subsystem design and software tests before support claims. |
| IDEA-003 | Deeper docs drift checking | Add code-aware checks only when they are cheap, deterministic, and not dependent on local hardware. |

## Deprecated plans

Deprecated plans belong in `docs/architecture/doc_index.md` or `docs/archive/` with reasons. They must not be revived without code verification.

## Hardware validation tasks

| ID | Device/path | Required proof | Status |
| --- | --- | --- | --- |
| HW-001 | SoundSwitch OS2L/local direct-DMX setup | version, interface, track, expected behavior, result, date | reviewed procedure/template exist; no run logged |
| HW-002 | Laser MIDI path | fixture/mapping, blackout behavior, manual override, safety notes | not logged |
| HW-003 | Govee realtime path | device model, firmware/app assumptions, effect, expected behavior, result | not logged |
| HW-004 | Govee/LED phrase-aware active-content hold | deck switch and active-deck track load landing > `1.0` beat into phrase hold the previous look until the next phrase entry when phrase data exists; missing phrase data releases by 16 beats / 8 seconds; <= `1.0` beat changes immediately; verify `[RGB] hold-engaged` / `[RGB] hold-released` and `[SP] reset-reason-change` lines line up with or rule out live freeze windows; verify no laser/SoundSwitch behavior changes | not logged |

## Compatibility expansion tasks

| ID | Target | Required proof | Status |
| --- | --- | --- | --- |
| COMP-001 | Rekordbox versions beyond my current setup | reader/offset validation plus tests/logs | unknown |
| COMP-002 | macOS versions beyond my current setup | launch/read/status validation | unknown |
| COMP-003 | Windows/Linux | architecture decision, not docs optimism | unsupported/unknown |

| AWR-195 | Spectral v4 decision-layer refactor PROGRAM (SOL-review-derived charter; ear-benchmark first; Fable/MAX owner seat pending) | `docs/plans/active/spectral_v4_refactor_program_2026_07_10.md`; provenance `docs/research/sol_spectral_review_2026_07_09.md` | **CHARTER AUTHORED 2026-07-10 00:3x (superman4)** — SOL one-shot design review at 00:35, then the dedicated owner seat dispatches; wave-2 look authoring keys off its axes |
| AWR-196 | ACCURATE LED render & simulation — near-real-life sim of the H612D perimeter strip (60 segs, 15 m rectangular-room loop): room-geometry view with SELF-CALIBRATING draggable corners/start/direction (operator ruling: NO room photos), photometric model (gamma/white-point/diffusion/bleed/indirect wash), temporal model (fps cadence/latency knobs), data-true input via the real `govee_frame_renderer` registry (lab drafts, production looks, frames-JSONL replay), persisted device profile + calibration procedure (ledsim lane manager, Fable/XHIGH, operator ~23:50 order; superman4 dispatch) | charter `docs/prompts/active/ledsim_charter_2026_07_10.md`; spec `docs/plans/active/led_sim_build_spec_2026_07_10.md`; code `tools/led_sim_web.py`, `tools/led_sim_engine.py`, `tools/led_sim_assets/**`; tests `tests.test_led_sim_engine`, `tests.test_led_sim_service`; contract key `led_sim` (new) | **BUILD LANDED 2026-07-10 ~01:10 (ledsimb lane) — all 6 spec tasks implemented + software-tested: engine (geometry/profile/bleed/adapter/codec/cards/CLI), server on 127.0.0.1:8767, room view + self-calibrating UI (4 assets), 24 led_sim tests green, 3 hard doc checks green; curl smoke on :8767 passed; full-suite reconcile vs five-red baseline in the lane report (`/tmp/rbss_lane_signals/ledsimb.A196.report.md`). Commit trail: T1 `3e93cd9`, T2 content in `60485f7` + T4 content in `8376165` (auto-sync Stop hook swept both before the named commits could land; contents verified byte-identical to the lane's final files), T3 `f6778e1`, T5 `138a2d2`, T6 = this row's commit. MANAGER ADVERSARIAL REVIEW PASS 2026-07-10 ~01:2x (ledsim seat, at its own desk): 24 scoped tests re-run green; per-commit file lists re-derived (fence held, T2/T4 sweep contents = HEAD, tree clean); forbidden-import sweep clean (loopback :8767 hardcoded); engine+server read line-by-line; adapter parity with render_preview_frames re-confirmed at HEAD by independent read; independent curl smoke (catalog 62 effects/62 looks/49 drafts, render 120×60, caps, profile roundtrip, replay fence, clean kill, zero stray processes); 3 hard checks green; extra suite red = known HEAD-embedding pack flapper, correctly isolated ×3 green per §4 doctrine. Two nits noted (no fix round): corner-straddling segments draw as chords (≤4 segs, calibration axes unaffected); unknown lab name fails dark w/ 200 (renderer's documented contract, picker-only UI). Awaiting executive gate; operator calibration pass against the real room = the only accuracy source. operator calibration pass against the real room = the only accuracy source.** FENCE held: zero pad-lane file edits; sim is offline tooling: NEVER sends to the device, no transport imports, no bridge-runtime edits. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-197 | Speed/size law codification — explicit beat-speed params on realtime looks + dead-pair cleanup (speedlaw lane, resumed by the executive seat) | spec `docs/plans/active/awr197_speed_law_finish_spec_2026_07_10.md`; law `tools/apply_speed_size_law.py` + `config/led_look_director.example.json` (via `4008de2`); tests `tests/test_speed_size_law.py` (`test_example_config_carries_the_speed_law`, `test_example_config_has_no_dead_pairs`, `test_apply_is_idempotent`, `test_apply_never_clobbers_existing`, `test_apply_aborts_on_missing_look`) + patch_b `test_tracked_config_validates`; contract key `led_govee` | **STAGED / SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED 2026-07-10.** Law codified in the tracked example config and idempotent set-if-missing apply script; tripwires and patch_b literal re-pin landed. The live apply was verified taken overnight, then WIPED by the 14:29 LED-Pad clobber incident. Restored 2026-07-11 (RESTORE lane) from the 14:29 pre-clobber backup, which already carried the law: re-running `tools/apply_speed_size_law.py` against the restored live config reported "already applied — no changes written" (idempotent, zero writes). The law is present on the live config on disk; it takes effect at the operator's next menubar bridge restart (no hot-reload). |
| AWR-199 | DAY-0 interim guard — deep-sub-void pickup abort (SOL2-confirmed darkness-pickup hazard; spectral owner seat, Fable/MAX; build lane `spectralb` Fable/XHIGH) | spec `docs/plans/active/spectral_day0_pickup_abort_spec_2026_07_10.md`; code `lighting_moments_v2.py` rung 0b; tests `tests.test_lighting_moments_v2`; contract key `led_govee` | **BUILT / software-tested 2026-07-10 ~01:4x** (`9757955` = rung-0b abort + 8 tests; first slice auto-synced mid-edit in `b2dab18`). Spec implemented exactly: rung 0b carries `abort_at` = first returned beat when the void ended >=3 beats before the drop with the sub back throughout; gap-0/1/2 operator-verdicted shapes stay dark; `growl_tail` observability field added; kill-switch `RBSS_F2_VOID_PICKUP_ABORT=0` restores always-None. Module suite 63/63 green (was 55); discover reconciled BY NAME: the five env reds exactly, plus 2 reds owned by the parallel AWR-187-lane `loop_beats` example-config edit (`4008de2`), not this change (`test_tracked_config_validates`, `test_tracked_and_live_configs_validate`). Utopia b192/b384 pins re-pinned from measured real-cache values, abort stays None. Desk-verified at d1ad03b: SOL2 repro reproduced via pure planner; guard boundary operator-verdicted (gap-0/1/2 pickups approved in Utopia/TOXIC/OMG pins → only >=3-beat returned-music runs release); library sweep 706 tracks/93 rung-0b firings = ZERO engagements today (prophylactic; ±1-beat marker moves can create the hazard cell). Growl tail-gate REJECTED on measurement (52/93 flips incl. OMG pin break). STAGED-ONLY (running bridge keeps old behavior until the operator's morning restart). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| AWR-200 | Stage-1 EAR BENCHMARK — runnable harness over the existing v4 cache + AWR-182 labels; grouped holdouts + anti-leak; the gate every stage-2 round must beat (spectral owner seat AWR-195 stage 1) | spec `docs/plans/active/spectral_ear_benchmark_spec_2026_07_10.md`; tool `tools/spectral_ear_benchmark.py`; tests `tests/test_spectral_ear_benchmark.py`; frozen run `docs/research/spectral_ear_benchmark_stage1_report_2026_07_10.md`; contract key `spectral_analysis` (extended) | **PARTIAL — BUILT / SOFTWARE-TESTED 2026-07-10 (spectral owner seat, Opus 4.8/XHIGH, solo).** Read-only offline harness (no runtime lighting change, no extraction, no cache/config writes; `--resolve-db` opens Rekordbox master.db + the v4 cache READ-ONLY). It loads the AWR-182 41-row machine layer (label sha256[:16] `a584dcb1e0293b24` after the 2026-07-11 content_id enrichment; was `bda63553da99cb9e`), validates/normalizes WITHOUT mutation, applies explicit cited exclusions (scripted=BLACKPINK JUMP; unusable_grid=Toxic Britney; variable_bpm=s.o.s; marker_blocked_pending_remap=OCHO+Latch; REWIND kept per charter §D), groups whole track/remix lineages, and emits a deterministic coverage manifest + grouped leave-one-lineage-out fold inventory (26 lineages, 21 usable / 5 excluded, 31 usable entries; Utopia's 10 entries never split). It imports/calls `lighting_moments_v2.build_track_plan` for the one axis needing no operator gold — marker sensitivity — resolving each track through its current library filepath + beatgrid fingerprint (never iterating raw cache files). **Why PARTIAL, honestly:** (1) the AWR-182 labels are the operator's FREE-TEXT verdicts — no structured per-drop gold — so every accuracy axis (tier/family/darkness/growl/laser) is reported UNAVAILABLE, never zero/PASS; blocked on a curation pass. (2) As of the 2026-07-11 coverage pass the marker axis resolves the FULL usable corpus — all 21 usable lineages now carry a proven Rekordbox content_id (Sexy/Utopia always did; the 19 B-rows were mechanically enriched from the read-only resolution lane's uniqueness-swept IDs), so it measures 21/21 lineages / 158 markers. Still NOT like-for-like with SOL's 15-track/113-marker sample (different track/marker set, possibly different roll-up) — do not compare the percentages. **Completion gate:** a single shared `is_partial` predicate over accuracy-axis availability decides PARTIAL/complete; marker-sensitivity availability is deliberately excluded, so a marker pilot can NEVER flip AWR-200 to complete (executive gate fix — the earlier build derived status from marker availability and wrongly printed `baseline-complete` under `--resolve-db`). **Trust-repair pass 2026-07-11 (Opus 4.8/XHIGH, solo):** the single `call_planner` boundary now guards EVERY base and perturbed planner call (positive allowlist + `assert_no_leak`), not a one-off hand-built dict; amendment grouping follows the `amends` parent link (the hard no-content_id/different-track case rides with its parent) and warns loudly on missing/cyclic/ambiguous links and on a duplicated amendment own-id that would else silently corrupt a primary's lineage; marker availability keys off markers actually SCORED (a resolved track that scores zero markers reads UNAVAILABLE); markers with no comparable perturbation at a radius leave that radius's denominator (per-radius `comparable_pm1/pm2` reported); the tautological `build_folds` assertion is replaced by the real train/test entry-id disjointness invariant; and same-title/no-content_id identity collisions surface a deterministic warning/limitation (identity not guessed — curation work). Trust-repair pilot numbers (2-track/16-marker, since superseded by the coverage pass below) were ±1 family 25.0/tier 6.2/darkness 0.0, ±2 43.8/12.5/18.8. The repair itself was adversarially self-reviewed (8-lens Opus 4.8/XHIGH panel); it surfaced one real latent hole — a duplicated amendment own-id could silently corrupt a primary's lineage via the by-id rewrite — now closed with an own-id guard + regression test. 32 unit tests green (anti-leak boundary, amendment-via-parent incl. duplicate-id guard, grouping, exclusions, availability incl. markers-scored-AND-comparable gate, per-radius denominators, identity warnings, determinism, flip counting, and the completion-gate regression: marker AVAILABLE + accuracy axes unavailable ⇒ status/return stay PARTIAL). LOLO fold inventory = 21 folds (one per usable lineage). 3 hard doc checks green. **Coverage + marker-availability pass 2026-07-11 (Opus 4.8/XHIGH ULTRACODE, lane `sol46spectral_coverage`, solo + adversarial self-review):** (a) marker-sensitivity availability now requires markers scored AND at least one comparable ±1/±2 perturbation — a resolved track with baseline decisions but zero comparable perturbations (both flip rates None) reads UNAVAILABLE, not a hollow AVAILABLE (root-cause fix in `run()`/`axis_availability`; new regression test); (b) the gitignored label layer was mechanically enriched with 19 proven usable content_ids + OCHO/Latch recorded-but-still-excluded (purely additive, verified byte-for-byte; new sha `a584dcb1e0293b24`); (c) real `--resolve-db` run over current DB + current strict v4 cache resolved **21/21** usable lineages / **158** markers (0 skipped, 158 comparable at each radius): ±1 family 11.4/tier 22.8/darkness 24.7, ±2 family 23.4/tier 39.2/darkness 51.3 (NOT compared to SOL's 15/113); bridge core verified 0 processes before/after. STAGE-1 EXIT is blocked on the structured-gold curation pass + the OCHO/Latch ear decisions (blackout lengths / drop-vs-buildup + tier). Content_ids on the B-rows are DONE; the growl-centroid values are present, aligned, non-NaN, and real-valued in the strict v4 cache — **no backfill remains**, only Stage-2 *musical* validation of those values. AWR-200 remains **PARTIAL** (content_ids/marker pilot never flip the `is_partial` accuracy-axis gate). SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
