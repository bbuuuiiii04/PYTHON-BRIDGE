---
doc_status: current
truth_level: code-and-config-grounded
last_verified_commit: b629b93
last_verified_date: 2026-07-13
validation_scope: software-validated only plus Rekordbox 7.2.11 passive mixer RE evidence routing; Rekordbox 7.2.16 direct-reader offset row software-tested (four roots STATIC-CONFIRMED; interior hops CANDIDATE from 7.2.14 deck + 7.2.11 mixer/CFX layout; live-unvalidated); AWR-157/AWR-160/AWR-161 software-tested; AWR-186 patch/builder/install hardening including the immediate grey menubar initial-check repaint, AppleDouble pruning, final mounted-DMG installer validation, and AWR-207 split-local-UUID classification software-tested, no hardware action; AWR-222 dormant AX measurement probe implemented/software-tested/not executed; hardware-unvalidated in repo evidence
---

# Validation Matrix

This page separates software tests from hardware validation. They are not the same thing, because reality remains rude.

Current repo-facing status remains:

> **SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED**

## Validation levels

| Level | Meaning |
| --- | --- |
| software-tested | Unit/integration/replay/static tests exist. |
| docs-checkable | Lightweight repo/docs checks can verify routing, metadata, and selected code/doc consistency. |
| operator-local | I have run it locally, but the repo does not contain repeatable validation evidence. |
| hardware-validated | A repeatable hardware validation log exists in `docs/validation/hardware_validation_log.md`. |
| unvalidated | No useful evidence yet. |
| unknown | Not inspected or not enough evidence. |

## Current validation summary

| Area | Software validation | Hardware validation | Notes |
| --- | --- | --- | --- |
| Core runtime/event/state | partial | unvalidated | Tests exist across state snapshots and runtime behavior, but hardware is separate. |
| Runtime command docs | docs-checkable | not applicable | `tools/check_docs_drift.py` checks command docs against `runtime_status.py`. |
| Runtime `[BEAT]` heartbeat | partial | unvalidated | `tests/test_runtime_status.py` covers heartbeat payload/log assembly and suppression, show-deck versus non-stale Rekordbox-master separation, stale-master suppression, idle deck-0 safety, StateManager-published color-engine status, fail-soft provider handling, and throttled provider-failure warnings; no hardware behavior changes are implied. |
| Runtime audit P1 cleanup | software-tested | unvalidated | `tests/test_runtime_status.py` covers smart-drop/breakdown callback failure surfacing. Compile/docs checks cover the confirmed dead-code and stale-text cleanup. |
| Logging visibility (AWR-125 overhaul) | software-tested | unvalidated | `tests/test_bridge_log.py` (45 tests) covers the JSONL record builder, queue handler, writer thread, redaction, `resolve_log_dir`/`prune_runs`, `event_scope`/`stamp_trace`, and init/shutdown lifecycle. `tests/test_bridge_view.py` (96 tests) covers the pure viewer layer: `parse_record`, `lens_of` (including the legacy-infra DEBUG-routes-to-DEBUG-only case), `format_line`, `format_age`, filter parsing, and `LatchState`. `tests/test_logging_surface.py` (7 tests) covers error-always-visible, `BRIDGE_LOG_LEVELS` parsing, and `BRIDGE_DEBUG` behavior against `bridge_log` (replaces the retired `tests/test_logging_diag_coverage.py`). `tests/test_bridge_log_integration.py` (9 tests) proves a real subprocess init→emit→shutdown round trip through the viewer's lens layer. `tests/test_bridge_fmt_rate.py` continues to cover `log_changed()`/`log_throttled()` spam-control primitives. Emit-assertion coverage for the new `perf.*`/`health.*` categories was added across the owning subsystem's existing tests (laser, LED, autoloop/SS/scripted, deck/drop/override/heartbeat, OS2L/MIDI/DMX/Govee/RB health transitions, thread liveness) rather than in the logging test files themselves — see `docs/validation/software_test_inventory.md`. No hardware-visible behavior is validated; this proves the log pipeline and viewer only. |
| Agent routing/contracts | docs-checkable | not applicable | `tools/check_agent_contracts.py` checks key routes and referenced files. `tests/test_docs_orphan_check.py` covers active-doc classification, and `tests/test_check_docs_staleness.py` covers advisory staleness contract parsing, glob expansion, and implementation-file filtering. |
| Rekordbox readers | partial; 7.2.16 offset row software-tested | unvalidated / live-unvalidated for 7.2.16 | Reader correctness depends on live app/version/permissions. `tests/test_rb_state_reader.py` covers transient ANLZ read failures preserving the last successful cache entry and later recovery emission, plus (AWR-160) the phantom track-load stability gate: a churning browse storm emits nothing and logs phantom-suppressed/phantom-storm, a stable load emits exactly one ANLZ_PATH+TRACK_LOADED pair in order after 3 ticks, a load that never plays still emits (the FEIN case), a title that changes again before stabilizing only lets the later one emit, unload after a stable load behaves as before, deck 1/2 gate symmetrically, and a stable load never re-emits. AWR-234 covers attach retry: fail twice then attach+tick, mid-run pid-gone detach+reattach, `stop()` during the waiting phase exits promptly, and status merge `waiting_for_rekordbox` when the event reader is unattached (memory reason still wins). `tests/test_rb_memory_scans.py` (AWR-148) proves the deck-2 scan filters are byte-identical across the numpy fast path, the pure fallback, and an old-loop oracle (software-only; no mach/live process). `tests/test_rb_memory_chain.py` (`ChainFreshnessTests`, AWR-157) proves the deck-2 chain-freshness gate: exactly-5-identical-reads staleness threshold, always-fresh on advancing raw, always-healthy while paused (the FEIN case), fallback engagement on staleness, deck 1 provably untouched, and both new log lines edge-triggered — pure fake-memory seams, no mach/live process. RB7216: `tests/test_rb_offsets.py` pins exact 7.2.16 master/deck/mixer/CFX chains and keeps the 7.2.11 literal regression; four roots STATIC-CONFIRMED; interior hops CANDIDATE from 7.2.14 deck + 7.2.11 mixer/CFX; not cleared for live use. |
| USB local-twin + foreign-import resolution (AWR-207/AWR-209) | software-tested; staged | room-visible/hardware unvalidated | `tests/test_filepath_resolver_usb_twin.py` covers exact mirror identity, the red-team 11 ms collision abstention, minimal DeviceSQL parsing, copied/referenced local imports, PDB-assisted cross-analysis, conflicts/misses, and the path-only split-local-UUID case versus `/Volumes`. `tests/test_smart_transitions.py` proves that exact local layout starts both early and resolved ANLZ workers while real USB keeps the resolved-time-only rule. The mounted MINK PDB parser evidence is unchanged; no restart, room-visible, or foreign-laptop claim. |
| Portable lighting sidecar runtime source (AWR-211) | software-tested; staged | foreign-laptop/two-USB/room-visible unvalidated | Focused lane: 257 OK. Tests cover schema/path validation, revalidated mounted+installed discovery, rebuild/unplug/replug, identical-root deduplication, conflicting-generation ambiguity, collision/tag rules, payload SSID/laser/v4 parity, local-first fallback, and the smart-rearm phrase worker with spectral and LED-v2 off. The read-only desk harness forced no DB and resolved real MINK `000086A0` to content `39964930`, sidecar ANLZ, and v4. No bridge restart or output action; AWR-222 stock foreign-Mac attach remains live-unvalidated / unknown. |
| USB DMG package/publication (AWR-186/AWR-226) | software-tested plus local mounted-image validation | physical foreign-Mac install unvalidated | Unit tests pin AppleDouble/`.DS_Store` pruning before package-manifest generation, exclusion of mutable transport companions from the outer FAT publication manifest, and final mounted-DMG deep-signature plus native installer validation before publication. The real builder gate mounts the produced DMG read-only and runs `install_controller._validate_package()` against its exact file list and hashes. This proves the local artifact, not Gatekeeper/TCC behavior or install success on a friend's Mac. |
| Rekordbox Accessibility MEASUREMENT probe (AWR-222) | software-tested; not executed | live AX/TCC/USB/foreign-Mac unvalidated; not a reader | `tests/test_usb_launcher_ax_probe.py` covers fake-facade dispatch, preflight refusals, permission gating, BFS caps, sanitizer canaries, file modes, tokens, sidecar abstention, process-wide lsof non-assignment, cadence unmeasured-without-reference, and static forbidden-symbol guards. No live AX tree, TCC, USB, or hardware action. Normal menu unchanged. Target-patch→stock-attach hypothesis reopened; next gate is ordered stock-SIP live attach evidence (AX E3 optional). |
| Rekordbox mixer active-deck authority | software-tested implementation plus static/passive RE proof for local Rekordbox 7.2.11 Deck 1/2 upfader and LOW/BASS chains; 7.2.16 mixer chains software-tested (live-unvalidated) | unvalidated | Unit/integration tests cover named offset parsing, finite mixer reads with `unreadable`/`non_finite`/`out_of_range` reasons, immutable mixer snapshots, resolver cases including neutral LOW/BASS tie handling and Deck 1/2-only candidates, StateManager bypass gates, direct-master refresh/invalidation, lost transport support, raw Deck C/D suppression/no-aliasing, default-on startup wiring, status/heartbeat stale-master separation, invalid/stale fallback, and idle deck-0 clear safety. RE proof confirms local Deck 1/2 channel ownership, upfader chains, LOW/BASS chains, EQ band 2 = LOW/BASS, CFX FILTER tracking chains, relaunch reacquire, and mixer-chain readability after labeled master-button actions. CFX FILTER is not active-deck authority. Direct-master byte authority is existing bridge code behavior, not a field proven by the mixer JSONL artifact. This does not prove SoundSwitch, laser, LED/Govee, DMX, MIDI, Enttec, bridge-output behavior, other Rekordbox versions, live loaded-track play/stop survival, or hardware-visible behavior. Resolver thresholds and stability timing are implementation policy only. 7.2.16 four roots STATIC-CONFIRMED; interior hops CANDIDATE from 7.2.14 deck + 7.2.11 mixer layout. |
| SoundSwitch OS2L | partial | unvalidated | OS2L output code exists; hardware/app validation log needed. Audit P2 adds default-off injector gating and pack-status `overlay_suppressed` software coverage only. Audit P3 removes the unused timing-compensation constant and covers explicit scripted elapsed threading in software tests. |
| CFX filter-sweep LED overlay (AWR-173) | software-tested | hardware-unvalidated; bloom threshold + ramps pending desk calibration | `tests/test_rb_offsets.py` covers the CFX 7.2.11 and 7.2.16 chain groups (exact chains), other-version inertness, and CFX/mixer group independence in both directions. `tests/test_rb_state_reader.py::CfxTickTests` covers valid readings, `wrong_effect`/`unit_channel_mismatch`/`non_finite`/`out_of_range`/`unreadable` reasons, per-deck independence, `Ev.CFX_STATE` never in `_authoritative_kinds`, and the isolation pin (broken CFX + healthy mixer ⇒ `MixerAuthoritySnapshot.valid == True`). `tests/test_led_cfx_sweep.py` covers the pure `cfx_sweep_envelope` (CCW/neutral exactness, flood/release ramps, continuous monotonic dim, dt=0/NaN safety, boundary continuity), dispatch gating (feature-off/blackout/F2-darkness/v2-off/stale/invalid/active-deck-0 all inert), the anchor provider (freewheel neutral, fresh attach, >0.5 s stale neutralized), the child overlay (`_parse_cfx_rgb`, wire round-trip, frozen-child skew, `scale(lerp(...))` parity, runner permitted-vs-emergency paths), and the config loader range validation. Software only; no bridge restart, process-memory sampling, or hardware output. 7.2.16 CFX is live-unvalidated. |
| SoundSwitch offline decoder/exporter/pack/verifier | software-tested | not applicable | Pinned to SoundSwitch 2.10.3 plus the canonical UUID/RAVE profile. Tests verify deterministic export, independent semantic verification, dynamic saved-project inventory reconciliation, proof-only strict snapshot rejection, required binding-sidecar publication/recovery, source sidecar ignored-path derivation/sanitization, the seven-class F-3 crosswalk, loader-superset runtime metadata rejections, report-only import diagnostics, and typed truncated-catalog decode failure. F9 and F10 proof seams remain covered. No project mutation or live output. |
| SoundSwitch scripted loader/player/MIDI/runtime/backend/Enttec | substantial partial software/wire-tested | unvalidated | Loader/player, input adapter (including SoundSwitch-saved Static Override Press/Toggle interaction mode and held-blackout release semantics), backend, sender, default-off config/startup, StateManager driver, commands, copied RW-5 status, and Art-Net truth-check gate have focused tests. Tests cover python-rtmidi `MidiIn`, static-controller auto-bind, alias override, missing/ambiguous input degradation, output-bus exclusion, provider-free reads, precedence, simultaneous degraded/scripted truth, fresh dict publication, lifecycle snapshots, stale menubar state, one-shot auto-enable retry, bounded render/submit failures, connected SoundSwitch shadow-render U1 with production software-zero, fake-only truth sink sidecar/sequence/overflow behavior, ordered sidecar matching across sequence wrap and denser U1 streams, normalized scripted timeline/rapid-pair coverage, Autoloop visible/authored-dark phase buckets based on each loop's cycle, static/blackout overlay-release coverage, active-deck/mode transitions, capture-derived scripted/Autoloop/Static Look parity registries, static fallback lane generalization, supported-layout-family scripted generalization, and 200 Hz loop skip-and-continue on ordinary errors with throttle preserved and no duplicate ZERO after `_push_tick()` inner failures. Fresh generated-pack export now has zero active `unverified_parity` documents and passes the trusted publication software gate. `tools/artnet_compare.py --self-check` covers synthetic parser/matcher/sidecar/timing/order/coverage verdicts without sockets or hardware. `software_zero_frame` and `frame_count` are software intent only; no physical output is validated. AWR-238 covers pack `pack_start_failed` supervisor retry + late-attach (`tests/test_soundswitch_pack_startup.py`) and SS-MIDI `InvalidPortError` rebind (`tests/test_soundswitch_midi_input.py`). |
| SoundSwitch native-DMX Autoloops/T7d | software-tested implementation plus historical evidence partial | unvalidated | Native resolver/player/loader/StateManager tests cover note-to-Autoloop binding, latching, phase, refire/re-anchor, missing binding/file/layout, all-zero dark looks, post-drop fallback, scripted/static/SoundSwitch-present precedence, stale reload clearing, and single submit path. Historical T7d tracer/conductor/oracle tests and captures remain evidence/tooling only; live runtime and hardware validation are still required. |
| Laser policy/executor | partial; lifecycle software-tested | unvalidated | Pure flat-window parity, smart-drop marker collapse, true-crossing impact gating, capped chorus-to-chorus second-hit re-arm, post-cap demotion, A3 phrase gating, A4 blackout arm/clear preservation, lifecycle teardown, autoloop-tick cycling, usable-only shuffle bags, static-impact fallback, send-error reopen recovery, AWR-238 `port_unavailable` reopen (same cooldown), high-impact/missing bank skip and restore behavior, blackout-mask refcounting, Laser Pad live-toggle command append, config fallback/cooldown validation, deprecated `pre_drop_scene` tolerance, and kill-switch-OFF behavior have deterministic tests. `tools/check_laser_midi_sync.py` reports 0 errors on the live config. Fixture validation must be recorded separately. |
| Laser color (menu/follow-LED + brightness floor + CH9=90) | software-tested | hardware-unvalidated | `tests/test_laser_color_engine.py` Part D covers the 10 menu cases: non-drop follow-solid (cyan/blue), drop-fires-chase, early white return (not the floor), rank-1 brightness filter, CH9 passthrough vs post-drop ease, fail-open (missing menu → legacy solid, invalid rgb → None, disabled → None), v2 solid pick, and v2 follow + engine-switch clear. `color_state()` purity is asserted across two reads. CH3/CH4 are never read or written; the chase CH8 values (172/68/100/164/72) await a supervised live eyeball because CH3/CH4 stay authored in the pack. |
| LED/Govee cloud | software-tested partial | unvalidated | Cloud path exists; device behavior must be logged. `tests/test_govee_runtime_sender.py` covers edge-triggered mirror target failure/recovery logging, primary return semantics, and `mirror_send_ok`; `tests/test_govee_scene_adapter.py` covers `circuit_open` degraded status clearing after a successful send. The pure lifecycle resolver does not replace live LED dispatch; `led_dispatch_policy.py` mirrors the same real-crossing drop-impact gate, capped chorus-to-chorus second hit, and post-cap demotion in the StateManager-owned LED path. Intra-section look rotation is software-tested for long buildup/pre-drop/breakdown/monotonic ambient role keys, stable section/cycle publication, unchanged drop/groove/post_drop keys, and a dispatch-path second look across a buildup cycle boundary. The active-content hold has a 16-beat / 8-second backstop plus hold/reset observability, but room-visible Govee behavior remains hardware-unvalidated. |
| LED dispatch policy extraction | software-tested | unvalidated | `tests/test_led_state_manager.py` and the full unittest suite cover the pure refactor that centralizes LED trigger/accept/reject bookkeeping and moves dispatch policy into `led_dispatch_policy.py`. This does not prove room-visible Govee behavior. |
| LED committed-drop eligibility | partial | unvalidated | `tests/test_led_look_director.py` covers `commit_role("drop", diy_eligible=...)` filtering like normal tick automation. |
| LED deterministic mixed-transport rotation (AWR-149) | software-tested | unvalidated | `tests/test_led_look_director.py` covers `plan_backend_sequence()` exact tuples (6RT+2cloud frozen spacing, 1+1 realtime-first, 3+3 realtime-leads-tie, single-backend, empty, order stability), seed-independent backend sequences over a 12-pick groove run, the no-latch regression (2RT+1cloud follows the plan cycle so cloud recurs at its planned slots — this fails on the old sticky code), plan-index-0 session phase for every seed, eligibility rebase to all-RT and back plus the C4 full-bank fallback, preview parity with the next unfiltered commit and zero cursor/bag/RNG mutation, and paired post_drop bypassing the plan without advancing any cursor. This proves the deterministic transport selection in software only; both-transports-per-role room-visible behavior remains hardware-unvalidated. |
| LED phrase-aware active-content hold | software-tested partial | unvalidated | `tests/test_led_state_manager.py` covers immediate release at `0.5` and `1.0` beats into phrase, hold at `1.1` beats until the next phrase marker, 16-beat release when phrase data is missing, 8-second release when no beat is readable, active-deck switch arming, active-deck track-load arming, inactive-deck exclusion, idle/stop cleanup, hold stamp cleanup, SmartPhrasing reset-reason change logging, automation-only `perf.led.look` beat/phrase enrichment, and no laser/SoundSwitch calls from the hold gate. This does not prove room-visible Govee behavior. |
| LED idle/pause ambient | software-tested | hardware-unvalidated | `tests/test_led_state_manager.py` covers no-audible idle entry dispatching one ambient decision from the last audible deck, realtime idle ambient freewheel anchors, blackout clearing the freewheel, playing automation returning to the normal realtime beat branch, and AWR-235: same-`role_key` repeat idle keeps the freewheel/playing anchor, gated blackout/manual-override idle passes still clear it, and a changed idle `role_key` re-dispatches and re-stamps the freewheel. `tests/test_govee_realtime_runner.py` covers idle-grace blackout-before-deactivate ordering. This does not prove the Govee firmware fallback explanation or room-visible pause behavior. |
| LED scripted-track automation policy | partial | unvalidated | `tests/test_led_config.py` covers the JSON blackout defaults and `utility` destination validation; `tests/test_led_state_manager.py` covers groove/drop/post-drop blackout mapping, active buildup/breakdown, opt-in overrides, and non-scripted identity behavior. This does not prove room-visible Govee behavior during scripted SoundSwitch tracks. |
| LED/Govee realtime | partial/experimental | unvalidated | Realtime path exists; slot-color strategy behavior, locked-palette resolution, Patch S `random_with_mono_chance`, generic groove/post_drop/drop slot cues, Patch E1 nebula slot cues, Patch E2 center-comet slot cue, Patch E3 ambient twinkle slot cue, Phase 3 renderer param unlocks, Patch F default-bank cleanup, runner-thread handoff teardown, and idle-grace blackout teardown have software tests, but a repeatable instrumented validation record is still pending. AWR-101–104 (M2.5 slot cues incl. Patch E1/E2/E3, color-engine core, realtime comet, beat-sync) have operator hardware sign-off — 2026-06-29, Home Govee, visual; see `docs/validation/hardware_validation_log.md`. The idle/pause ambient fix, solid-color strategy (Patch S), role-mapping v2 (AWR-105/106), Patch D sparkle, Locked Palette, and Phase 3 renderer param unlock visual outcomes remain SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| LED razer keepalive + blackout backstop + dispatch retry + pad mutual exclusion (AWR-145) | software-tested | hardware-unvalidated | `tests/test_govee_realtime_runner.py` covers the 2 s razer keepalive, on-demand assert while active, 2-tick brightness send, and pure-vs-handoff emergency teardown (`set_brightness(0)` only on pure emergency). `tests/test_led_dispatch_coordinator.py` covers razer asserts on realtime takeover + tactical blackout and `restore_brightness`. `tests/test_led_state_manager.py` covers the latch-on-accept retry re-sending the same decision without re-ticking the director, retry-slot clearing on deck switch/idle/blackout/gate, and the idle retry starting the freewheel. `tests/test_led_pad_playback.py` covers pad auto-stop when a fresh bridge owns the strip. The ~2 s room recovery, the any-mode LAN dim, and "drops never come up dim" are SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED — they rest on device firmware behavior the operator observed 2026-07-08, and need his next live pass. |
| Govee frame engine child process (AWR-146) | software-tested | hardware-unvalidated | `tests/test_govee_frame_engine.py` covers the host (init/deactivate ordering, anchor staleness, list-param render parity, command fan-out, AWR-145-through-the-boundary brightness/activate/keepalive, EOF/shutdown teardown, heartbeat fps + degraded edge-trigger, `decode_buffer`) and the client (no caller-thread I/O + wire order, respawn intent replay, hung/stuck respawn, emergency queue-jump, status merge, stop escalation). `tests/test_govee_frame_engine_integration.py` spawns a real child: `achieved_fps` = 60.9 this machine (>= 55 gate), and an orphaned child exits 0 within 2 s of parent EOF. Task 6 dim proven in `tests/test_led_dispatch_coordinator.py` (`blackout_brightness()`), `tests/test_led_state_manager.py` (accepted/cloud-only-no-op/rejected), and `tests/test_govee_realtime_runner.py` (inactive runner `request_brightness(0)` reaches transport twice). Live fps under real load and on real hardware is unproven until the operator's next mix — SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| Govee frame-engine scheduling-band self-report (AWR-151 Tasks 1-2) | software-tested | hardware-unvalidated | Instrumentation only, no timing change (runner byte-identical). `tests/test_govee_frame_engine.py` covers the heartbeat carrying `band_setpriority`/`band_nsactivity`/`band_darwin_prio` with getpriority re-read + setpriority re-assert called once per heartbeat (faked module-level libc seam, live semantics recorded: `getpriority(PRIO_DARWIN_PROCESS,0)` == 0 = not backgrounded on this machine), the client passing the three scalars through `status()` with safe defaults pre-heartbeat, the band report logged once per (re)spawn with a failed-raise health edge, and the `_sanitize_led_adapter_status` whitelist admitting the three keys. `tests/test_govee_frame_engine_integration.py` (CI-skipped) asserts the real child's first heartbeat carries `band_setpriority` True + a `band_darwin_prio` value. This proves the band is now VISIBLE in software; it does NOT prove the raise is effective under the real launch chain — that WHY is Phase B, and what fps holds live is the operator's next mix. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| LED drop-impact transport guarantee (AWR-150) | software-tested | hardware-unvalidated | `tests/test_govee_realtime_runner.py` covers the keepalive yield: it suppresses the 2 s keepalive inside the window, the 30 s cap restores it, each of the five intents (`set_desired`/`fire_trigger`/`request_activate_assert`/`emergency_stop`/`force_deactivate`) cancels it, a cancel lets the keepalive fire again, and a yield does not block the brightness drain. `tests/test_led_dispatch_coordinator.py` covers `stage_cloud_takeover` triggering the adapter with NO `force_deactivate`/owner change/dwell bookkeeping and requesting the yield on accept, and no yield on reject. `tests/test_led_look_director.py` covers `substitute_realtime_drop` picking only realtime drop looks, advancing only the `(drop, realtime_razer)` backend cursor (plan cursor unchanged — the next real drop commit is unaffected), queuing no pair, and returning None on a cloud-only drop bank. `tests/test_led_state_manager.py` covers the end-to-end cloud drop: blackout goes tactical (realtime), the impact dispatches the RT substitute and stages the committed cloud decision, a cloud-only adapter keeps today's behavior, and a retry-then-accept stages exactly once. Existing `test_cloud_previewed_drop_still_gets_realtime_tactical_blackout` (Task 3.1) stays green. Proves the on-beat/staging logic in software only; whether cloud scenes visibly upgrade mid-drop and no blackout sticks is the operator's next mix. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| LIGHTING ENGINE v2 F1 identity + correction surface | software-tested | unvalidated | `tests/test_led_identity_v2.py`, `tests/test_led_color_engine.py`, `tests/test_color_engine_config.py`, `tests/test_led_palette_control.py`, `tests/test_runtime_status.py`, `tests/test_soundswitch_midi_input.py`, and `tests/test_streamdeck_midi.py` cover identity helpers/store, v1/v2 engine behavior, config parser gates, Stream Deck zone/manual/max-energy control, runtime commands, MIDI bindings, and layout rendering. Focused StateManager LED tests cover runtime wiring. This does not prove live Rekordbox analysis, Stream Deck hardware, Govee output, or room-visible behavior. |
| LED Pad + Template Lab | software-tested | unvalidated | `tests/test_led_pad_controls.py`, `tests/test_led_pad_playback.py`, `tests/test_led_pad_service.py`, `tests/test_led_pad_lab.py`, and `tests/test_awr260_lab_accept_wirein.py` cover pad/lab behavior plus AWR-260 Accept→production wire-in (look write, bank placement, reload command append, lab adapter fail-dark/budget). Phase 3 locked-palette coverage remains in config/engine/renderer tests. This does not prove Govee device behavior, live reload on a running show bridge, or strip restore. |
| LED white-knob round 1 (AWR-152 #2/#3/#6/#7/#8/#10) | software-tested | hardware-unvalidated | `tests/test_led_identity_v2.py` covers `derive_dressing` returning a configured `slot5_white` verbatim (no sat-floor/hue-shift) and the pure-white default when absent. `tests/test_led_config.py` covers `slot5_white` parse (valid/absent-default/malformed-fails-closed-disables-v2-only), legacy `white` keys in palettes and zones ignored without error, the example config loading clean, and `groove_diy_bright_white_chase` in the buildup bank (not groove). `tests/test_led_color_engine.py` and `tests/test_color_engine_config.py` cover the deleted `Palette.white`/`_blend_white` surface (dataclass defaults, frozen-field behavior) and the all-6-slot palate-reset dim derived from NEUTRAL's `slot5_white`. `tests/test_led_state_manager.py` covers `_led_diy_eligible_predicate` returning `None` under the v2 latch and a v2-latched automation tick passing no DIY filter to the director. `tests/test_govee_frame_renderer.py` covers `breakdown_star_twinkle` never lighting slot 5 across a multi-cycle sweep. Proves the config/engine logic in software only; whether the tinted white accents and even cloud-scene rotation read correctly in the room is the operator's next mix after mirroring the live config. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| LED pad blackout unlatch fix (AWR-154) | software-tested | hardware-unvalidated | `tests/test_led_pad_playback.py` covers `OwnershipGate.release()` sending `{"cmd": "led_clear_blackout", "reason": "led_pad"}`. `tests/test_runtime_status.py` covers `parse_command` accepting a valid `reason`, rejecting an empty `reason`, rejecting a genuinely unknown field, and `CommandReader.handle_command` passing the parsed reason (or `None` when absent) through to the callback. `tests/test_led_state_manager.py` covers the end-to-end owner-set behavior: a `led_clear_blackout` with `reason=led_pad` discards exactly the `led_pad` owner and leaves a bystander `legacy` owner blacked out. Proves the reason threads through the whole command chain in software only — not that a currently-latched-dark process actually unlatches, since `led_dispatch_policy.py` (the owner set itself) is untouched and this fix only takes effect on the next process restart. Note: the bare-clear-discards-only-legacy claim this row originally made was true only until AWR-155 changed that scope the same day — see below. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| LED bare-clear fail-open (AWR-155) | software-tested | hardware-unvalidated | `tests/test_led_state_manager.py` covers a bare `led_clear_blackout` clearing all three of `led_pad`/`drop_spotlight`/`legacy` owners at once and emitting exactly one `[RGB] blackout-clear-all` INFO log naming them (via `assertLogs`); a separate case covers the adapter's `restore_brightness()` firing when the clear-all empties the owner set. The pre-existing reasoned-clear-stays-scoped test (AWR-154) is unchanged and still green. Proves the owner-set and logging logic in software only — not that a currently-latched-dark process actually unlatches, since this fix only takes effect on the next process restart. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| LED round 2: strobe-gate rebuild + accepted-look promotion (AWR-156) | software-tested | hardware-unvalidated | `tests/test_govee_frame_renderer.py` covers the Hz gate's BPM-invariance, frame-aware widening (>=1.6 rendered frames land at least one ON frame per cycle at 28 fps), hz/duty caps, the colorway effect (solid/alternating/`_color` fallback/strobe registration), the three promotions (balloon monotonic shrink, `_head_weights` peak==1.0 across a sub-pixel sweep — the 0.53x regression guard, all 4 heartbeat color_modes writing only their documented slots, remnants ember timeline + determinism), knob #4 (single slot per pixel at all 8 rewritten sites, the untouched positional-mapping prototypes still splitting), and Task 9 (firework bursts bake pure white, nebula comets and the remnants background keep the injected tint). `tests/test_govee_realtime_runner.py` covers `frame_period_s` reaching the renderer and the EMA pulling toward a simulated stalled tick (Event-lockstep, no real wall-clock delay). `tests/test_led_config.py` covers the example config loading clean, all 7 colorway looks validating, bank membership (colorways + promotions present, the renamed/moved looks in post_drop and absent from drop, their `drop_pairs` entries gone), and `step_within_section.groove` still `true`. `tests/test_led_pad_controls.py` and the `test_led_color_engine_m2_patch_*`/`phase*` regression-lock suites were updated for the new registry/bank/rename state. Full suite: 3543 tests, same 5 known pre-existing reds (4 unrelated soundswitch pack-parity + 1 pre-existing LED live-config test), nothing new. Proves the gate math, slot routing, and config wiring in software only; the room-visible strobe feel, comet colors, and widths are the operator's next mix after mirroring the config and restarting. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| LED round 3: Hz-gate migration + rainbow/firework promotions + center-burst fix (AWR-161) | software-tested | hardware-unvalidated | `tests/test_govee_frame_renderer.py` (`Awr161HzMigrationCoverageTests`) sweeps all 18 migrated effect names (13 non-slot + 5 slot) confirming each flashes on the wall-clock Hz gate independent of `beat_pos`, hz/duty are dialable, and the C5 allowlist guard covers hz/duty for every one; `RainbowOrderedTests` covers registration, beat-locked `travel_per_beat` advance independent of `local_t`, the legacy `loop_beats` pace byte-stable when `travel_per_beat` is absent, and hue-ordered-by-position with full HSV value at the anti-aliased peak; `DropFireworkExplosionTests` covers the contrast gate itself (measured post-surge ember deviation 101/255, clears the >=60/255 bar), the surge resolving down to `bg_hold` instead of staying pinned at full, and embers being time-based (`local_t`-driven) not beat-tied. `tests/test_led_color_engine_m2_patch_d.py` replaces the stale even-pixels-only fixture with one proving both even- and odd-index pixels light up across a beat sweep, and the existing main/accent slot-band-split test is untouched (proving that discipline survived the fix). Verified against the untouched pre-session HEAD: the pre-existing 6-red baseline in the `led_color_engine`/slot-effect family is unchanged by this round; `govee_frame_renderer.py`/`config/led_look_director.example.json` and their test files introduce zero new failures. Proves the gate math, the two new effects' render math, and the pixel-coverage fix in software only; the room-visible Hz feel, rainbow pair, firework explosion, and center-burst fix are the operator's next mix after mirroring the config and restarting. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| Firework remnants sparse-tail rebuild (AWR-215) | software-tested | hardware-unvalidated | `PostDropFireworkRemnantsTests` pins exact parity with the first-eight-beat drop-chase sparkle mechanics, zero slot-5/background contribution, frame-to-frame flicker, peak lit fraction <=20% across 480 dry-rendered frames at 60 segments, and darkness from beat 8 through the cue tail. Renderer/config/pad focused suites: 180/180 green. Full discovery: 4381 tests, 6 failures + 1 error, all outside AWR-215 (two export-parity self-heal, Patch-C stale speed-law params, Patch-D slot snap, SoundSwitch slot-16 golden, missing-Enttec startup expectation, parity-oracle captures); no remnants failure. No bridge restart, live-config write, Govee packet, or room-visible validation occurred. |
| Firework remnants ember knobs (AWR-256) | software-tested | hardware-unvalidated | Extends `PostDropFireworkRemnantsTests` for hold/decay (default dark at 8; hold=16 lit at 12 dark at 16; monotonic decay scale; absent params == explicit defaults). `dim_beats` removed from allowlist. Module `tests.test_govee_frame_renderer` green. No bridge restart or hardware output by implementer. |
| True-drop LED section identity + cycling (AWR-257) | software-tested | hardware-unvalidated | `tests/test_true_drop_sections.py` (31 pure-seam cases) covers `select_true_drops` (runway filter, fail-open, empty), `drop_sections` (chorus-run end, next-true-drop clamp, ≥16-beat advance floor, T-exclusion, outro-trim M1/M2 pins, unsorted/duplicate raw), the false-marker "I Cannot" shape, the no-buildup inferred-up track, LED pool governance (section true-drop anchor + same-family ≤-tier union; scripted stand-down; explicit-anchor narrowing when `active_drop_beat` is `None`), the D6 laser byte-identity pin (`_f2_laser_tiers` reads RAW drops and ignores sections; laser + drop_presentation source never reference sections), advance cursor cycling, fired-set reset with track, exact resume landing, unique `:a{index}` role-key dedupe-once, the gate stack blocking advances under blackout/override/scripted, and `meta.drop_sections` reset parity. Full suite reconciled BY NAME to the standing baseline reds; three hard doc checks green. Proves the section math + LED narrowing/cycling and the laser/SS non-interference in software only; whether the section identity and cycling read correctly in the room is the operator's next mix. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| LED darkness-fix round: blank-role hold + reader freshness (AWR-157) | software-tested | hardware-unvalidated | `tests/test_led_state_manager.py` (`BlankRoleHoldTests`, 8 cases) covers: blank/scripted-mapped role + audible playing + a prior accepted decision suppresses the blackout dispatch (gate reason `blank_role_hold`, current look retained); not-playing and no-prior-accepted-decision both dispatch normally; emergency blackout still wins immediately; the tactical pre-drop blackout dispatches unaffected even with a prior accepted decision primed; the knob off is byte-identical (including that the Q-A log still fires); the log is edge-triggered (INFO once, DEBUG on identical repeats). `tests/test_led_config.py` (`BlankRoleHoldConfigTests`, 5 cases) covers the config parse contract. `tests/test_rb_memory_chain.py` (`ChainFreshnessTests`, 7 cases, paired with the Rekordbox readers row above) covers the reader-side freshness gate. Full suite: 3563 tests, the known six environmental reds, zero new regressions from the pre-round 3543-test baseline. Proves the dispatch-suppression and freshness-gate logic in software only; whether a blank-role blackout ever recurs live, and whether the deck-2 fallback actually re-engages on a real freeze, is the operator's next mix after his next menubar start. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| Laser Pad/frontend | partial | unvalidated | Syntax/frontend smoke tests do not prove live safety. |
| Spectral EAR benchmark harness (AWR-200, offline tooling) | software-tested | not applicable | `tests/test_spectral_ear_benchmark.py` (32 tests) covers `tools/spectral_ear_benchmark.py`: row taxonomy, explicit cited exclusions, the real fold invariant (no entry id / resolved amendment lineage in both train and test), amendment grouping through the `amends` parent link with loud missing/cyclic/ambiguous/duplicate-id warnings, the single `call_planner` boundary (positive allowlist + `assert_no_leak`) rejecting forbidden/unexpected fields on every planner call, accuracy axes staying UNAVAILABLE (never zero/PASS) with the marker axis gated on markers actually SCORED AND at least one comparable ±1/±2 perturbation (baseline decisions alone with both flip rates None read UNAVAILABLE), per-radius comparable denominators for marker sensitivity, same-title/no-content_id identity warnings, and deterministic output. Read-only offline tooling — no runtime lighting behavior, no cache/config writes; `--resolve-db` opens Rekordbox master.db and the v4 cache READ-ONLY. AWR-200 is PARTIAL: the labels carry no structured per-drop operator gold so every accuracy axis is UNAVAILABLE. After the 2026-07-11 content_id enrichment the marker axis resolves all 21 usable lineages (21/21, 158 markers; not SOL's 15/113 sample), but marker availability never flips the accuracy-axis PARTIAL gate. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| Intrinsic-hardness offline shadow descriptor (AWR-203, offline candidate) | experimental / software-tested | not applicable | `tests/test_hardness_v0.py` (28 tests) covers `hardness_v0.py` (frozen SOL3 three-path candidate) + `tools/hardness_ablation.py` (read-only ablation): B/A/R/N term math + clipping vs frozen anchors, first-8/following-8 per-term averaging and short/end-of-track window clamping, deterministic reducer selection + tie-breaking, three-path math + winning-path diagnostics, per-term track median + `MIN_STABLE_DROPS`, marker-shift range, malformed/empty/out-of-range/None-baseline and non-finite (NaN/±inf)/unshaped required series → no result (unknown reducer raises; never a phantom T3), determinism, and the AST-based zero-runtime-importer invariant (only `tools/`+`tests/` may import it). Adversarially reviewed by a 9-lens ULTRACODE workflow: zero module defects; two test-guard gaps fixed. **NON-AUTHORITATIVE / OFFLINE ONLY:** `hardness_v0.py` does no I/O and has zero runtime importers; `violence`/`.tier` stay the sole live authority — hardness never routes live, replaces tier, or feeds darkness. `H>=1.0` is path-threshold firing (in-sample, binary-T3 only), NOT a validated boundary; there is no T1/T2 split. The evaluator is read-only (resolves only current filepath+ANLZ grid via `get_cached_v4`, asserts the current-key payload's realpath+beatgrid_fingerprint, never enumerates raw cache JSON, never extracts/writes) and makes no taste-accuracy/held-out/readiness claim. A small read-only corpus run (40 tracks → 36 scored/206 drops) completed with zero identity rejects and no writes. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |
| Offline raw approach descriptors (AWR-204, offline layer) | experimental / software-tested | not applicable | `tests/test_approach_features_v0.py` (27 tests) covers `approach_features_v0.py`, the raw four-view approach-descriptor layer for the SOL2 finding-1 refactor (pre-drop shape-over-time, not a one-sample gate): rising/falling/held trajectories (slope + half-split delta sign), ringing-layer-independent-of-sub-cut (no min-masking), broad-vs-single-band and approach-floor (p10-not-p50) depth, section-relative depth, separate first-8/next-8 landed windows, track baseline + across-`genuine_drops` landing reference (no-drops → None, empty-drops → zero-ref), the track-referenced void run curve + `longest_run_below` primitive (a hole breaks the run), the ±2 marker-offset bundles (length window slides, explicit-start end-only, offset-0 == primary, `descriptor_range` stability), boundary clamp (requested vs available), short-window insufficiency (no slope), window-level `sufficient` requiring finite data not just beat slots (an all-non-finite or single-finite window → `sufficient=False` with a reason distinct from the too-few-beats case), missing series → `present=False`, non-finite filtered + counted, top-level availability requiring ≥1 finite approach descriptor (all-non-finite/missing → `available=False` with an honest reason; per-series partial availability retained), empty track unavailable-not-error, inconsistent caller inputs raising `ValueError`, determinism, and the AST+raw-text zero-runtime-importer invariant. An earlier independent ULTRACODE review reported zero module-logic defects; a later executive review found two finiteness-blind honesty gaps (top-level availability, then `WindowStats.sufficient`), each since fixed with a regression test. **RAW / OFFLINE ONLY — DECIDES NOTHING:** no class, threshold, darkness length, or live authority; missing/short/non-finite data yields an explicit unavailable/insufficient result, never a fabricated darkness event; zero runtime importers (only `tools/`+`tests/` may import it); pure, no I/O, reuses `spectral_profile.percentile`. **SOL2 finding-1 is NOT fixed by this layer** — it is only the measuring surface; no classifier, no tool (`tools/approach_feature_report.py` deliberately NOT built), no live wiring, no hardware validation. SOFTWARE-VALIDATED ONLY / HARDWARE-UNVALIDATED. |

## Required hardware validation record

Use `docs/validation/soundswitch_hardware_validation_procedure.md` and copy
`docs/validation/soundswitch_hardware_runs/TEMPLATE.md` for the SoundSwitch local-setup slice. Their
existence is not hardware evidence; only a completed operator run can change hardware status.

A hardware validation entry must include:

- commit SHA
- date
- OS version
- Rekordbox version
- SoundSwitch version
- device/fixture models
- config file path or redacted config hash
- exact test steps
- observed result
- pass/fail
- caveats
- rollback notes
